# AppShell 历史包归档

本目录不可覆盖地保存各正式版本的 `.nupkg`、`.snupkg` 和 Demo ZIP，用于审计、调试和回滚。它不是其他项目或 AI 的默认包源。

消费项目始终使用 `../../z-Package-AppShell/feed` 中的当前正式四包；只有回滚到明确的旧版本时，才临时使用本目录。第三方依赖仍从 NuGet.org 恢复。

同一版本号禁止覆盖。需要修复时发布新的补丁版本。
