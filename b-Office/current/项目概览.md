# AppShell 项目概览

更新日期：2026-08-07

## 一句话定义

AppShell 是供 OneHistory 桌面应用和服务宿主固定版本消费的通用 .NET 8/WPF 壳框架，统一提供
命令、停靠布局、日志、设置、面板、模块、MCP/Web 接入与服务生命周期能力；资源浏览由独立模块提供。

## 目标与边界

### 目标

- 以固定 NuGet 包提供稳定、可测试、可回滚的公共合同，不再复制源码作为应用底座。
- 以单一窗口模型承载中央主工作区和四边工具页，并保留布局持久化与模块生命周期。
- 以默认最小能力、安全边界、公开 API 基线和自动化门禁守住 3.0.3 冻结契约。
- 让消费方和 AI 从稳定入口定位当前包、API、命令和限制，无需扫描历史归档。

### 范围

- `AppShell.Core`、`Services`、`Shell`、`ServiceHost` 四个公共包。
- AppShell 演示宿主、根级 `b-Code-Tests` 自动化测试、隔离包消费测试和示例模块。
- 消费合同编辑源、候选打包、正式归档及 Z 级当前消费快照。

### 非目标

- 不承载 OHS、WBall 或其他消费应用的业务功能和数据模型。
- 不提供已在 3.0 删除的数据库服务、通用表窗口或 `db.*` 命令。
- CI 不自动发布包、更新正式快照、创建标签或部署消费方。
- 3.0.x 不新增公开 API 或一般性功能；后续演进进入新版本线。

## 当前里程碑

V3 冻结基线仍为 `3.0.3`（标签 `v3.0.3`），只收致命缺陷。当前源码版本为
`3.1.4`：以完成顶栏手势、前台激活、独立浮窗几何和主题外框收口的 3.1.3 为基线，统一控制台与命令集的
域筛选，并让控制台长文本随窗格宽度软换行。

| 状态 | 内容 |
| --- | --- |
| 已完成 | 3.0.3 正式发布、OHS/WBall 消费复验、冻结提交与标签推送 |
| 已完成 | GitHub Actions 冻结门禁与公开 API 反向断言 |
| 已完成 | 3.1.0 界面改造主体落地并纳入 3.1.1：Debug/Release 0 警告、0 错误，103/103 |
| 已完成 | 3.1.1 命令管线收口、资源/电机演示删除、模块管理归属和断头指令审计 |
| 已完成 | 原 3.2 双进程方案已在 3.1.2 落地：后台权威宿主、前端单实例、日志/模块修订事件与全局快捷键；方案归档于 history |
| 已完成 | 3.1.2 命令目录远程 JSON 解码修复；Release 构建与 Debug/Release 109/109 测试回归 |
| 已完成 | 测试外置到 `b-Code-Tests`；顶栏采用 250ms 双击和最大化两倍拖动阈值；候选验证 115/115、PackageSmoke PASS |
| 已完成 | 3.1.3 基线 `be49828a` 与交互收口 `6b56f529` 已提交；普通主窗口顶栏即时拖动，主窗口最大化及独立浮窗保留 120ms 延时；浮窗恢复、嵌入尺寸、鼠标锚点和主题外框统一治理；双 `/` 人工审查通过；Debug/Release 135/135 |
| 已完成 | 3.1.4 命令集移除来源筛选/列；控制台与命令集共享命令总线已注册域，长文本响应式换行；浮窗状态等 UI 入口继续收进命令总线 |
| 下一步 | 审核并合并 3.1.4 PR；正式发布仍需单独批准，不直接更新 Z 快照 |

## 主要交付物

| 交付物 | 权威位置 | 完成判据 |
| --- | --- | --- |
| 框架源码 | `b-Code-AppShell/src/` | Debug/Release 0 warning、0 error，全部测试通过 |
| 公共包 | `b-Publish/` | manifest、包身份、大小和 SHA-256 全部匹配 |
| 当前消费快照 | `z-Package-AppShell/` | 只含一个正式版本的四个运行包和同版本合同 |
| 消费合同源 | `b-Office/package/` | 发布脚本复制后与正式副本哈希一致 |
| 现行项目合同 | `b-Office/current/` | 与 manifest、源码、测试和冻结边界一致 |

## 关键约束

- 平台：Windows、WPF、`net8.0` / `net8.0-windows`、win-x64 演示宿主。
- 工具链：根 `global.json` 固定 .NET SDK `9.0.315`；C# 检出行尾固定为 CRLF。
- 兼容性：3.1.1 对资源/Workspace 公开面是破坏性收缩；3.1.2 修复双进程命令目录数据解码；3.1.3 与 3.1.4 不改变公开 API。
  模块、命令、停靠与 MCP 由现行消费合同固定。
  四份 `PublicAPI.Unshipped.txt` 仍只有 `#nullable enable`，公开基线以 shipped 文件为准。
- 默认消费合同：`EnableMcp=false`、`EnableModules=false`，消费方按需显式启用；仓库内 AppShell 独立可执行宿主
  明确设置 `EnableModules=true`，提供模块管理页并承担唯一模块生命周期。
- 安全：MCP 暴露需要描述符显式允许；敏感值不得进入日志、历史或未授权响应。
- 发布：正式发布脚本只在人工批准、来源洁净时执行，且不自动推送 NuGet.org。

## 外部依赖与消费方

| 依赖或消费方 | 固定版本或位置 | 用途 | 权威位置 |
| --- | --- | --- | --- |
| .NET SDK | `9.0.315` | 构建、测试、格式化 | `global.json` |
| AvalonDock | `4.72.1` | Shell 停靠布局 | `b-Code-AppShell/Directory.Packages.props` |
| OneHistoryStudio | `2.7.9 / AppShell 3.0.3` | 正式桌面消费回归 | 020 项目自身证据 |
| WBall | `AppShell.Shell [3.0.3]` | 独立桌面消费回归 | 022 项目自身证据 |

## 已知问题与最近验证

| 编号或日期 | 类型 | 结果或影响 | 关闭条件或证据 |
| --- | --- | --- | --- |
| DIR-EXC-001 | 目录边界 | 根 `eng/`、`Unused/` 和本地 `artifacts/` 是 manifest 明确排除的既有非活动目录 | 不作为新入口；当前不迁移、不删除 |
| 2026-08-07 | 本地验证 | 3.1.3 Debug/Release 0 warning、0 error，外置测试各 135/135，格式与公开 API 门禁通过；双 `/` 人工审查通过 | `b-Office/current/验证合同.md` |
| 2026-08-07 | 本地验证 | 3.1.4 双配置构建与 142/142 通过；格式、项目合同、API 门禁、候选四包与 PackageSmoke 通过 | `b-Office/current/验证合同.md` |
| 2026-07-30 | 远端 CI | AppShell Freeze Gate #3 全部通过，耗时 3m11s | GitHub Actions run `30469169167` |
