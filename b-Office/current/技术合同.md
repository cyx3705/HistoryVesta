# HistoryJanus 技术合同

## 现行需求

### REQ-001 宿主化运行（3.9.2 当前版本；三个窗口）

- Janus 必须作为 `HistoryJanus` 模块由 HistoryVulcan 加载，不创建独立应用、后台服务或第二套 Shell。
- UI 宿主注册 `overview`、`graph`、`projops` **三个**单例窗口。Meta 文件夹浏览并入 `overview`；
  提交 DAG 由 `graph` 窗口承载，**并入宿主控制台标签组**，不占中央主窗口（DEC-013）；
  `projops` 底部以同一行分段切换三个面板：**Git 文件规则 / 分支历史 / GitHub**。
- **默认落位（3.7.0）**：`overview` 占**中央工作区**（`DockSide.Center`），
  `projops` 停靠**左侧**、默认宽度占比 `0.38`。
  `overview` **不得**声明 `DefaultTabTarget`：此前它并入 Mcp 标签组，而那个窗口由
  HistoryMercury 提供，模块装载顺序不保证它先到——先到就并入、没到就回退右侧停靠，
  首次启动落位因此不确定（服务日志中的「标签组目标 mcp 不可用」）。
  中央区不依赖任何别的模块，落位才是确定的。
  3.4.0 引入、3.6.0 仍存在的独立 `github` 窗口在 3.7.0 退役（DEC-008）。
- UI 与无 UI Service 宿主都必须注册同一组 Janus 业务命令，来源统一为 `module:HistoryJanus`。
- 分段面板不得各自显示重复标题：分段按钮已给出面板名，上方选中项标题已给出当前项目，
  面板内不再重复这两项。

### REQ-002 业务能力

- 模块提供项目列举、创建、提交、推送、继承树命令、Meta 文件夹浏览、分支历史、回滚和 Git 文件规则治理，
  以及 3.9.0 起的只读分支 DAG；继承树与分支历史不再提供独立页面。
  DAG 由 `graph` 窗口承载，并入宿主控制台标签组；主线是当前编号分支，提交从相对父分支的分叉点截断，
  泳道按平行分支（主要是 `ai/<项目>/`）动态增加。已合并平行分支保留合并前历史行，右侧不再画端点；
  `--no-ff` 合并后即使分支 ref 已删除，仍从第二父还原该行（DEC-013）。
- **指令分类（3.9.2，DEC-012 / DEC-013）**：`janus` 域业务类为 `proj`、`gitrule`、`history`、`github`、`graph`；
  目标 39 条业务命令 + 宿主投影的两段式只读命令 `janus.status`，运行时 40 条，
  实现与本合同同版本落地。一律三段式 `janus.<类>.<方法>`（3.5.0 DEC-007 确立）。
  模块不注册诊断或自动化辅助指令：日志承压使用宿主 `vulcan.log.flood`，
  不在业务模块内重复实现。`aiworktree` 类预留给 3.10.0，3.9.x 不得注册。
- 页面通过宿主 `CommandBus` 调用业务命令；跨 HTTP 的结构化结果必须先从 JSON 投影为页面模型。
- **Git 执行等待（3.7.0，DEC-010）**：产品 `GitRunner` 不设置墙钟超时；`add`、`commit`、
  `push`、`fetch` 和其余 Git/Git LFS 操作必须等待进程自行结束，只响应宿主传入的取消令牌。
  GitHub 连接诊断的用户指定超时和验证套件的挂死检测不属于产品 Git 操作，继续保留。
- **文件规则离页保存（3.7.0，DEC-010）**：规则页内的勾选、新增只修改内存草稿；
  规则面板从可见变为不可见、切换项目、刷新或删除前，才通过一次 `janus.gitrule.batchset`
  预览和一次宿主确认批量写入。并发触发必须复用同一个保存任务；取消、校验失败或写入失败时
  保留草稿并恢复规则分段，不能把失败当作已保存。
- 卸载或热重载后不得残留窗口或旧命令注册。

### REQ-003 正式交付

- `b-Publish/current/HistoryJanus` 只保存本次验证候选；旧正式版本只以扁平历史包保存在 `b-Publish/history`。
- `z-HistoryJanus` 只保存最新正式快照。
- 正式快照只允许 `HistoryJanus.dll`、`HistoryJanus.xml`、`module.manifest.json` 和 `SHA256SUMS`。
- 部署只把正式快照中的运行文件同步到 HistoryVulcan UI 与 Service 两个模块槽；API 文档由源码
  `b-Office/package` 与 HistoryDiana 的消费文档镜像托管，不复制进 z 快照。

### REQ-004 项目总览显示工作树状态

- 项目总览以 `janus.proj.list status=true` 列出编号项目，并行执行各工作树的只读
  `git status --porcelain=v1 --untracked-files=all`，同时覆盖已跟踪修改和未跟踪文件。
- 返回的每个 `WorktreeInfo` 以可空 `IsClean` 表示状态：`true` 为干净，`false` 为有改动，
  `null` 为目录缺失、Git 失败或取消导致的未知；`WorktreeStatusMessage` 提供悬停诊断。
- 项目总览使用 `✓` 表示干净、`×` 表示有改动、`?` 表示未知；点击“刷新项目”必须重新读取状态。
- 单个工作树状态检查失败不得阻断其余项目展示，也不得把未知猜测为干净或有改动。
- `status` 默认 `false`，项目操作页等只需要名称的消费者不承担额外状态扫描。

### 质量属性

| 编号 | 属性 | 可度量要求 | 验收方式 |
| --- | --- | --- | --- |
| QA-001 | 可装载性 | UI/Service 两种宿主均发现 1 个模块并注册 40 条运行时命令（39 条业务命令 + `janus.status`，3.9.2 目标；实现与本合同同版本落地） | ModuleSmoke Debug/Release |
| QA-002 | 可卸载性 | 卸载后 Janus 窗口和命令残留为 0 | ModuleSmoke |
| QA-003 | 供应链 | 正式树每个文件均由 manifest 与 SHA256SUMS 双重覆盖 | 发布与部署脚本复验 |
| QA-004 | 体积边界 | 正式包不含 EXE、PDB、HistoryVulcan DLL、deps/runtimeconfig | 静态包门禁 |
| QA-005 | 验证执行效率 | 互相独立的 Smoke 套件并行执行；整轮墙钟时间不回退到串行水平 | Smoke 运行器打印每套耗时 |
| QA-006 | 挂死可见性 | 任何套件超时都产出指名失败，不得整轮静默停住 | Smoke 每套具名超时 |
| QA-007 | Git 超时归属 | 产品 Git 调用不存在应用级秒数上限；只由 Git/Git LFS 结果或宿主取消结束 | TestArchitecture Smoke + GitRunner 取消路径 |
| QA-008 | 规则批量交互 | 页面内连续编辑不写盘；一次离页最多产生一次预览和一次应用，失败保留 dirty 状态 | ProjectOperations Smoke |
| QA-009 | 工作树状态可见性 | 总览对干净、已跟踪修改、未跟踪文件和恢复干净给出正确状态；未知不误报 | RepositoryTargets Smoke |

## 系统架构

| 组件 | 路径或位置 | 职责 | 输入 | 输出 |
| --- | --- | --- | --- | --- |
| 模块入口 | `b-Code-Studio/Module` | 接收宿主上下文，注册命令和窗口 | `IModuleContext`、`IShellUiRegistrar` | 命令注册与三个页面 |
| 业务组合 | `b-Code-Studio/StudioBusinessComposition.cs` | 组合项目、Git 规则、历史、GitHub 与图谱命令 | 宿主设置、日志、数据根 | 3.9.2 目标 39 条业务命令（`janus.*`，五类含 `graph`）；组合根注册 `graph` 命令；宿主另投影 `janus.status`；实现与本合同同版本落地 |
| 页面 | `b-Code-Studio/Views` | 展示项目与 Git 工作流 | 宿主 CommandBus 结果 | WPF 页面状态 |
| 验证 | `b-Code-Verify` | 契约、功能和模块生命周期回归 | 源码与候选包 | 通过/失败结果 |
| 正式消费区 | `z-HistoryJanus` | 保存最新模块和 API 合同 | 已验证候选 | 跨项目可消费快照 |

### 关键流程

1. HistoryVulcan 从自身 Modules 目录读取 manifest 并装载 `HistoryJanus.dll`。
2. `IModuleContextAware.Attach` 使用宿主 registry、bus、settings、log 与 data directory 注册业务组合。
3. `IUiModule.CreateUi` 把三个页面登记到宿主布局；项目总览同时消费项目和 Meta 列表，分支图谱并入控制台标签组并共享当前编号项目，项目操作页以同一行分段内嵌 Git 文件规则、分支历史与 GitHub 连接面板（GitHub 面板直连模块内连接服务，写操作仅限页面），其余交互只通过宿主总线执行业务命令。
4. 发布管道验证源码、模块生命周期和包集合后提升到 z 级正式消费区。
5. 部署管道原子更新 UI/Service 两个模块槽，并保留上一正式部署用于回滚。

### 不变量与生成物边界

- 唯一产品版本源：`b-Code-Studio/JanusVersion.props`，当前开发版本为 `3.9.2`。
  版本沿革：3.4.0 把 GitHubConnection 并入 Janus 成为第三页；3.5.0/3.5.1 指令全量改为
  `janus.<类>.<方法>` 三段式；3.6.0 分支名以 worktree 路径为唯一权威、总览挂入中央区工具页、
  github 页去 Tab、总览显示最近提交描述；3.7.0 收敛页面与指令类并重整测试执行效率；
  3.9.0 增加 `graph` 类与总览内嵌 DAG（DEC-012）；3.9.1 将图谱改为独立 `graph` 窗口，
  主线改为当前编号分支，泳道按平行分支动态增加；3.9.2 将图谱并入控制台标签组，
  并从合并第二父还原已删除平行分支的历史行（DEC-013）。
  （3.4.0–3.6.0 的需求此前未写入本合同，3.7.0 一并补记。）
- 模块 manifest 源：`b-Code-Studio/Module/module.manifest.json`，版本必须与版本源一致。
- HistoryVulcan API 权威源：`2026-023-HistoryVulcan/z-HistoryVulcan`；Janus 不复制其运行库。
- 可重建生成物：`bin/`、`obj/`、`b-Publish/work`、`b-Publish/quarantine`。
- 正式消费快照：`z-HistoryJanus`。
