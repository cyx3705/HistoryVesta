# v245 快照(V2.4.5 结构化结果加法接入后)

> 采集时间:2026-07-26 17:36:57 +08:00
> 对账基准:`../v245-before-sameenv/`(**同环境**,17:36:30 采集,间隔 27 秒)

## 三个快照目录的关系(重要)

| 目录 | 网关 | 环境 | 用途 |
|---|---|---|---|
| `v245-before/` | V2.4.4(旧部署实例,11:56 启动) | 113 命令 / 83 暴露(**4 模块**) | M0 原始基线;ToolRelay 1.0.0 版本哈希留档 |
| `v245-before-sameenv/` | V2.4.4(回退包 EXE 重启采集) | 119 命令 / 104 暴露(**7 模块**) | **最终对账基准** |
| `v245/`(本目录) | **V2.4.5**(新部署) | 119 命令 / 104 暴露(7 模块) | 交付后快照 |

**为什么多出 sameenv 基线**:旧部署实例启动于 11:56,晚于它启动时刻落入
`Modules/` 的 DemoModule / ToolAlpha / ToolBeta 三个历史测试模块未被装载;
部署重启后 7 个模块全量装载,命令 113→119、暴露 83→104。这是**模块目录漂移**,
与网关改动无关(v244 README 早有先例:「运行时总数随模块目录漂移,只作记录不作判据」)。
为保住「同环境零差异」判据,用回退包 EXE 在**相同 7 模块环境**下补采了 sameenv 基线,
新旧网关唯一变量就是网关代码本身。

## 对账结果(sameenv → v245,23 条免参只读工具)

**23/23 PASS,0 失败。** 逐条判据:

| 判据 | 结果 |
|---|---|
| 块数与 isError 逐条一致 | ✅ 23/23 |
| 确定性工具 content 文本**字节级相等**(含 command_list 47953 字符 JSON 块) | ✅ |
| 旧网关全部响应**无** structuredContent | ✅ 23/23 |
| 新网关 12 条双块工具**全部有** `structuredContent`,且 `data` 与 `content[1]` 解析后相等(JavaScriptSerializer 双侧同路径规范化比对) | ✅ 12/12 |
| 单块/报错工具不带 structuredContent | ✅ |
| McpState 逐条对账 | ✅ **119 条零差异** |
| 核心 readonly | ✅ 恒为 **32** |

非确定性工具(`history` `ToolKit_Now` `ToolKit_NewGuid` `win_list` `proj_list`)
只比块数与结构,内容漂移属预期(时间/随机数/状态)。

## 交付环境

| 项 | 值 |
|---|---|
| 部署 | `C:\OneHistory\OneHistory-Push\OneHistoryStudio`,41 文件与 b-Publish 逐一 SHA-256 相等 |
| EXE | ProductVersion **2.4.5**,SHA-256 `F34DE7E52CAC78D0A7B2CB71729681FCB26E734C6EE6D70D92B12016CC539E10` |
| 回退包 | `OneHistoryStudio-rollback-2.4.3-20260726-172932`(41 文件,EXE SHA-256 `3B045ED4...` 与发布记录一致) |
| 命令手册 | 与 v245-before 手册 diff **仅版本号一行**(2.4.3→2.4.5) |
| ToolRelay | **1.0.1** 在槽,哈希 `0A789F3DC11F76ADCECD41661ABFD5851CDFB6E7267D7C162D78D40757143506` 与构建产物一致 |

## 部署中发现并处置的遗留问题(如实留档)

`settings.json` 的 `mcp.port` 在 12:13:42(V2.4.4 交付测试时段)被某个日志被
多实例限制吞掉的实例改写为 18744。旧实例 11:56 启动时已把 8737 读入内存,
故一直未暴露;本次部署重启后新实例按设置起在 18744,是**任何一次重启都会踩中**的
遗留配置漂移,与 V2.4.5 改动无关。已在实例关闭状态下将设置改回 8737 并验证生效。
