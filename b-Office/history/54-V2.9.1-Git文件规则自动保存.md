# V2.9.1 Git 文件规则自动保存

> 日期：2026-08-04
> 状态：实施与开发候选验证完成

## 目标

移除文件规则的独立“保存修改”动作，让表格编辑和新增规则在短暂防抖后自动批量保存；同时把“添加规则”移到
扩展名输入框所在行，减少规则操作条长度。OHS 提升为 `2.9.1`，AppShell 保持 `3.0.3`。

## 实现

- 删除 `SaveRuleButton` 与 `OnSaveRuleClick`。
- 使用单个 600ms `DispatcherTimer` 合并连续 Git/LFS/LF 属性变化；一次计时只提交一批有效脏行。
- 自动保存继续调用 `git.rule.batch-set` 的 `apply=false` 预览和 `apply=true` 应用，保留既有本机确认边界。
- 保存成功后接受当前快照，不强制重扫表格；覆盖率需要时由“刷新规则”统一重扫。
- 保存失败或取消确认时保留脏行；切换项目、刷新项目、刷新规则和删除规则前先尝试完成待保存批次，失败则
  阻止后续动作。
- 新增规则默认普通 Git，并进入同一自动保存队列；尚未落盘的草稿仍可直接删除。
- `PatternBox` 与 `AddRuleButton` 使用同一行两列布局；规则操作条只保留刷新、审查、同步基线和删除。

## 测试

- 静态断言保存按钮、保存事件和未保存 `MessageBox` 不存在。
- 静态断言添加按钮与扩展名输入框同一行，剩余四个操作按钮保持单排横向滚动。
- 验证 600ms 单一防抖器、批量命令、失败保留和前置动作等待链仍在。
- Debug 构建、Contracts、`RepositoryTargets`、`GitRules`、`PromptGovernance`、`Docking` 和
  `VersionProjection` 通过后生成开发候选。
- 本轮不执行 Release、完整 22 次 Smoke、正式发布、部署、开机自启动、真实推送、UAC 或双机 LAN 测试。

## 实施结果

- Debug 构建 0 warning、0 error；Contracts 3/3。
- `GitRules`、`RepositoryTargets`、`PromptGovernance`、`Docking`、`VersionProjection` 全部通过；实际批量保存
  回归确认快速 LFS/LF 联动只产生一次预览和一次应用，并保存最终状态。
- 运行时命令手册保持 81 条，SHA-256=
  `47890DE011FB7D341AE25EDDC6092D7BECF0979527813BA838ADCBDF4A28C437`。
- Debug 开发候选已生成到 `b-Publish/candidate`，版本 `2.9.1.0`，候选 EXE SHA-256=
  `70F95B5A2580836F368A548585A2C16646BA5E8CAF0F959BE37BB78629C3AC7D`。
- 正式 `z-Package` 与部署目录继续保持 2.9.0，本轮未执行正式发布或部署。
