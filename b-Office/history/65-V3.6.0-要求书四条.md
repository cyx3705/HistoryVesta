# V3.6.0 要求书四条

> 状态：候选验证通过，双槽 DLL 已部署（未提升 z）

## 目标

1. 分支名唯一权威：路径一律走 `git worktree` 的 `WorktreePath`；目录名≠分支名时总览警示。
2. 项目总览默认挂入中央区，但是工具页（`DockSide.Tab` + `DefaultTabTarget=mcp`），不是主文档窗口。
3. github 页去掉 Tab：单页聚合；账号/公钥用选择框；诊断弹窗。
4. 总览最右列显示最近一次提交描述。

## 验证

- [x] Contracts / Smoke / ModuleSmoke
- [x] 双槽 DLL 部署（未提升 z）
