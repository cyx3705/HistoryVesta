# V2.8.4 活动坞迁出遗留修复

> 日期：2026-08-02  
> 状态：待排期  
> 关联：`50-V2.8.4-活动坞服务侧常驻.md`

活动坞迁出为独立模块 `ActiveDock 1.0.0` 后，构建与 Smoke 全绿，但以下事项在本轮明确未做或未验证。
按用户决定，这些不阻塞 2.8.4 交付，集中记录在此，后续单独排期。

## 未验证项

| 编号 | 事项 | 风险 | 处理方式 |
|---|---|---|---|
| L-01 | 服务宿主真机建窗未实测。所有验证止于构建与反射级 Smoke，没有真正跑起服务宿主看到活动坞 | 高。这是本次改动的核心目的，若不成立整轮无效 | 部署后首个动作：只启动 `--service-host`，确认活动坞出现 |
| L-02 | `dock.hide/show/pin/refresh` 对活动窗口是否立即生效未实测。改动前失效的结论也只是源码推导 | 中。属于顺带修复，未验证则无法宣称已修 | 与 L-01 同批验证，改动前后各测一次以坐实结论 |
| L-03 | 模块热重载（`tool.sync` 触发）后活动坞是否正确销毁重建未实测 | 中。`DestroyUi` 只关窗不清静态 `_window` 以外状态，残留窗口会表现为重影 | 连续两次 `tool.sync name=ActiveDock` 观察窗口数量 |
| L-04 | `svc.stop` 后活动坞消失、重启 `--service-host` 后恢复未实测 | 低 | 部署后顺带验证 |
| L-05 | 模块移除后 `dock.*` 命令与窗口是否干净消失未实测 | 低 | 与 2.8.3 遗留的模块移除专项验收合并执行 |
| L-06 | 非交互会话（SYSTEM、远程无桌面会话）下服务宿主建窗行为未定义、未测试 | 低。当前部署形态是登录自启的用户级 Run 项，不触发该路径 | 若将来改为 Windows 系统服务，必须先补该场景 |

## 未做项

| 编号 | 事项 | 影响 | 处理方式 |
|---|---|---|---|
| L-07 | OHS Smoke 未新增"服务组合承载界面模块"的断言。服务侧 `EnableUiModules = true` 目前没有回归保护，被改回 `false` 不会有测试报警 | 中。属于本次改动的核心开关，缺少门禁 | 在 Wiring 套件补一条组合根断言 |
| L-08 | `ActiveDock` 的 Smoke 缺少 `TestFormalPackage` 级断言（`z-ActiveDock` 文件集、清单字段、程序集版本对齐）。GitHubConnection 有同类断言 | 中。正式产物与清单版本可能漂移而不被发现 | 参照 `b-Code-GitHubConnection/tests/Smoke/Program.cs` 补齐 |
| L-09 | `z-ActiveDock` 与 `z-GitHubConnection` 的正式产物由手工复制生成，019 项目没有打包脚本 | 中。手工步骤易漏，且与 OHS 侧 `Publish-Studio.ps1` 的自动化程度不一致 | 为 019 增加统一的模块打包脚本 |
| L-10 | `z-StudioTools` 清单的 `artifact` 仍指向 `b-Code-StudioTools/bin/Release/...`，而 ActiveDock 与 GitHubConnection 用 `z-*` 自包含约定，两套约定并存 | 低。功能正常，但对新模块作者是坏示范 | 统一到 `z-*` 自包含约定，与 L-09 一并处理 |
| L-11 | 桌面进程仍加载 `ActiveDock` 程序集并实例化 UI 类型后弃权 | 低。多一次程序集加载，无功能影响 | 需要按槽位控制承载面，要求解冻 AppShell V3，暂不处理 |
| L-13 | 四模块聚合期的遗留副本 `2026-019/Unused/StudioTools-Merge-Legacy-20260802-0015/b-Code-ActiveDock` 与新模块并存 | 低。`Unused/` 不参与发现、构建与发布 | 确认新模块稳定后清理 |

## 建议顺序

先做 L-01、L-02，这两项决定本轮改动是否真正成立；再补 L-07、L-08 两条门禁；L-09、L-10 合并为一次 019
打包治理；其余按优先级自然排期。

`current/使用说明.md` 已在本轮补写"活动坞由后台服务承载、服务未运行则不显示"的说明，不再列为遗留项。
