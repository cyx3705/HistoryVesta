# V2.8.4 活动坞迁出遗留修复

> 日期：2026-08-02  
> 状态：L-01 至 L-03 已于 2026-08-02 实机验证通过并关闭；L-14、L-15 为部署中新发现，待排期  
> 关联：`50-V2.8.4-活动坞服务侧常驻.md`

活动坞迁出为独立模块 `ActiveDock 1.0.0` 并部署 2.8.4 后，核心目标已实机验证成立。以下事项仍未做或未验证，
集中记录在此，后续单独排期。

## 已关闭项

| 编号 | 事项 | 结论 |
|---|---|---|
| L-01 | 服务宿主真机建窗 | 通过。只运行服务宿主时，该进程持有唯一可见顶层窗口 360×282 落在工作区右下角；桌面进程关闭后窗口继续存在 |
| L-02 | `dock.*` 命令对活动窗口即时生效 | 通过。`dock.hide` 立即隐藏、`dock.show` 立即恢复、`dock.pin` 返回"已置顶"，无需重启。迁出前的失效缺陷确认消除 |
| L-03 | 热重载后正确销毁重建 | 通过。同步 `StudioTools 1.2.0` 后活动坞消失且无残留可见窗口，同步 `ActiveDock 1.0.0` 后重新出现，全程单实例 |

## 未验证项

| 编号 | 事项 | 风险 | 处理方式 |
|---|---|---|---|
| L-04 | `svc.stop` 后活动坞消失、重启 `--service-host` 后恢复未单独执行 | 低。热重载路径已证明销毁与重建可用 | 下次运维时顺带验证 |
| L-05 | 模块移除后 `dock.*` 命令与窗口是否干净消失未实测 | 低 | 与 2.8.3 遗留的模块移除专项验收合并执行 |
| L-06 | 非交互会话（SYSTEM、远程无桌面会话）下服务宿主建窗行为未定义、未测试 | 低。当前部署形态是登录自启的用户级 Run 项，不触发该路径 | 若将来改为 Windows 系统服务，必须先补该场景 |
| L-16 | GitHubConnection 窗口的停靠位置与 readonly 投影未逐项复核，只确认模块加载为 `github 1.0.1` | 低 | 下次打开桌面前端时目视确认 |

## 未做项

| 编号 | 事项 | 影响 | 处理方式 |
|---|---|---|---|
| L-07 | OHS Smoke 未新增"服务组合承载界面模块"的断言。服务侧 `EnableUiModules = true` 目前没有回归保护，被改回 `false` 不会有测试报警 | 中。属于本次改动的核心开关，缺少门禁 | 在 Wiring 套件补一条组合根断言 |
| L-08 | `ActiveDock` 的 Smoke 缺少 `TestFormalPackage` 级断言（`z-ActiveDock` 文件集、清单字段、程序集版本对齐）。GitHubConnection 有同类断言 | 中。正式产物与清单版本可能漂移而不被发现 | 参照 `b-Code-GitHubConnection/tests/Smoke/Program.cs` 补齐 |
| L-09 | `z-ActiveDock` 与 `z-GitHubConnection` 的正式产物由手工复制生成，019 项目没有打包脚本 | 中。手工步骤易漏，且与 OHS 侧 `Publish-Studio.ps1` 的自动化程度不一致 | 为 019 增加统一的模块打包脚本 |
| L-10 | `z-StudioTools` 清单的 `artifact` 仍指向 `b-Code-StudioTools/bin/Release/...`，而 ActiveDock 与 GitHubConnection 用 `z-*` 自包含约定，两套约定并存 | 低。功能正常，但对新模块作者是坏示范 | 统一到 `z-*` 自包含约定，与 L-09 一并处理 |
| L-11 | 桌面进程仍加载 `ActiveDock` 程序集并实例化 UI 类型后弃权 | 低。多一次程序集加载，无功能影响 | 需要按槽位控制承载面，要求解冻 AppShell V3，暂不处理 |
| L-13 | 四模块聚合期的遗留副本 `2026-019/Unused/StudioTools-Merge-Legacy-20260802-0015/b-Code-ActiveDock` 与新模块并存 | 低。`Unused/` 不参与发现、构建与发布 | 确认新模块稳定后清理 |
| L-14 | `SE2SW 3.1.2`（补空守）已构建并通过 Smoke，但无法入槽，运行中的 `3.1.1` 仍在服务宿主抛空引用告警 | 中。服务日志常驻一条 UI 模块告警，`50` 号文档该项验收未通过 | 先 `tool.remove name=SE2SW` 释放命令域，再 `tool.sync name=SE2SW`。`tool.remove` 是 dangerous 命令，需要人工二次确认 |
| L-15 | `ToolManifest` 的保留域校验拿清单名比对已注册命令域，导致"清单名与自身命令域同名"的模块自锁，永远无法原地更新 | 中。是 OHS 的设计缺陷而非本次引入；当前只有 SE2SW 命中（清单名 `SE2SW` / 命令域 `se2sw`） | 校验时排除该清单自身已占用的域，或要求清单名与命令域不同名并在文档中写明 |

## 部署中新发现

L-14 与 L-15 是 2.8.4 实机部署时才暴露的，不在原计划内。

带界面的模块共有三个而不是两个：除 ActiveDock 与 GitHubConnection 外，`SE2SW` 也是停靠型 UI 模块。本轮只给
GitHubConnection 补了空守，漏掉 SE2SW，导致服务宿主打开界面承载后它空引用。框架对每个模块的 `CreateUi`
单独 try/catch 并降级为 warn，因此不崩溃，桌面侧 SE2SW 窗口也不受影响，但服务日志会常驻该告警。

源码已按同样方式修复并提升到 `3.1.2`（`SE2SWUiModule.CreateUi` 在 `ShellUi` 为空时直接返回），Release 构建
0 warning / 0 error、`SE2SW.Smoke: PASS`。受 L-15 阻塞未能入槽。

## 建议顺序

先做 L-14（消除常驻告警，需要人工确认一次 `tool.remove`），再做 L-15（消除自锁的根因）；随后补 L-07、L-08
两条门禁；L-09、L-10 合并为一次 019 打包治理；其余按优先级自然排期。

`current/使用说明.md` 已在本轮补写"活动坞由后台服务承载、服务未运行则不显示"的说明，不再列为遗留项。
