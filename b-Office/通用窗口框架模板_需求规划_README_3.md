# AppShell · 通用程序窗口框架 —— 需求规划文档

> 文档性质：需求规划（README），不含实现代码
> 技术栈（已定）：C# / .NET 8.0 ＋ WPF ＋ AvalonDock 4.72.1 二次封装 ｜ 交付：独立版本包
> 版本：v1.3 ｜ 日期：2026-07-28 ｜ 状态：全部决策点已关闭；AppShell 3.0.0 独立冻结
> 修订轨迹：v0.1 需求草案 → v0.2 技术选型（Q1–Q3 定稿）→ v1.0 全部决策关闭 → v1.1 Q13 改判为 .NET 8 → v1.2 V2.4.0 唯一源码治理 → v1.3 AppShell 3.0.0 独立冻结
>
> 文件名保留“模板”字样用于兼容既有链接；本文件的现行规则以 §14.4 为准。

---

## 0. 一句话定义

AppShell 是一个**通用桌面程序框架**：内置工业软件风格的多窗口停靠系统，与"一切操作皆指令"的统一指令架构，预置四类标准窗口（主窗口 / 控制台窗口 / 控制窗口群 / 资源窗口）。3.0.0 起框架在 023 独立维护，消费方统一通过版本化包复用。

---

## 1. 背景与目标

### 1.1 背景

- 后续会持续开发多类桌面程序（小工具、游戏、工业软件、上位机），每次从零搭窗口、日志、数据查看等基础设施是重复浪费。
- 工业软件中被长期验证的"多窗口自由停靠"交互范式，覆盖了绝大多数桌面程序的窗口设计需求。
- 需要一个统一底座：新程序在第一天就拥有完整的窗口体系、指令体系、日志体系、数据查看体系。

### 1.2 设计目标

| 编号 | 目标 |
|---|---|
| G1 | **复用**：任何新程序基于 AppShell 半天内跑出可用骨架 |
| G2 | **一致**：所有消费程序共享同一套交互语言（停靠布局 + 指令集），学会一个等于学会全部 |
| G3 | **可追溯**：任何人为操作都以指令形式留痕于控制台与日志，可查、可复现 |
| G4 | **可退化**：即使关闭其他一切窗口，仅靠控制台 + 指令仍可完成程序全部核心操作（"控制台完备性"） |
| G5 | **可扩展**：业务应用只做少数几件事——注册业务窗口与指令、声明面板、指定资源目录 |

### 1.3 非目标（框架明确不做的事）

- 不内置任何具体业务逻辑。
- 不内置设备通讯协议（串口 / TCP / Modbus 等由上位机业务程序自行实现，框架只提供指令入口、日志出口、面板承载能力）。
- 不内置图表 / 报表控件库。
- 不做权限与多用户系统（在指令总线上预留拦截器扩展点，将来可加）。
- 不做 Web 端 / 移动端。

---

## 2. 术语表

| 术语 | 含义 |
|---|---|
| Shell / 框架层 | AppShell 提供的窗口框架、指令核心与基础服务 |
| **主程序窗体（Main Frame）** | 整个应用的外层窗口（含菜单栏、状态栏），是停靠系统的容器 |
| **中央标签区** | 由 `DockSide.Center` 描述符投影为主文档的业务区，可承载画布、设计器或普通 WPF 视图 |
| 工具窗口 | 控制台窗口、控制面板窗口、资源窗口及消费方自定义窗口的统称，均可停靠 / 浮动 / 隐藏 |
| 停靠（Dock） | 工具窗口吸附在主窗体的上 / 下 / 左 / 右或并入某个标签组 |
| 标签组（Tab Group） | 多个工具窗口叠放于同一区域，通过组**顶部**标签条切换 |
| 浮动（Float） | 工具窗口脱离主窗体成为独立顶层窗口 |
| 指令（Command） | 程序中一切人为操作的统一表达形式，文本语法见 §5.1 |
| 指令总线 | 指令的唯一执行通道：解析 → 校验 → 执行 → 回显 → 落日志 |
| 消费应用 | 通过固定版本包使用 AppShell 的具体程序；当前已验证 OneHistoryStudio 与 WBall |
| 工作区（Workspace） | 资源窗口所管理的根目录 |

> 注：本文档中"主窗口"若不加说明，指中央设计器区；涉及整个应用外框时写"主程序窗体"。停靠比例均相对**主程序窗体**计算。

---

## 3. 总体架构

### 3.1 分层

```
┌───────────────────────────────────────────────────────┐
│ 应用扩展层（各派生应用：小工具 / 游戏 / 工业软件 / 上位机）  │
│   · 主窗口内容   · 自定义指令   · 控制面板声明   · 业务数据  │
├───────────────────────────────────────────────────────┤
│ 标准窗口模块层                                           │
│   控制台窗口 │ 控制窗口群 │ 资源窗口 │ 自定义窗口             │
├───────────────────────────────────────────────────────┤
│ 指令核心层（一切操作的唯一入口）                            │
│   解析器 · 指令注册表 · 分发执行 · 历史 · 撤销(预留) · 脚本  │
├───────────────────────────────────────────────────────┤
│ 服务层                                                  │
│   日志服务 · 数据服务 · 文件服务 · 配置服务                 │
├───────────────────────────────────────────────────────┤
│ Shell 窗口框架层                                         │
│   停靠系统 · 布局持久化 · 菜单栏/状态栏 · 主题              │
└───────────────────────────────────────────────────────┘
```

### 3.2 架构不变量（三条铁律）

1. **一切操作皆指令**：任何改变程序状态或触发动作的人为操作（点按钮、改表格数据、开关/停靠窗口、菜单项……）必须最终经由指令总线执行。UI 控件只是指令的"可视化外壳"。
2. **控制台完备性**：凡是 UI 能完成的操作，必须存在等价指令可在控制台手动完成；反之不要求（允许有指令没有对应 UI）。
3. **窗口皆可弃**：除主窗口（中央区）外，任何工具窗口都可隐藏 / 浮动 / 重排，程序功能不因此受损。

---

## 4. 窗口系统需求

优先级标注：**P0** 必须（首版交付）｜ **P1** 应该（首版尽量）｜ **P2** 可选（后续版本）

### 4.1 停靠框架（通用行为）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| W-01 | 每个工具窗口拥有标题栏，可被鼠标拖拽独立移动 | P0 |
| W-02 | 「视图」菜单列出全部窗口，支持对每个窗口执行：显示 / 隐藏 / 浮动 / 复位到默认位置 | P0 |
| W-03 | 拖拽窗口时显示停靠引导（中心十字 + 四边指示器 + 半透明落点预览），支持五种落点：上 / 下 / 左 / 右 / 中心（并入目标标签组） | P0 |
| W-04 | 多个窗口叠放为标签组，**标签条位于组顶部**，点击标签切换当前显示的窗口 | P0 |
| W-05 | 相邻停靠区之间有分隔条，可拖动调整比例；比例按"占主程序窗体的百分比"记录，主窗体缩放时各区按比例重算 | P0 |
| W-06 | 工具窗口可拖出为独立顶层浮动窗口，支持多显示器摆放，可再拖回停靠 | P0 |
| W-07 | 布局持久化：退出时自动保存、启动时自动恢复；提供「视图 → 重置默认布局」 | P0 |
| W-08 | 命名布局方案：可保存 / 加载多套布局（layout.save / layout.load / layout.list） | P1 |
| W-09 | 自动隐藏：工具窗口可"取消图钉"收起到主窗体边缘，鼠标悬停标签滑出 | P1 |
| W-10 | 窗口与布局操作本身也走指令（win.* / layout.*）。拖拽类连续手势在**动作结束时**生成一条等价指令，避免拖动过程刷屏 | P0 |
| W-11 | 同一类工具窗口允许多实例（主要服务于控制面板；表 / 控制台 / 资源窗口默认单例，预留多实例开关） | P1 |

补充规则：

- 中央主工作区是停靠树的原生文档根；中央内容仍走统一描述符与 owner 链，消费方不直接接触 AvalonDock 文档类型。
- 工具窗口标题栏上的"关闭"按钮语义为**隐藏**（状态保留），不销毁。
- 布局类指令在控制台归入独立来源类别 `layout`，默认显示、可一键过滤（见 §4.4 C-04）。
- 布局文件损坏时自动回退默认布局并在控制台告警（见 §10 N-06）。

### 4.2 主窗口（中央设计器区）

**定位**：中央标签承载区。派生应用使用普通 `ToolWindowDescriptor` 和 `DockSide.Center` 注册业务窗口。

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| M-01 | `DockSide.Center` 窗口进入原生文档主区，与侧边工具窗共享描述符、owner 回收和布局恢复契约 | P0 |
| M-02 | 框架启用命令管理视图时，以“命令集”作为固定中央主文档；只有命令集被禁用且消费方没有中央窗口时才允许空背景 | P0 |
| M-03 | 中央区始终显示自己的页面头与单层顶部页面标签；多个中央窗口进入同一选择区 | P0 |
| M-04 | 命令集不可隐藏、浮动或移出中央；业务中央文档卸载后命令集仍留在主区 | P0 |
| M-05 | 旧布局没有中央窗、把命令集保存在四边或使用“0.01px 空文档 + 并排工具窗”时，就地迁入主文档区 | P0 |
| M-06 | 中央区尺寸变化事件对内容组件开放（游戏画布 / 绘图区需要据此重建渲染表面） | P0 |
| M-07 | 中央区需能承载自绘 / 实时渲染表面（游戏帧循环由派生应用驱动，模板保证宿主能力） | P0（WPF 承载方案见 §14.3） |
| M-08 | 四边普通工具页可拖入中央成为页面标签，并可再次拖回；嵌入位置随布局保存恢复 | P0 |

### 4.3 消费方自定义数据窗口

AppShell 3.0.0 已删除通用表窗口、`IDataService` 与 `db.*`。需要表格或数据库的消费方应在自己的工具窗口
或模块中实现数据访问和交互，框架只提供停靠、命令、确认、日志与模块托管能力。

### 4.4 控制台窗口

**定位**：程序的"素颜"，即程序最基础的样貌。所有指令回显、执行结果、运行日志汇聚于此，并且**必须支持手动输入指令**。关闭其他一切窗口，仅靠控制台即可驱动整个程序（架构不变量 2 的落点）。

**输出区**：

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| C-01 | 回显每一条被执行的指令，带时间戳与来源标签：`[UI]` `[手动]` `[脚本:文件名]` `[layout]` | P0 |
| C-02 | 显示指令执行结果 / 返回摘要 / 错误信息，错误红色高亮 | P0 |
| C-03 | 汇聚运行日志：Trace / Debug / Info / Warn / Error / Fatal 六级，按级别着色 | P0 |
| C-04 | 过滤器：按日志级别、按来源类别（UI / 手动 / 脚本 / layout / 各指令域）、按关键字；layout 类可一键屏蔽 | P0 |
| C-05 | 滚动缓冲上限（默认 5 万行，超出丢弃最旧），列表 UI 虚拟化保证性能 | P0 |
| C-06 | 右键菜单：复制所选、清屏（等价 cls 指令）、导出当前可见内容到文本文件 | P1 |
| C-07 | 自动滚动开关：新内容到达时是否跟随到底部 | P0 |

**输入区**：

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| C-10 | 单行指令输入框，Enter 执行 | P0 |
| C-11 | 历史记录：↑ / ↓ 翻阅指令历史；历史可持久化（条数可配，默认 500） | P0 |
| C-12 | `help` 列出全部指令；`help <指令>` 显示参数说明与示例 | P0 |
| C-13 | Tab 自动补全指令名与参数名 | P1 |
| C-14 | 粘贴多行文本时按行拆分为多条指令顺序执行 | P1 |
| C-15 | 全局快捷键聚焦控制台输入框（默认 Ctrl + `，可配置） | P1 |

### 4.5 控制窗口群

**定位**：一"群"可自由停靠的操作面板窗口。每个面板由按钮与输入控件组成，本质是**指令的图形触发器**：按钮点击 → 收集同面板输入控件当前值 → 拼装完整指令 → 交指令总线执行 → 控制台回显。

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| P-01 | 面板由派生应用**声明式定义**，以 **JSON 配置文件为主**（Q11 已定）；C# 注册 API 作为等价通道保留 | P0 |
| P-02 | 首版支持控件类型：按钮、文本框、数字框（含范围/步进）、下拉框、开关/复选、滑条、文件/目录选择、只读标签 | P0 |
| P-03 | 按钮绑定"指令模板"，模板中以 `{控件id}` 占位引用同面板输入控件的当前值 | P0 |
| P-04 | 输入控件校验：必填、数值范围、正则；不合法时相关按钮置灰并给出提示 | P1 |
| P-05 | 面板可多实例、各自独立停靠、随布局一起持久化 | P0 |
| P-06 | 控件布局采用简单行列栅格（首版不做可视化自由拖放的面板设计器） | P0 |
| P-07 | 控件状态可被指令反向驱动（panel.set），用于程序向面板回写显示（如设备状态灯、当前值） | P1 |
| P-08 | 面板配置热加载：修改配置文件后 panel.reload 生效，无需重启 | P2 |

**面板配置结构示意**（格式细节实现时再定，此处仅表达信息模型）：

```json
{
  "id": "motor",
  "title": "电机控制",
  "controls": [
    { "type": "combo",  "id": "axis",  "label": "轴",   "items": ["X", "Y", "Z"] },
    { "type": "number", "id": "speed", "label": "速度", "min": 0, "max": 3000, "default": 100 },
    { "type": "button", "label": "点动", "command": "motor.jog axis={axis} speed={speed}" },
    { "type": "button", "label": "停止", "command": "motor.stop axis={axis}", "style": "danger" }
  ]
}
```

### 4.6 资源窗口

**定位**：内嵌式资源管理器，浏览与管理**工作区根目录**（一个可配置的文件夹）。

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| R-01 | 目录树 + 文件列表（或单棵树视图），显示名称 / 大小 / 修改时间 | P0 |
| R-02 | 基本操作：打开（系统默认程序）、新建文件夹、重命名、删除（进回收站）、刷新、复制完整路径、在系统资源管理器中显示 | P0 |
| R-03 | "双击打开"行为可被派生应用接管（例：游戏编辑器中双击 .map 文件在主窗口打开该关卡） | P0 |
| R-04 | 通过文件系统监控（FileSystemWatcher）感知外部变更并自动刷新 | P1 |
| R-05 | 全部操作有等价指令：res.open / res.mkdir / res.rename / res.delete / res.reveal / res.root … | P0 |
| R-06 | 安全边界：一切操作限制在根目录以内（拒绝越界路径）；删除必须二次确认且仅进回收站 | P0 |
| R-07 | 支持将文件拖拽到主窗口或其他窗口，由接收方决定如何处理（模板定义拖放数据协议，作为扩展点） | P1 |
| R-08 | 根目录默认为 `<应用数据目录>/workspace`（Q9 已定，应用数据目录定义见 F-02），可经 res.root 指令或设置界面更改 | P0 |

---

## 5. 指令系统（核心）

### 5.1 语法（默认方案 v0）

```
域.动作 [位置参数 ...] [键=值 ...]
```

规则：

- **域** = 模块名：框架内置 win / layout / db / res / panel / log / app 等，派生应用可注册自己的域（如 motor、game）。
- 指令名大小写不敏感；参数值保留原始大小写。
- 含空格的值用双引号包裹，内部引号以 `\"` 转义。
- `#` 开头整行为注释（用于脚本文件）；空行忽略。
- 一行一条指令。

示例：

```
res.list path="素材/北京"
win.dock name=console pos=bottom ratio=0.25
motor.jog axis=X speed=200
res.rename path="配方/旧.json" to="配方/新.json"
```

> ✅ 本语法已定稿（Q12 已确认），自此成为**所有派生程序的通用语言**；后续如需变更，须按 §14.4 守则 4 的破坏性变更流程执行。

### 5.2 指令生命周期

```
文本输入 / UI事件
   → 解析（词法+参数）
   → 查指令注册表
   → 参数校验（类型/必填/范围）
   → 拦截器链（二次确认、将来的权限控制，预留扩展点）
   → 执行（同步 或 异步）
   → 结果对象
   → 控制台回显 + 日志落盘
```

约束：

- **长任务必须异步**执行，向控制台上报进度（如 `motor.home ... 45%`），全程不阻塞 UI。（P0）
- 任何指令抛出的异常都被总线捕获：**程序不崩溃**，错误进控制台与日志文件。（P0）
- 未知指令 / 参数拼写错误时，提示最接近的候选指令（"你是不是想输入…"）。（P1）

### 5.3 指令注册

- 框架内置指令组：help / cls / history / run / app.* / log.* / win.* / layout.* / res.* / panel.* / module.* / mcp.*（清单见附录 B）。
- 派生应用注册自定义指令时需提供：指令名、参数定义（名 / 类型 / 是否必填 / 默认值 / 说明）、执行体、帮助文本、是否需要二次确认、是否支持撤销。
- 指令名冲突在启动时检测并报错，禁止静默覆盖。（P0）

### 5.4 历史与撤销

- `history` 查看本次会话指令历史；历史持久化条数可配置。（P1）
- 撤销 / 重做（Q6 已定）：首版**只预留接口**——指令接口带撤销能力位、总线维护可撤销栈，不强制任何内置指令实现；整体功能列 P2。

### 5.5 脚本

- `run file=<路径>`：逐行执行指令脚本（纯文本，一行一条，支持 # 注释与空行）。（P1）
- 出错策略：默认中断并报告行号；`run file=x.txt continue=true` 时跳过错误继续。（P1）
- 意义：这是"操作可复现"的最终落地——控制台历史稍作整理即可保存为脚本，重放一次昨天的全部操作。

---

## 6. 服务层需求

### 6.1 数据服务边界

框架不提供数据库服务。消费方自行选择数据库、文件或远端 API，并通过自己的命令与工具窗口暴露能力。

### 6.2 日志服务

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| L-01 | 统一日志管道，多输出端：控制台窗口 / 滚动文件 / IDE 调试输出 | P0 |
| L-02 | 文件滚动策略：按天或按 10MB 切分，保留天数可配（默认 30 天） | P0 |
| L-03 | 指令回显与普通日志共用管道、不同类别，日志文件中可区分并可按类别检索 | P0 |
| L-04 | log.level 指令运行时调整控制台显示级别（文件始终全量） | P0 |

### 6.3 配置服务

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| F-01 | 应用设置与指令历史为 JSON；窗口布局采用停靠库序列化格式单独成文件（AvalonDock 4.x 为 XML，v5 起提供 JSON） | P0 |
| F-02 | **应用数据目录 = `%AppData%/<应用名>/`**（Q9 已定），布局 / 设置 / 历史 / workspace 均置于其下；程序目录便携模式做成配置开关（P2） | P0 |
| F-03 | 配置读写也指令化：app.set key= value= / app.get key= | P1 |

### 6.4 文件服务

- 资源窗口与 res.* 指令的公共实现层；回收站删除、根目录边界校验在此层**统一保证**，避免 UI 与指令两条路径行为不一致。（P0）

---

## 7. 菜单栏 / 状态栏（Shell 自带）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| S-01 | 菜单：文件（退出…）/ 编辑（预留）/ 视图（全部窗口开关、布局方案、重置布局）/ 工具（选项、打开数据目录）/ 帮助（指令手册 = help 的图形化版本、关于） | P0 |
| S-02 | 派生应用可向菜单追加自己的菜单与菜单项；**菜单项点击同样是发指令** | P0 |
| S-03 | 状态栏：左侧显示最近一条指令的结果摘要；右侧显示当前布局名 / 错误计数（点击错误计数跳转控制台并过滤 Error） | P1 |
| S-04 | 主题：浅色为默认；深色主题后置（Q10 已定）——低成本路径依赖 v5 Arc 主题，随 .NET 10 迁移预案（§14.1）一并评估 | P2 |

---

## 8. 四类目标程序如何落在模板上（范式映射）

| 程序类型 | 主窗口（中央区） | 控制窗口群 | 控制台 | 资源窗口 |
|---|---|---|---|---|
| 个人小工具 | 工具主界面 | 参数 / 动作面板 | 操作留痕 + 批处理脚本 | 输入输出文件 |
| 游戏 / 游戏编辑器 | 游戏画布 / 关卡编辑器 | 调试与作弊面板 | 调试指令（give / tp / spawn…） | 素材与关卡文件 |
| 工业软件 | 组态 / 监视画面 | 工艺操作面板 | 操作审计 + 手动指令 | 工程 / 配方文件 |
| 上位机 | 设备状态总览 | 设备控制面板 | 通讯日志 + 手动下发指令 | 固件 / 参数文件 |

边界说明：通讯协议栈、游戏主循环、渲染管线和业务数据视图都属**派生应用职责**。模板保证操作入口、日志出口与窗口宿主天然就位。

---

## 9. 消费应用开发流程

新建一个程序 = 选择引用方式后完成以下清单（目标：半天内出可运行骨架）：

1. 从 023 的 `z-Package-AppShell/feed` 使用固定版本 `PackageReference`；禁止跨项目源码引用或复制框架源码。
2. 新建业务 App 项目，设置应用名 / 图标 / 版本信息，并引用 `AppShell.Core`、`AppShell.Services`、`AppShell.Shell`。
3. 通过 `ToolWindowDescriptor` 注册中央或侧边业务窗口；中央窗口使用 `DockSide.Center`。
4. 注册业务指令（每条必须带帮助文本，help 才有意义）。
5. 用 JSON 声明控制面板（按钮 + 输入控件 + 指令模板）。
6. 指定资源窗口的工作区根目录。
7. （可选）追加菜单项、定制默认布局、替换主题与关于页。

`b-Code-AppShell/src/App` 必须保留为**独立演示宿主**和活文档；它既验证框架自身可运行，也作为所有框架改动的第一道 GUI 验收。真实业务程序不得在这个演示宿主中堆叠。

---

## 10. 非功能需求

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| N-01 | 平台：Windows 10 x64 及以上（WPF 已定，仅 Windows）；每显示器 DPI 感知（Per-Monitor V2） | P0 |
| N-02 | 冷启动 ≤ 2 秒（独立演示宿主，不含消费应用业务初始化） | P1 |
| N-03 | 控制台承压：持续 1000 条/秒日志涌入时 UI 不卡顿（虚拟化 + 批量刷新合并） | P0 |
| N-04 | 模块与工具窗口反复装卸不泄漏加载上下文或长期锁住 DLL | P0 |
| N-05 | 稳定性：任何指令异常不导致进程崩溃；全局未处理异常捕获 → 落日志 → 友好提示 | P0 |
| N-06 | 布局文件损坏时自动回退默认布局并在控制台告警，不阻断启动 | P0 |
| N-07 | 单进程单实例（可配置开关） | P2 |
| N-08 | 界面中文优先；界面字符串集中管理，为将来多语言留位 | P1 |

---

## 11. 验收标准（框架首版，逐条可勾选）

1. 将工具窗口停靠到左侧，比例调至 30%，**重启后布局原样恢复**。
2. 将两个工具窗口拖成一个标签组，标签条位于组顶部，点击可切换。
3. 任一工具窗口可拖出为浮动窗置于第二显示器，重启后位置恢复。
4. 命令集作为固定中央主文档；中央页头与页面标签始终可见，四边工具页可拖入中央并再拖回，旧布局恢复后命令集和嵌入页位置保持。
5. 关闭除主窗口与控制台外的全部窗口，仅用手输指令新建文件夹、显示并停靠控制面板。
6. 面板按钮回显的指令包含输入控件当前值。
7. `help` 与命令详情输出完整、可读、含示例。
8. 以 1000 条/秒注入日志持续 30 秒：UI 不冻结，滚动缓冲上限生效。
9. 资源删除在 UI 与手输路径都触发确认，并拒绝越过工作区根目录。
10. 手动破坏布局文件后启动：自动回退默认布局，控制台出现告警。

---

## 12. 里程碑建议

| 阶段 | 内容 | 出口标准 |
|---|---|---|
| M1 | AvalonDock spike（§14.2 末尾）+ 停靠二次封装（§14.2 清单）+ 中央标签区 + 布局持久化 | 验收 1 / 2 / 3 / 10 通过 |
| M2 | 指令核心 + 控制台窗口 + 日志服务 | 验收 7 / 8 通过，win.*、layout.* 可用 |
| M3 | 控制窗口群 + 资源窗口 | 验收 5 / 6 / 9 通过 |
| M4 | MCP/Web + 模块托管 | 协议、安全、热重载和前端目录集成通过 |
| M5 | 版本包发布 + 独立演示宿主 | 按 §9 清单半天走通，所有消费方固定使用包引用 |

---

## 13. 决策记录（原开放问题，v1.0 已全部关闭）

| # | 问题 | 影响面 |
|---|---|---|
| Q1 | ✅ **已定：WPF**（2026-07-11），细则见 §14.1 | 已关闭 |
| Q2 | ✅ **已定：成熟开源库二次封装**，选定 AvalonDock（Dirkster 分支），封装工作清单见 §14.2 | 已关闭 |
| Q3 | ✅ **3.0.0 起独立维护**；所有消费方使用 023 发布的固定版本包，见 §14.4 | 已关闭 |
| Q4 | ✅ **3.0.0 已移除框架数据库能力**；消费方按需自备数据窗口或模块 | 已关闭 |
| Q5 | ✅ **3.0.0：中央标签区**，业务窗口通过 `DockSide.Center` 注册 | 已关闭 |
| Q6 | ✅ **已定：只预留接口**，撤销/重做整体列 P2（§5.4） | 已关闭 |
| Q7 | ✅ 数据编辑提交策略由消费方自定，框架不再提供表窗口 | 已关闭 |
| Q8 | ✅ **已定：默认回显**，控制台提供 layout 类一键过滤（C-04 / W-10） | 已关闭 |
| Q9 | ✅ **已定：%AppData%/<应用名>/**，便携模式做成开关后置（F-02） | 已关闭 |
| Q10 | ✅ **已定：深色主题后置 P2**；其低成本路径依赖 v5 Arc 主题，需待迁移 .NET 10 后（S-04） | 已关闭 |
| Q11 | ✅ **已定：JSON 声明为主**，C# 注册 API 作为等价通道（P-01） | 已关闭 |
| Q12 | ✅ **已定：采用 §5.1 语法**，即日起为全部派生程序的通用语言 | 已关闭 |
| Q13 | ✅ **改判（v1.1）：维持 .NET 8**——开发环境不支持 VS2026，无法使用 .NET 10 工具链；搭配 AvalonDock 4.72.1 稳定版；升级 .NET 10 转为迁移预案（§14.1） | 已关闭 |
| Q14 | ✅ **3.0.0 冻结决策**：删除表窗口、数据库服务与 `db.*`；框架独立到 023；显式增加 `DockSide.Center` | 已关闭 |

---

## 14. 技术选型与工程结构（v1.0 定稿）

### 14.1 UI 框架：WPF，及 .NET 基线的新决策点（Q13）

**已定（v1.1 改判）**：WPF，目标框架 **net8.0-windows**——开发环境不支持 VS2026，暂无法使用 .NET 10 工具链（Q13）；仅 Windows 10/11 x64；应用清单启用 Per-Monitor V2 DPI。

**架构模式**：MVVM（建议 CommunityToolkit.Mvvm 处理属性与命令样板）。桥接铁律：**View → ViewModel → 组装指令文本 → 指令总线**。ViewModel 不得绕过总线直接调用服务，否则破坏架构不变量 1（一切操作皆指令）。

**⚠️ 选型调研发现的新事实（引出 Q13）**：

- .NET 8（LTS）官方支持至 **2026-11**，距今约 4 个月；.NET 9（STS）已于 2026-05 结束支持；当前 LTS 为 .NET 10（支持至 2028-11）。
- AvalonDock（Dirkster 分支）现状（2026-07 查证）：稳定版 **4.72.x** 可在 .NET 8 上使用；开发中的 **v5**（仍为 preview）目标框架为 net9.0-windows / net10.0-windows / net48，**不支持 net8**。v5 新能力与本需求高度相关：JSON 布局序列化（F-01）、官方 MVVM / 依赖注入配套包、Arc 深/浅色主题（S-04）、内置侧边栏 ToggleDockingManager（W-09 自动隐藏）。

| 路线 | .NET 基线 | 停靠库 | 说明 |
|---|---|---|---|
| **A ✅ 现行（v1.1）** | .NET 8 | AvalonDock 4.72.1 稳定版 | 受开发环境约束采用；注意基线 LTS 支持至 2026-11 |
| B（迁移目标） | .NET 10 LTS | v5 正式版后切换 | 待开发环境支持 VS2026（或改用 dotnet CLI ＋ VS Code / Rider 工具链）后执行；届时获得深色主题 / JSON 布局 / 自动隐藏；封装层保证迁移成本近乎为零 |

封装层（§14.2）保证：无论 4.x 还是 v5，业务代码零感知。

**迁移预案（v1.1 新增）**：
- 触发条件（满足其一即启动评估）：开发环境可用 VS2026 或等效 .NET 10 工具链；.NET 8 支持终止日（2026-11-10）临近。
- 迁移动作：各项目 TargetFramework 改为 net10.0-windows → 视 v5 正式版发布情况决定是否同步升级停靠库 → 全量回归 §11 十条验收。
- 风险留痕：.NET 8 于 2026-11 停止官方支持，此后模板及全部派生程序将运行在无安全补丁的基线上；迁移窗口建议不晚于 2026 年 Q4。

### 14.2 停靠库二次封装

**选定**：AvalonDock（Dirkster 维护分支，NuGet：Dirkster.AvalonDock）＋ 官方主题包（4.x 用 VS2013 系；v5 可用 Arc 系）。
**兜底备选**：Syncfusion DockingManager（商业，有社区免费许可，条件需核实）→ 再兜底为自研。

**封装原则**：Shell 对外只暴露自己的"窗口注册与布局模型"；四类标准窗口与派生应用代码**禁止**直接引用 AvalonDock 类型——这是将来换库或升 v5 的保险。AppShell.Shell 是解决方案中唯一引用 AvalonDock 的项目。

**封装工作清单（M1 范围）**：

1. 窗口注册模型：ID、标题、图标、默认停靠位与比例、单例/多例、"关闭 = 隐藏"语义（对应 W-11 与 §4.1 补充规则）。
2. 标签条置顶：AvalonDock 工具窗格（Anchorable）标签默认位于组底部，需样式改造移至**组顶部**（W-04）。
3. 比例语义换算：库内 DockWidth / DockHeight ↔ "占主程序窗体百分比"，窗体缩放时按比例重算（W-05）。
4. 布局事件 → 指令：监听停靠 / 浮动 / 隐藏 / 调比例的**完成**事件，生成等价 win.* / layout.* 指令进总线（W-10）。
   ⚠️ **防再入循环**：指令驱动布局变化 → 触发布局事件 → 又生成指令。封装层必须带"变更来源标记 ＋ 抑制期"，由指令引发的布局变更不再回声成新指令。
5. 布局序列化对接：库序列化器（4.x 为 XmlLayoutSerializer）↔ 配置服务；文件损坏回退默认布局并告警（N-06）。
6. 浮动窗验证：多显示器、混合 DPI 下的位置保存 / 恢复（W-06、N-01）。
7. 主题挂钩：库主题与应用主题统一切换，为深色主题（S-04）预留。
8. 自动隐藏（W-09，P1）：优先验证库内建能力（v5 的 ToggleDockingManager 天然覆盖），4.x 下不达标则后置。

**M1 首周 spike**：用裸 AvalonDock 快速验证清单中 2 / 3 / 4 / 6 四个高风险点；任何一点被证明不可行，立即触发换库或自研评估，避免封装做到一半才发现地基不稳。

### 14.3 游戏画布在 WPF 中的承载（派生游戏项目选用）

| 方案 | 适用 | 注意事项 |
|---|---|---|
| SkiaSharp（SKElement / SKGLElement） | 2D 游戏、关卡编辑器 | 首选；与 WPF 合成器兼容性好 |
| D3DImage ＋ Vortice / SharpDX | 3D、高性能渲染 | 集成成本较高 |
| HwndHost 嵌入原生窗口 | 复用现成引擎的窗口 | 空域（airspace）限制：其上方无法叠加 WPF 控件，与浮动 / 停靠窗口的遮挡关系需专项验证 |

框架职责边界：只保证 M-04 / M-05 的宿主接口（尺寸变化事件、生命周期钩子），渲染技术由各消费游戏自选。

### 14.4 独立工程与唯一源码治理

3.0.0 起，`2026-023-AppShell` 是唯一框架项目：根 `AppShell.sln` 统一构建源码、演示宿主与测试，
`b-Publish` 保存历史包、manifest、校验和、changelog，以及发布时从 `b-Office/package` 复制的
版本化消费文档；`b-Office/package` 是消费文档编辑源，发布副本不得直接修改。

守则：

1. **唯一源码**：`b-Code-AppShell/src/AppShell.Core|Services|Shell|ServiceHost` 只存在于 023。
2. **固定包消费**：020、022 及后续消费方使用 `PackageReference`，不得跨项目引用源码或复制快照。
3. **冻结纪律**：3.0.0 起整个框架都是冻结区；致命修复必须先有复现，并完成框架与全部消费方回归。
4. **版本治理**：四包同版本发布；3.x 只允许兼容补丁，破坏性改动进入 4.0 并附迁移文档与试点。
5. **文档唯一性**：消费方只链接 023 权威文档，不维护可漂移副本。

---

## 附录 A · 默认布局示意

```
┌──────────────────────────────────────────────────────────────────┐
│ 菜单栏：文件  编辑  视图  工具  帮助                                  │
├────────────┬─────────────────────────────────────┬───────────────┤
│            │                                     │               │
│  资源窗口   │        主窗口（中央设计器区）           │  控制窗口群    │
│  (左, 18%) │        中央标签区（业务窗口）            │ ┌─────┬─────┐ │
│            │                                     │ │面板A│面板B│ │
│            │                                     │ └─────┴─────┘ │
│            │                                     │  (右, 22%)    │
├────────────┴──────────────────────────────┬──────┴───────────────┤
│ ┌────────┬────────┐← 标签条在组顶部          │                     │
│ │ 业务窗B │ 控制台 │   (底部标签组, 高 28%)   │                     │
│ ├────────┴────────┴─────────────────────── ┤                     │
│ │ [10:21] [UI] > res.list path=workspace   │                     │
│ │ > _                                      │                     │
├──┴───────────────────────────────────────────────────────────────┤
│ 状态栏：✓ 指令完成   │ 布局: 默认 │ 错误: 0                         │
└──────────────────────────────────────────────────────────────────┘
```

（各区域百分比仅为默认值示意，均可拖动分隔条调整并持久化。）

---

## 附录 B · 内置指令清单（首版草案）

| 指令 | 关键参数 | 说明 |
|---|---|---|
| help | [指令名] | 列出全部指令 / 显示某指令详情与示例 |
| cls | — | 清空控制台显示（不清日志文件） |
| history | [count=] | 查看指令历史 |
| run | file=, [continue=] | 执行指令脚本文件 |
| app.exit / app.about | — | 退出 / 关于 |
| app.set / app.get | key=, value= | 读写应用配置 |
| log.level | trace…error | 调整控制台日志显示级别 |
| win.list | — | 列出全部窗口及状态 |
| win.show / win.hide / win.float | name= | 显示 / 隐藏 / 浮动某窗口 |
| win.dock | name=, pos=left/right/top/bottom/center/tab, [target=], [ratio=] | 停靠窗口（center 进入中央标签组） |
| win.ratio | name=, value=0~1 | 调整窗口占主窗体比例 |
| layout.save / layout.load / layout.list / layout.reset | [name=] | 布局方案管理 |
| res.root | [path=] | 查看 / 切换工作区根目录 |
| res.list | [path=] | 列出目录内容 |
| res.open / res.reveal | path= | 打开 / 在系统资源管理器中显示 |
| res.mkdir / res.rename / res.delete | path=, [to=] | 目录与文件操作（delete 进回收站 + 确认） |
| panel.list / panel.show | [id=] | 列出 / 显示控制面板 |
| panel.set | panel=, control=, value= | 程序向面板控件回写值 |
| panel.reload (P2) | — | 热加载面板配置 |

派生应用的自定义指令（如 motor.jog、game.spawn）通过 §5.3 的注册机制并入同一张表，help 自动收录。

---

## 附录 C · 控制台会话示例

```
[10:21:03] [手动] > help win.dock
  win.dock —— 停靠窗口
  参数: name(必填) pos(必填) target ratio

[10:22:05] [手动] > win.dock name=console pos=bottom ratio=0.3
  ✓ 控制台已停靠至底部（30%）

[10:22:30] [脚本:每日巡检.txt] > motor.home axis=X
  ... 45% ... 100%
  ✓ X 轴回零完成，耗时 3.2s
```

—— 文档结束 ——
