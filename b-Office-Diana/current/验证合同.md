# HistoryDiana 验证合同

## 验证层级

| 层级 | 范围 | 命令 |
| --- | --- | --- |
| 构建 | 模块与测试项目编译无警告 | `dotnet build .\b-Code-HistoryDiana\HistoryDiana.csproj -c Release` |
| Smoke | 模块身份、命令全集、只读性、无 UI、巡检可执行 | `dotnet run --project .\b-Code-HistoryDiana\tests\HistoryDiana.Smoke\HistoryDiana.Smoke.csproj -c Release` |
| 快照校验 | `z-HistoryDiana` 四件齐备且哈希一致 | 见下「快照校验」 |
| 集中发布候选 | 按登记表构建、项目合同/质量门禁、测试、候选 SHA | `.\b-Code\Publish-OneHistoryModule.ps1 -Module HistoryJanus` |
| 集中正式发布 | 候选通过后原子提升 z 与 Diana 文档镜像 | 上述命令加 `-Publish`；脏源另加 `-AllowDirtySource` |
| 集中发布（宿主） | Vulcan 候选、单元测试、质量门禁、公共 API 基线、宿主快照递归 SHA | `.\b-Code\Publish-OneHistoryModule.ps1 -Module HistoryVulcan` |

## Smoke 覆盖的断言

1. 程序集恰好提供 1 个模块入口，模块名 `HistoryDiana`。
2. `ModuleInfo.Version` 与程序集版本一致（不一致会让宿主静默跳过整个模块）。
3. 命令全集恰为 10 条，逐条比对名称：
   `diana.kit.base64 / guid / now / sha256`、
   `diana.project.largest / recent / summary`、
   `diana.relay.call / describe / list`。
4. 全部命令域为 `HistoryDiana`，类恰为 `kit` / `project` / `relay` 三个。
5. 除 `diana.relay.call` 外全部 `Readonly`。
6. 三条巡检指令在临时工作树上校验通过并可执行。
7. 程序集不含 `IUiModule` 实现，不含遗留的 `ProjectPulse` 类型。

摘要行里的命令数与类数**取自实际注册结果**，不写字面量——写死的计数会在增删命令后
悄悄失真，本项目此前就出现过摘要仍写「3 commands」而实际已是 10 条的情况。

## 快照校验

`z-HistoryDiana` 恒为四件：`HistoryDiana.dll`、`HistoryDiana.xml`、
`module.manifest.json`、`SHA256SUMS`。校验方式：逐行读 `SHA256SUMS`，
比对同目录文件的 SHA-256。

manifest 的 `name` / `version` 必须与 `ModuleInfo` 一致，`ui` 恒为 `false`。

## 部署方式

宿主的模块发现扫描各项目根下的 `z-*` 目录，因此**发布到 `z-HistoryDiana` 即完成部署**，
无需另外拷贝到宿主模块槽。部署后需重启宿主或执行 `vulcan.module.reload`。

同名模块出现在多个 z 目录时所有同名候选一起被跳过，因此不得保留 `z-StudioTools` 之类的旧快照。

## 当前证据

- 2026-08-10：构建 0 警告 0 错误；Smoke PASS（1 module, 10 commands in 3 classes）；
  `z-HistoryDiana` 四件齐备，SHA256SUMS 逐件一致。
- 2026-08-10：HistoryMinerva 首次集中正式发布通过；候选构建 0 警告 0 错误，项目合同、
  `HistoryMinerva.Smoke` 与 320x680 深色 UI Smoke 全部通过。模块 z 与 Diana 文档镜像均为
  `4.2.3`，正式包 12 项 SHA256 一致；镜像 manifest 记录 `sourceDirty=true`，API 文档 SHA 一致。
- 2026-08-10：HistoryVulcan（Kind=host）首次集中正式发布通过。候选构建 → 宿主快照递归 SHA 校验
  → 单元测试 174/174 → 质量门禁 0 抑制 0 热点 → 公共 API 基线 3.4.0 匹配 → 文档镜像 → 原子提升。
  `z-HistoryVulcan` 与 Diana 镜像同为 `3.4.0`，`sourceCommit=ff5d3926`、`sourceDirty=false`，
  镜像含 7 份消费文档。至此 module 与 host 两种 Kind 各有一个项目在 Diana 跑通。
- 2026-08-10：HistoryJanus 3.7.0 首次集中正式发布通过。Debug/Release 构建均 0 警告 0 错误；
  质量门禁通过，Contracts 各 20/20，功能 Smoke 八套均 PASS，ModuleSmoke 双配置均报告
  `32 commands / 2 windows`；z 快照与 Diana 镜像版本一致，镜像记录 `sourceDirty=true`。
- 2026-08-10：HistoryMercury 4.2.0 通过 Diana 完整正式管线；Release 构建 0 警告 0 错误，
  项目合同与 `HistoryMercury.Smoke`（19 条命令）通过，z 与消费文档镜像原子提升，旧快照已归档。
- 2026-08-10：普通模块切换到 `module-publish.manifest.json` 配置驱动后，HistoryJanus 双配置、
  HistoryMercury Release、HistoryMinerva Release（含 UI Smoke）候选管线均通过；未知模块按注册表拒绝。
- 2026-08-10：HistoryVulcan 3.4.0 改用候选构建器后再次通过 Diana 候选管线；锁定离线还原后
  宿主快照、174/174 单元测试、质量门禁和公共 API 基线全部通过，未执行正式提升。
