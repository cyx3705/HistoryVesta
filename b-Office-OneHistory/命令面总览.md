# HistoryVulcan 命令手册

> [!IMPORTANT]
> 本文件由运行时指令注册表自动生成。禁止手工增删或改写下方指令条目；
> 需要更新时，请在程序控制台执行 `vulcan.command.manual file=<相对 Markdown 路径> apply=true`。

> 版本：3.9.0
> 来源：运行时 `CommandRegistry` 与 MCP 投影自动生成；请勿手工维护指令条目。
> 当前 MCP 策略：`standard`
> 指令总数：99

<!-- command-count: 99 -->

## diana (10)

### kit (4)

#### `diana.kit.base64`

把 UTF-8 文本编码为 Base64

- 域：`diana`
- 类：`kit`
- 来源：`module:HistoryDiana`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `diana_kit_base64`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `text` | `string` | 是 | - | - | 要编码的文本 |

```text
diana.kit.base64 text=hello
```

#### `diana.kit.guid`

生成一个新 GUID

- 域：`diana`
- 类：`kit`
- 来源：`module:HistoryDiana`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `diana_kit_guid`

参数：无。

```text
diana.kit.guid
```

#### `diana.kit.now`

当前本地时间与 Unix 秒

- 域：`diana`
- 类：`kit`
- 来源：`module:HistoryDiana`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `diana_kit_now`

参数：无。

```text
diana.kit.now
```

#### `diana.kit.sha256`

计算文本的 SHA-256（十六进制大写）

- 域：`diana`
- 类：`kit`
- 来源：`module:HistoryDiana`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `diana_kit_sha256`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `text` | `string` | 是 | - | - | 要哈希的文本 |

```text
diana.kit.sha256 text=hello
```

### project (3)

#### `diana.project.largest`

列出一个已登记工作树中最大的文件

- 域：`diana`
- 类：`project`
- 来源：`module:HistoryDiana`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `diana_project_largest`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 已登记工作树名称，例如 2026-020-HistoryJanus |
| `limit` | `int` | 否 | 20 | - | 最多返回文件数，范围 1~200 |
| `minMb` | `double` | 否 | 1 | - | 最小体积 MB，范围 0~1048576 |
| `includeGenerated` | `bool` | 否 | false | - | 是否包含 bin、obj、.vs、node_modules 等生成目录 |

```text
diana.project.largest name=2026-020-HistoryJanus limit=20 minMb=1
```

#### `diana.project.recent`

列出一个已登记工作树最近修改的文件

- 域：`diana`
- 类：`project`
- 来源：`module:HistoryDiana`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `diana_project_recent`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 已登记工作树名称，例如 2026-020-HistoryJanus |
| `days` | `int` | 否 | 7 | - | 回看天数，范围 1~3650 |
| `limit` | `int` | 否 | 30 | - | 最多返回文件数，范围 1~200 |
| `includeGenerated` | `bool` | 否 | false | - | 是否包含 bin、obj、.vs、node_modules 等生成目录 |

```text
diana.project.recent name=2026-020-HistoryJanus days=7 limit=30
```

#### `diana.project.summary`

汇总一个已登记工作树的文件、体积和一级目录热点

- 域：`diana`
- 类：`project`
- 来源：`module:HistoryDiana`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `diana_project_summary`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 已登记工作树名称，例如 2026-020-HistoryJanus |
| `includeGenerated` | `bool` | 否 | false | - | 是否包含 bin、obj、.vs、node_modules 等生成目录 |
| `top` | `int` | 否 | 10 | - | 格式和一级目录统计最多返回多少项，范围 1~50 |

```text
diana.project.summary name=2026-020-HistoryJanus top=10
```

### relay (3)

#### `diana.relay.call`

按当前 MCP 工具目录调用一个工具，不依赖会话里的旧快照

- 域：`diana`
- 类：`relay`
- 来源：`module:HistoryDiana`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `diana_relay_call`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | MCP 工具名 |
| `argumentsjson` | `string` | 否 | - | - | JSON 对象字符串，默认空对象，最大 64 KiB |

```text
diana.relay.call name=ProjectPulse_Summary argumentsjson={"name":"2026-019-HistoryDiana"}
```

#### `diana.relay.describe`

查看一个当前可见 MCP 工具的描述与 JSON Schema

- 域：`diana`
- 类：`relay`
- 来源：`module:HistoryDiana`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `diana_relay_describe`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | MCP 工具名 |

```text
diana.relay.describe name=ProjectPulse_Summary
```

#### `diana.relay.list`

实时列出当前 MCP 策略下可见的工具

- 域：`diana`
- 类：`relay`
- 来源：`module:HistoryDiana`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `diana_relay_list`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `filter` | `string` | 否 | - | - | 按工具名或描述包含匹配；留空不过滤 |
| `modulesonly` | `bool` | 否 | true | - | true 只返回模块工具，false 返回全部可见工具 |

```text
diana.relay.list filter=project modulesonly=true
```

## janus (32)

### core (1)

#### `janus.status`

HistoryJanus 模块 HistoryJanusCommands.status 方法

- 域：`janus`
- 类：`core`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_status`

参数：无。

```text
janus.status
```

### github (3)

#### `janus.github.accounts`

列出服务器 GCM 中已知的 GitHub HTTPS 凭据账号

- 域：`janus`
- 类：`github`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_github_accounts`

参数：无。

```text
janus.github.accounts
```

#### `janus.github.status`

查看服务器 Git、GCM、提交身份、origin 和 SSH 状态

- 域：`janus`
- 类：`github`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_github_status`

参数：无。

```text
janus.github.status
```

#### `janus.github.test`

只读检测服务器 GitHub SSH 或 HTTPS 连接，不执行 push

- 域：`janus`
- 类：`github`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_github_test`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `transport` | `string` | 否 | auto | auto / ssh / https | 检测通道 |
| `timeout` | `int` | 否 | 15 | - | 超时秒数(1~120) |

```text
janus.github.test transport=auto timeout=15
```

### gitrule (7)

#### `janus.gitrule.batchset`

一次预览、确认并保存多条 Git/LFS/LF 文件规则

- 域：`janus`
- 类：`gitrule`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_gitrule_batchset`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 已登记 Project 名/分支名 |
| `changes` | `string` | 是 | - | - | 1 至 500 条规则的 JSON 数组 |
| `apply` | `bool` | 否 | false | - | false 仅预览；true 经一次确认后批量写入并同步索引 |

```text
janus.gitrule.batchset name=demo changes="[{\"pattern\":\"*.xlsx\",\"track\":true,\"lfs\":true,\"lf\":false}]" apply=false
```

#### `janus.gitrule.list`

列出项目根文件格式的纳入 Git、LFS、LF 规则和实际索引状态

- 域：`janus`
- 类：`gitrule`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_gitrule_list`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 已登记 Project 名/分支名 |

```text
janus.gitrule.list name=0000-000-Template
```

#### `janus.gitrule.remove`

预览或确认后移除托管文件格式规则；不删除本地文件

- 域：`janus`
- 类：`gitrule`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_gitrule_remove`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 已登记 Project 名/分支名 |
| `pattern` | `string` | 是 | - | - | 要移除的简单文件格式 |
| `apply` | `bool` | 否 | false | - | false 仅预览；true 经本地确认后移除 |

```text
janus.gitrule.remove name=demo pattern=*.xlsx apply=false
```

#### `janus.gitrule.review`

一次扫描合并查看未决格式、目录候选、规则建议与需人工判断的未知格式

- 域：`janus`
- 类：`gitrule`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_gitrule_review`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 否 | - | - | 项目名;省略则针对全库 |

```text
janus.gitrule.review
```

#### `janus.gitrule.scan`

扫描项目库全部文件格式,输出台账与覆盖率(省略 name 扫全库)

- 域：`janus`
- 类：`gitrule`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_gitrule_scan`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 否 | - | - | 项目名;省略则扫描全库 |
| `deep` | `bool` | 否 | false | - | true 时追加 git lfs 指针核验(较慢) |
| `refresh` | `bool` | 否 | false | - | true 时忽略缓存全量重扫 |

```text
janus.gitrule.scan depth=normal
```

#### `janus.gitrule.set`

预览或确认后设置文件格式的纳入 Git、LFS、LF 状态并同步索引

- 域：`janus`
- 类：`gitrule`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_gitrule_set`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 已登记 Project 名/分支名 |
| `pattern` | `string` | 是 | - | - | 简单文件格式，如 *.xlsx |
| `track` | `bool` | 否 | true | - | 是否纳入 Git |
| `lfs` | `bool` | 否 | false | - | 是否使用 LFS 指针 |
| `lf` | `bool` | 否 | false | - | 是否作为文本并统一 LF |
| `apply` | `bool` | 否 | false | - | false 仅预览；true 经本地确认后写入并同步索引 |

```text
janus.gitrule.set name=demo pattern=*.xlsx track=true lfs=true lf=false apply=false
```

#### `janus.gitrule.sync`

把模板项目的规则基线刷入各项目的 baseline 块(不动项目自身 managed 块与手写内容)

- 域：`janus`
- 类：`gitrule`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_gitrule_sync`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 否 | - | - | 目标项目;省略则同步全部项目(模板自身除外) |
| `apply` | `bool` | 否 | false | - | false 仅预览;true 经确认后写入 |

```text
janus.gitrule.sync apply=false
```

### history (6)

#### `janus.history.diff`

预览历史节点与当前分支 HEAD 的提交及文件差异

- 域：`janus`
- 类：`history`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_history_diff`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 分支名（项目名） |
| `sha` | `string` | 是 | - | - | 分叉点至 HEAD 范围内的提交 SHA |

```text
janus.history.diff name=2026-018-MyAPI sha=abc1234
```

#### `janus.history.forcepush`

使用 --force-with-lease 更新非保护远端分支

- 域：`janus`
- 类：`history`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_history_forcepush`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 分支名（项目名） |

```text
janus.history.forcepush name=2026-018-MyAPI
```

#### `janus.history.list`

查看分支从父分支分叉点到当前 HEAD 的提交历史

- 域：`janus`
- 类：`history`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_history_list`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 分支名（项目名） |
| `limit` | `int` | 否 | 200 | - | 本次最多显示的自有提交数（1~2000） |
| `skip` | `int` | 否 | 0 | - | 从最新提交向前跳过的数量 |
| `refresh` | `bool` | 否 | false | - | 重新扫描继承树后计算父分支 |
| `remote` | `bool` | 否 | false | - | 先刷新所选 origin 分支的跟踪引用 |

```text
janus.history.list name=2026-018-MyAPI limit=200 remote=false
```

#### `janus.history.reset`

把非保护分支硬重置到历史节点（仅本地，不修改远端）

- 域：`janus`
- 类：`history`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_history_reset`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 分支名（项目名） |
| `sha` | `string` | 是 | - | - | 分叉点至 HEAD 范围内的提交 SHA |

```text
janus.history.reset name=2026-018-MyAPI sha=abc1234
```

#### `janus.history.rollback`

把工作树恢复到历史节点内容并生成新的恢复提交

- 域：`janus`
- 类：`history`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_history_rollback`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 分支名（项目名） |
| `sha` | `string` | 是 | - | - | 分叉点至 HEAD 范围内的提交 SHA |
| `msg` | `string` | 是 | - | - | 新恢复提交的说明 |

```text
janus.history.rollback name=2026-018-MyAPI sha=abc1234 msg="恢复到稳定版本"
```

#### `janus.history.show`

查看分支历史节点的提交详情与文件变更

- 域：`janus`
- 类：`history`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_history_show`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 分支名（项目名） |
| `sha` | `string` | 是 | - | - | 分叉点至 HEAD 范围内的提交 SHA |

```text
janus.history.show name=2026-018-MyAPI sha=abc1234
```

### proj (15)

#### `janus.proj.commit`

提交单个项目到本地仓库；可按子模块先、父项目后联动提交

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_commit`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 分支名(= 工作树文件夹名) |
| `msg` | `string` | 是 | - | - | 提交描述(Commit Message) |
| `target` | `string` | 否 | - | parent / submodules / both | 实际仓库目标；存在时优先于兼容参数 submodules |
| `submodules` | `bool` | 否 | false | - | 兼容参数：true=both，false=parent；target 存在时忽略 |
| `submsg` | `string` | 否 | - | - | 子模块统一提交描述；空时沿用 msg |

```text
janus.proj.commit name=2026-018-MyAPI msg="更新说明" target=both
```

#### `janus.proj.commitall`

一键提交全部工作树；可联动各项目直属子模块

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_proj_commitall`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `msg` | `string` | 是 | - | - | 统一提交描述 |
| `target` | `string` | 否 | - | parent / submodules / both | 实际仓库目标；存在时优先于兼容参数 submodules |
| `submodules` | `bool` | 否 | false | - | 兼容参数：true=both，false=parent；target 存在时忽略 |
| `submsg` | `string` | 否 | - | - | 全部子模块统一提交描述；空时沿用 msg |

```text
janus.proj.commitall msg="每日推送" target=both
```

#### `janus.proj.config`

显示 proj.* 当前生效配置(经 app.set 修改)

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_config`

参数：无。

```text
janus.proj.config
```

#### `janus.proj.create`

创建新项目:新建分支 + 同名工作树(分支名 = 文件夹名)

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_create`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 项目名称(合法文件夹字符) |
| `base` | `string` | 否 | - | - | 基础分支(缺省取 proj.basebranch 配置) |

```text
janus.proj.create name=2026-025-新项目
```

#### `janus.proj.delete`

删除项目:移除工作树 + 强制删除分支(不可撤销;受保护分支拒绝)

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_proj_delete`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 要删除的分支名(= 工作树文件夹名) |

```text
janus.proj.delete name=9999-901-测试
```

#### `janus.proj.list`

列出全部项目工作树(编号/分支/路径/状态)

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_list`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `filter` | `string` | 否 | - | - | 分支名关键字过滤(包含匹配,忽略大小写) |

```text
janus.proj.list filter=2026
```

#### `janus.proj.metaopen`

在系统资源管理器中打开指定元文件夹(path= 或 name=+meta=)

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_metaopen`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `path` | `string` | 否 | - | - | 元文件夹完整路径 |
| `name` | `string` | 否 | - | - | 所属项目分支名(= 工作树文件夹名) |
| `meta` | `string` | 否 | - | - | 元文件夹名(须以 z/Z 开头) |

```text
janus.proj.metaopen name=2026-016-AD学习 meta=z-AD库文件汇总
```

#### `janus.proj.metas`

列出全部项目根下以 z/Z 开头的一级元文件夹

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_metas`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `filter` | `string` | 否 | - | - | 项目名/元文件夹名/路径关键字过滤(包含匹配,忽略大小写) |

```text
janus.proj.metas filter=AD
```

#### `janus.proj.note`

写入/更新分支的项目描述(继承树与 janus.proj.tree 优先显示此描述)

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_note`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 分支名 |
| `text` | `string` | 是 | - | - | 项目描述文本 |

```text
janus.proj.note name=2026-018-MyAPI text="基础设施整合项目"
```

#### `janus.proj.open`

在系统资源管理器中打开项目工作树(不带 name 打开工作树根目录)

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_open`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 否 | - | - | 分支名(= 工作树文件夹名);省略打开根目录 |

```text
janus.proj.open name=2026-018-MyAPI
```

#### `janus.proj.push`

推送单个分支；可先推直属子模块，全部成功后再推父项目

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_push`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 分支名(= 工作树文件夹名) |
| `target` | `string` | 否 | - | parent / submodules / both | 实际仓库目标；存在时优先于兼容参数 submodules |
| `submodules` | `bool` | 否 | false | - | 兼容参数：true=both，false=parent；target 存在时忽略 |

```text
janus.proj.push name=2026-018-MyAPI target=both
```

#### `janus.proj.pushall`

推送全部分支；可先去重推送所有直属子模块

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_proj_pushall`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `target` | `string` | 否 | - | parent / submodules / both | 实际仓库目标；存在时优先于兼容参数 submodules |
| `submodules` | `bool` | 否 | false | - | 兼容参数：true=both，false=parent；target 存在时忽略 |

```text
janus.proj.pushall target=both
```

#### `janus.proj.repair`

worktree 断链批量修复:删除全部工作树目录→prune→按分支清单重建

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `janus_proj_repair`

参数：无。

```text
janus.proj.repair
```

#### `janus.proj.scan`

扫描项目大文件并输出分级报告(不提交)

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_scan`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 分支名(= 工作树文件夹名) |

```text
janus.proj.scan name=2026-018-MyAPI
```

#### `janus.proj.tree`

输出分支继承树(默认读文件缓存秒开;refresh=true 重新扫描并更新缓存)

- 域：`janus`
- 类：`proj`
- 来源：`module:HistoryJanus`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `janus_proj_tree`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `refresh` | `bool` | 否 | false | - | true 时忽略缓存重新扫描裸仓库 |
| `cached` | `bool` | 否 | false | - | true 时仅读缓存,无缓存不触发扫描(视图自动加载用) |

```text
janus.proj.tree refresh=true
```

## mercury (22)

###  (1)

#### `mercury.go`

聚焦到指定指令域；省略 domain 则退出聚焦。

- 域：`mercury`
- 类：``
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：是
- MCP：`standard`，当前策略可见，工具名 `mercury_go`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `domain` | `string` | 否 | - | - | 要聚焦的指令域；省略则退出聚焦回到「全部」。 |

### app (2)

#### `mercury.app.open`

打开 HistoryVulcan。

- 域：`mercury`
- 类：`app`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_app_open`

参数：无。

#### `mercury.app.status`

查看托管的资源管理器入口状态。

- 域：`mercury`
- 类：`app`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `mercury_app_status`

参数：无。

### dock (3)

#### `mercury.dock.hide`

隐藏项目坞。

- 域：`mercury`
- 类：`dock`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_dock_hide`

参数：无。

#### `mercury.dock.policy`

查看或更新项目坞策略。

- 域：`mercury`
- 类：`dock`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_dock_policy`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `min` | `int` | 否 | - | - | 可选整数策略值。 |
| `max` | `int` | 否 | - | - | 可选整数策略值。 |
| `halflife` | `double` | 否 | - | - | 可选小数策略值。 |

#### `mercury.dock.show`

显示项目坞。

- 域：`mercury`
- 类：`dock`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_dock_show`

参数：无。

### explorer (2)

#### `mercury.explorer.register`

注册托管的资源管理器入口。

- 域：`mercury`
- 类：`explorer`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_explorer_register`

参数：无。

#### `mercury.explorer.remove`

移除托管的资源管理器入口。

- 域：`mercury`
- 类：`explorer`
- 来源：`module:HistoryMercury`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `mercury_explorer_remove`

参数：无。

### hotkey (3)

#### `mercury.hotkey.list`

列出当前生效的全局快捷键注册。

- 域：`mercury`
- 类：`hotkey`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `mercury_hotkey_list`

参数：无。

#### `mercury.hotkey.register`

注册「按键序列 → 命令」的全局快捷键。

- 域：`mercury`
- 类：`hotkey`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_hotkey_register`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `id` | `string` | 是 | - | - | 快捷键标识；同 id 重复注册会覆盖旧的。 |
| `stroke` | `string` | 是 | - | - | 按键序列：逗号分隔多次击键，每次为「修饰键+主键」。例 Ctrl+Alt+M、Slash,Slash（连按两次 /）、VK:0xBF（虚拟键码）。 |
| `command` | `string` | 是 | - | - | 命中后要执行的指令文本。 |
| `owner` | `string` | 否 | - | - | 注册方标识；省略则记为 HistoryMercury。 |
| `interval` | `string` | 否 | - | - | 多次击键之间的最大间隔毫秒数；省略为 350。 |

#### `mercury.hotkey.unregister`

注销此前注册的全局快捷键。

- 域：`mercury`
- 类：`hotkey`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_hotkey_unregister`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `id` | `string` | 是 | - | - | 快捷键标识；同 id 重复注册会覆盖旧的。 |

### proj (8)

#### `mercury.proj.add`

添加并置顶项目。

- 域：`mercury`
- 类：`proj`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_proj_add`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 项目名称或序号。 |

#### `mercury.proj.exclude`

从项目坞排除项目。

- 域：`mercury`
- 类：`proj`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_proj_exclude`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 项目名称或序号。 |

#### `mercury.proj.include`

将项目重新纳入项目坞。

- 域：`mercury`
- 类：`proj`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_proj_include`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 项目名称或序号。 |

#### `mercury.proj.list`

列出活动项目。

- 域：`mercury`
- 类：`proj`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `mercury_proj_list`

参数：无。

#### `mercury.proj.open`

打开项目目录。

- 域：`mercury`
- 类：`proj`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_proj_open`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 项目名称或序号。 |

```text
mercury.proj.open 2026-021-HistoryMercury
```

#### `mercury.proj.pin`

置顶活动项目。

- 域：`mercury`
- 类：`proj`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_proj_pin`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 项目名称或序号。 |

#### `mercury.proj.refresh`

重新扫描活动项目。

- 域：`mercury`
- 类：`proj`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_proj_refresh`

参数：无。

#### `mercury.proj.unpin`

取消置顶活动项目。

- 域：`mercury`
- 类：`proj`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_proj_unpin`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 项目名称或序号。 |

### shortcut (1)

#### `mercury.shortcut.wakeconsole`

兼容入口：调用 Vulcan 语义命令唤出并聚焦控制台。

- 域：`mercury`
- 类：`shortcut`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `mercury_shortcut_wakeconsole`

参数：无。

### usage (2)

#### `mercury.usage.forget`

清除项目使用历史。

- 域：`mercury`
- 类：`usage`
- 来源：`module:HistoryMercury`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `mercury_usage_forget`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 否 | - | - | 项目名称或序号；省略则清除全部。 |

#### `mercury.usage.list`

列出项目使用记录。

- 域：`mercury`
- 类：`usage`
- 来源：`module:HistoryMercury`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `mercury_usage_list`

参数：无。

## minerva (5)

### worker (5)

#### `minerva.worker.capabilities`

查询 Minerva Worker 支持的协议能力

- 域：`minerva`
- 类：`worker`
- 来源：`module:HistoryMinerva`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `minerva_worker_capabilities`

参数：无。

```text
minerva.worker.capabilities
```

#### `minerva.worker.hide`

报告 Minerva 工作区隐藏状态

- 域：`minerva`
- 类：`worker`
- 来源：`module:HistoryMinerva`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `minerva_worker_hide`

参数：无。

```text
minerva.worker.hide
```

#### `minerva.worker.path`

查询 Minerva Worker 的实际定位路径

- 域：`minerva`
- 类：`worker`
- 来源：`module:HistoryMinerva`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `minerva_worker_path`

参数：无。

```text
minerva.worker.path
```

#### `minerva.worker.show`

报告 Minerva 工作区入口状态

- 域：`minerva`
- 类：`worker`
- 来源：`module:HistoryMinerva`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `minerva_worker_show`

参数：无。

```text
minerva.worker.show
```

#### `minerva.worker.status`

查询 Minerva Worker 是否就绪

- 域：`minerva`
- 类：`worker`
- 来源：`module:HistoryMinerva`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `minerva_worker_status`

参数：无。

```text
minerva.worker.status
```

## vulcan (30)

### app (2)

#### `vulcan.app.get`

读应用配置项;不带参数列出全部

- 域：`vulcan`
- 类：`app`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_app_get`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `key` | `string` | 否 | - | - | 配置键;省略列出全部 |

```text
vulcan.app.get key=console.history
```

#### `vulcan.app.set`

写应用配置项

- 域：`vulcan`
- 类：`app`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `vulcan_app_set`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `key` | `string` | 是 | - | - | 配置键 |
| `value` | `string` | 是 | - | - | 配置值 |

```text
vulcan.app.set key=console.history value=1000
```

### command (4)

#### `vulcan.command.domains`

列出全部指令域及注册数量

- 域：`vulcan`
- 类：`command`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_command_domains`

参数：无。

```text
vulcan.command.domains
```

#### `vulcan.command.list`

结构化列出全部注册指令及其来源、风险和 MCP 投影

- 域：`vulcan`
- 类：`command`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_command_list`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `domain` | `string` | 否 | - | - | 可选指令域，如 proj / attr / command |
| `class` | `string` | 否 | - | - | 可选域内命令类，如 win / log / module |
| `mcp` | `string` | 否 | all | all / visible / hidden | 按当前策略过滤 MCP 可见性 |
| `filter` | `string` | 否 | - | - | 按名称、说明或来源搜索 |

```text
vulcan.command.list domain=vulcan class=win mcp=visible filter=dock
```

#### `vulcan.command.manual`

从运行时注册表和 MCP 投影预览或生成 Markdown 命令手册

- 域：`vulcan`
- 类：`command`
- 来源：`framework:service`
- 安全：本地二次确认
- UI 线程：否
- MCP：`dangerous`，当前策略隐藏，工具名 `vulcan_command_manual`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `file` | `string` | 是 | - | - | 相对当前工作目录的 Markdown 输出路径 |
| `apply` | `bool` | 否 | false | - | false 仅预览；true 经本地确认后原子写入 |

```text
vulcan.command.manual file=command-manual.md apply=false
```

#### `vulcan.command.show`

查看单条指令的 Help 参数、来源、风险和 MCP 映射

- 域：`vulcan`
- 类：`command`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_command_show`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 完整指令名 |

```text
vulcan.command.show name=vulcan.mcp.apply
```

### mcp (12)

#### `vulcan.mcp.apply`

本地应用已批准提案；基线变化时拒绝覆盖

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `id` | `string` | 是 | - | - | 提案 ID |
| `reviewer` | `string` | 否 | - | - | 应用人；省略使用当前 Windows 用户 |

```text
vulcan.mcp.apply id=proposal_xxx reviewer=Administrator
```

#### `vulcan.mcp.approve`

本地批准提示词提案；批准后仍需 apply 才生效

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `id` | `string` | 是 | - | - | 提案 ID |
| `reviewer` | `string` | 否 | - | - | 审核人；省略使用当前 Windows 用户 |

```text
vulcan.mcp.approve id=proposal_xxx reviewer=Administrator
```

#### `vulcan.mcp.autostart`

查看或设置 MCP 随宿主自动监听(持久;省略 enabled 只查看)

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `enabled` | `bool` | 否 | - | - | true 随宿主自动监听；false 只能手动 vulcan.mcp.start。省略则只报告当前值。 |

```text
vulcan.mcp.autostart enabled=true
```

#### `vulcan.mcp.desc`

本地查看/直接修订 MCP 工具描述；远程 AI 请使用 vulcan.prompt.propose

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 指令名或工具名 |
| `text` | `string` | 否 | - | - | 新描述；省略只查看 |
| `reason` | `string` | 否 | 本地直接修订 | - | 修订理由 |
| `reviewer` | `string` | 否 | - | - | 执行人；省略使用当前 Windows 用户 |
| `reset` | `bool` | 否 | false | - | 恢复指令自带描述 |

```text
vulcan.mcp.desc name=vulcan.command.list text="列出当前可用指令" reason=人工修订
```

#### `vulcan.mcp.parse`

调试:模拟 tools/call 反向解析——JSON arguments 组装为指令文本,exec=true 随即经总线执行

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `command` | `string` | 是 | - | - | 目标指令名 |
| `args` | `string` | 否 | - | - | JSON 对象文本(工具调用的 arguments) |
| `argsfile` | `string` | 否 | - | - | 从文件读 JSON(替代 args,规避命令行转义;自动化验收用) |
| `exec` | `bool` | 否 | false | - | true 时组装后立即经总线执行(来源 MCP:parse) |

```text
vulcan.mcp.parse command=vulcan.command.list args="{}" exec=true
```

#### `vulcan.mcp.pending`

本地列出待审核或已批准未应用的提示词提案

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 否 | - | - | 可选指令名 |
| `limit` | `int` | 否 | 100 | - | 最多返回条数 |

```text
vulcan.mcp.pending name=vulcan.command.list
```

#### `vulcan.mcp.reject`

本地拒绝提示词提案并保留理由

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `id` | `string` | 是 | - | - | 提案 ID |
| `reason` | `string` | 是 | - | - | 拒绝理由 |
| `reviewer` | `string` | 否 | - | - | 审核人；省略使用当前 Windows 用户 |

```text
vulcan.mcp.reject id=proposal_xxx reason=边界描述不准确
```

#### `vulcan.mcp.revert`

本地把指定历史修订内容生成为新的当前修订

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `revision` | `string` | 是 | - | - | 目标历史修订 ID |
| `reason` | `string` | 是 | - | - | 回滚理由 |
| `reviewer` | `string` | 否 | - | - | 执行人；省略使用当前 Windows 用户 |

```text
vulcan.mcp.revert revision=rev_xxx reason=回退错误描述
```

#### `vulcan.mcp.schema`

查看指令的 MCP 工具形态(不带参列全部;带 name 输出单条完整 JSON Schema)

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 否 | - | - | 指令名或工具名(如 vulcan.command.list / command_list);省略列出全部 |

```text
vulcan.mcp.schema name=vulcan.command.list
```

#### `vulcan.mcp.start`

启动 MCP 服务(仅 127.0.0.1;策略/令牌经 vulcan.app.set mcp.policy / mcp.token 配置)

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `port` | `int` | 否 | - | - | 监听端口(缺省读 mcp.port 配置,默认 8737;显式指定时持久化) |

```text
vulcan.mcp.start port=8737
```

#### `vulcan.mcp.status`

查看 MCP 服务状态(运行/端口/策略/暴露工具数/累计调用/最近一次调用)

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

参数：无。

```text
vulcan.mcp.status
```

#### `vulcan.mcp.stop`

停止 MCP 服务并释放端口

- 域：`vulcan`
- 类：`mcp`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`hidden`，当前策略隐藏
- MCP 排除原因：防止远程递归管理或关闭 MCP 服务

参数：无。

```text
vulcan.mcp.stop
```

### module (4)

#### `vulcan.module.list`

列出已加载模块

- 域：`vulcan`
- 类：`module`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_module_list`

参数：无。

#### `vulcan.module.open`

打开模块发现根

- 域：`vulcan`
- 类：`module`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_module_open`

参数：无。

```text
vulcan.module.open
```

#### `vulcan.module.reload`

重载全部后台模块

- 域：`vulcan`
- 类：`module`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `vulcan_module_reload`

参数：无。

#### `vulcan.module.roots`

查看或设置后台 Z 模块发现根

- 域：`vulcan`
- 类：`module`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `vulcan_module_roots`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `paths` | `string` | 否 | - | - | 分号分隔的绝对根；auto 恢复自动识别；省略时查询 |

### prompt (8)

#### `vulcan.prompt.correct`

提交工具描述勘误，不直接修改生效描述

- 域：`vulcan`
- 类：`prompt`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `vulcan_prompt_correct`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 指令名或工具名 |
| `claim` | `string` | 是 | - | - | 需要纠正的原说法 |
| `correction` | `string` | 是 | - | - | 正确解释 |
| `evidence` | `string` | 否 | - | - | 证据或复现记录 |
| `proposal` | `string` | 否 | - | - | 可选关联的提示词提案 ID |

```text
vulcan.prompt.correct name=vulcan.command.list claim="只返回框架指令" correction="返回权威目录中的全部指令"
```

#### `vulcan.prompt.corrections`

列出 MCP 工具描述勘误记录

- 域：`vulcan`
- 类：`prompt`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_prompt_corrections`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 否 | - | - | 可选指令名 |
| `limit` | `int` | 否 | 50 | - | 最多返回条数 |

```text
vulcan.prompt.corrections name=vulcan.command.list limit=20
```

#### `vulcan.prompt.diff`

查看提示词提案的原文、新文和文本差异

- 域：`vulcan`
- 类：`prompt`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_prompt_diff`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `id` | `string` | 是 | - | - | 提案 ID |

```text
vulcan.prompt.diff id=proposal_xxx
```

#### `vulcan.prompt.get`

查看 MCP 工具的默认描述、生效描述、当前修订和待审核提案数

- 域：`vulcan`
- 类：`prompt`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_prompt_get`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 指令名或工具名 |

```text
vulcan.prompt.get name=vulcan.command.list
```

#### `vulcan.prompt.history`

查看某个 MCP 工具的描述修订历史

- 域：`vulcan`
- 类：`prompt`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_prompt_history`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 指令名或工具名 |
| `limit` | `int` | 否 | 20 | - | 最多返回条数 |

```text
vulcan.prompt.history name=vulcan.command.list limit=20
```

#### `vulcan.prompt.incidents`

列出 MCP 工具调用或描述事故记录

- 域：`vulcan`
- 类：`prompt`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`readonly`，当前策略可见，工具名 `vulcan_prompt_incidents`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 否 | - | - | 可选指令名 |
| `limit` | `int` | 否 | 50 | - | 最多返回条数 |

```text
vulcan.prompt.incidents name=vulcan.command.list limit=20
```

#### `vulcan.prompt.propose`

提交 MCP 工具描述修改提案；不会直接改变生效描述

- 域：`vulcan`
- 类：`prompt`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `vulcan_prompt_propose`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 指令名或工具名 |
| `text` | `string` | 是 | - | - | 建议的新描述 |
| `reason` | `string` | 是 | - | - | 修改理由 |
| `evidence` | `string` | 否 | - | - | 证据、错误现场或引用 |

```text
vulcan.prompt.propose name=vulcan.command.list text="列出当前可用指令" reason=澄清目录语义
```

#### `vulcan.prompt.record`

记录工具调用或描述事故；保留预期、实际和证据

- 域：`vulcan`
- 类：`prompt`
- 来源：`framework:service`
- 安全：普通
- UI 线程：否
- MCP：`standard`，当前策略可见，工具名 `vulcan_prompt_record`

| 参数 | 类型 | 必填 | 默认值 | 允许值 | 说明 |
|---|---|---|---|---|---|
| `name` | `string` | 是 | - | - | 指令名或工具名 |
| `symptom` | `string` | 是 | - | - | 问题现象 |
| `expected` | `string` | 是 | - | - | 预期行为 |
| `actual` | `string` | 是 | - | - | 实际行为 |
| `evidence` | `string` | 否 | - | - | 日志、复现步骤或引用 |
| `correction` | `string` | 否 | - | - | 可选关联的勘误 ID |

```text
vulcan.prompt.record name=vulcan.command.list symptom=遗漏前端指令 expected=返回权威目录 actual=只返回后端指令
```
