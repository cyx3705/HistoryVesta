# OneHistory-V2.0 · 项目验收单

> 用法：每完成一个里程碑，按本单逐条执行验收步骤并勾选；全部通过才进入下一节点。
> 「验收步骤」是可照做的操作序列；「通过标准」是唯一判据。
> 涉及生产裸仓库的破坏性验收（删除/修复）一律先在沙盒仓库演练（见开发提示 §4-8）。
> 验收记录：勾选 `[x]` 并在行尾追加日期，如 `……（2026-07-20）`。

---

## 节点 V2-M0 —— 骨架派生（出口：能跑的空壳）

- [x] **A0-1 派生完成**（2026-07-14）
  步骤：拷贝 z-APPShell 解决方案 → 更名 OneHistoryStudio → 删除 WpfApp1 与 bin/obj → 构建。
  通过标准：`dotnet build` 零错误零警告（环境变量 `DOTNET_EnableWriteXorExecute=0` 已设置）。
  记录：解决方案更名 OneHistoryStudio.sln；App 项目 AssemblyName/RootNamespace = OneHistoryStudio；构建 0 警告 0 错误。
- [x] **A0-2 启动冒烟**（2026-07-14）
  步骤：运行程序。
  通过标准：窗口标题为「OneHistory 项目管理工具 V2.0」；主窗口占位页显示应用名与版本；
  控制台输入 `help` 输出完整内置指令表；冷启动 ≤ 2 秒。
  记录：标题实际为「OneHistory 项目管理工具 v2.0.0-M0」（Shell 按 AppName+AppVersion 自动拼接）；
  `--exec help` 输出 45 条指令；启动→执行→退出全程 902ms。
- [x] **A0-3 数据目录就位**（2026-07-14）
  步骤：检查 `%AppData%\OneHistoryStudio\`。
  通过标准：layout / data / workspace / logs / panels 等目录结构自动生成。
  记录：五目录 + main.db / history.txt / window.json / motor.json 全部生成。
- [x] **A0-4 布局持久化回归**（2026-07-14，指令级）
  步骤：把控制台拖到左侧停靠、调整比例 → 退出 → 重启。
  通过标准：布局原样恢复（模板验收 1 的回归）。
  记录：`win.dock name=console pos=left ratio=0.3` → 重启 → `win.list` 显示「console 停靠·左 30%」，
  持久化链路通过；鼠标拖拽手势留人工复验（同模板验收惯例）。验收后已 layout.reset 回默认。
- [x] **A0-5 基线登记**（2026-07-14）
  步骤：检查 TEMPLATE_VERSION.md。
  通过标准：已登记「跟随基线 0.4.1-M4，未分叉」。
  记录：docs\框架文档\TEMPLATE_VERSION.md 顶部新增本应用登记段。

## 节点 V2-M1 —— proj.* 指令域（出口：纯控制台可管理项目库）

> 本节点验收全程**不使用任何工具窗口**，只在控制台手输指令——这是控制台完备性的落点。
> A1-1 ~ A1-6 在沙盒裸仓库执行；A1-8 在生产仓库以只读指令复核。

- [x] **A1-1 列表**（2026-07-14，自动化）
  步骤：`proj.list`，再 `proj.list filter=2026`。
  通过标准：输出与 `git worktree list` 一致；过滤生效；含编号/分支/路径/状态。
  记录：沙盒与生产（34 个 worktree）均验证；filter 包含匹配生效；目录缺失时标注「疑似断链」。
- [x] **A1-2 创建**（2026-07-14，自动化）
  步骤：`proj.create name=9999-901-验收测试`。
  通过标准：分支与同名 worktree 目录生成；基于分支默认 0000-000-Template；
  重复创建、含 `\/:*?"<>|` 的名称、目标目录已存在三种情况分别被明确拒绝且不崩溃。
  记录：沙盒验证创建成功（含 base= 指定基础分支）；重复名/非法字符/目录冲突三类拒绝路径全过。
- [x] **A1-3 提交与大小检查**（2026-07-14，自动化）
  步骤：在测试 worktree 放入普通文件 + 一个 60MB 文件 → `proj.commit name=9999-901-验收测试 msg=验收`；
  再放入一个 110MB 文件重复提交。
  通过标准：60MB → 警告回显但提交成功；110MB → 触发 LFS 流程（无 LFS 环境则明确失败提示；
  确认对话拒绝时该项目跳过，结果为 Rejected 且不产生半截提交）。
  记录：60MB 警告后提交成功；110MB 经 `--yes` 自动确认走通 LFS 全链路（裸仓库内为标准指针
  version/oid/size，.gitattributes track 规则生成）。「拒绝→跳过」分支留人工点否复验。
- [x] **A1-4 推送**（2026-07-14，失败路径自动化；成功路径留真实推送时复验）
  步骤：`proj.push name=<已配置 remote 的测试分支>`。
  通过标准：成功/失败均有明确回显与 ExitCode 判定，失败不吞错。
  记录：无 remote 沙盒推送 → 「退出码 128 + fatal 原文」完整回显。成功路径待首次生产推送确认。
- [x] **A1-5 批量**（2026-07-14，自动化）
  步骤：`proj.commitall msg=批量验收`。
  通过标准：逐项进度回显（n/总数）；结束输出成功/跳过/拒绝/失败四类汇总；全程 UI 不冻结。
  记录：沙盒 2 项目逐项 [n/2] 进度 + 四类汇总输出正确；UI 不冻结由长任务异步机制保证，交互复验留 A5-2。
- [x] **A1-6 删除与保护**（2026-07-14，自动化 + 人工确认框待点验）
  步骤：`proj.delete name=9999-901-验收测试`；再 `proj.delete name=0000-000-Template`。
  通过标准：普通分支弹总线二次确认，确认后 worktree 与分支均消失；
  受保护分支（Template/main/master）在确认之前即被拒绝。
  记录：受保护分支未弹框直接拒绝 ✓；`--yes` 自动确认后 worktree+分支删除 ✓；
  确认对话框的人工「是/否」点击留交互复验。
- [x] **A1-7 继承树与配置**（2026-07-14，自动化）
  步骤：`proj.tree`；`proj.config` / `app.get` 读配置；`app.set` 改仓库路径后 `proj.list`。
  通过标准：树形输出反映真实分支继承关系；配置改动即时生效且重启后保留。
  记录：生产 34 分支继承树 50s 构建成功；沙盒↔生产配置切换同进程即时生效、跨重启保留。
  **修复一处 V1 遗留缺陷**：tip 相同的分支（刚创建未提交的新项目）merge-base 距离互为 0 会互认
  父子成环、从根不可达——已加不可达分量修复（tip 最早者改挂根），沙盒复现并验证修复。
- [x] **A1-8 控制台完备性（本节点总验收）**（2026-07-15，用户人工交互验收通过）
  步骤：生产仓库上，关闭全部工具窗口只留控制台，依次完成：list → tree → create（沙盒名）→
  commit → delete（刚建的沙盒名）。
  通过标准：全流程仅靠手输指令完成，每条均有回显与日志落盘。
  备注：全部指令已在沙盒+生产（只读）自动化验证；用户已亲手交互复验（含确认框点击）。

> 附注（2026-07-14）：为自动化回归新增 `--yes` 启动参数（确认通道自动通过，App 层实现，
> 框架 IConfirmationService 注释预留的场景）。日常交互使用严禁携带。
> 新增 `proj.config` 指令（PJ-12 的查看入口）。

## 节点 V2-M2 —— 界面外壳（出口：UI 与指令双向一致）

- [x] **A2-1 项目总览**（2026-07-14，机制自动化验证；视觉与双击留人工）
  步骤：打开主窗口总览视图，搜索框输入关键字，双击某行。
  通过标准：列表与 proj.list 同源；过滤即时；双击后资源管理器打开对应文件夹，
  控制台回显等价 `proj.open` 指令。
  记录：视图启动即自动执行 `[UI] proj.list` 并消费其 Data（日志可证，双向同源）；
  搜索为纯视图过滤不发指令；双击 = `proj.open name="…"` 经总线。列表新增「最后提交」列
  （单次 for-each-ref 补全，不拖慢列表）。
- [x] **A2-2 继承树视图**（代码就绪，留人工：启动自动加载缓存秒开，悬停看描述，核对树形）
  步骤：打开「继承树」窗口（自动读缓存），悬停节点，全部展开/折叠；点「重新扫描」重建。
  通过标准：与生产裸仓库真实分支一致；悬停显示描述；操作流畅。
  备注（2026-07-14 更新）：①总览/继承树已改为**独立可停靠工具窗口**（win.show name=overview / tree），
  中央区回到占位页（D7）；②继承树算法重写 v2（D8）——修正 V1"同点分叉的兄弟分支被随机挂成父子"
  的缺陷，生产 34 分支重扫从 50s 降至 2s，产出树形＝一条 AGV 继承链 + 其余平铺模板下（待你核对）；
  ③树结构缓存至 data/branch-tree.json，启动秒开，「重新扫描」= proj.tree refresh=true。
- [x] **A2-3 面板 → 指令**（面板已就位，留人工点击）
  步骤：「项目操作」面板输入项目名后点创建；「提交推送」面板填分支与描述后点提交。
  通过标准：控制台回显的指令中包含输入框**当前值**（模板验收 6 的机制）；执行结果正确。
  记录：projops / projpush 两面板已注册为可停靠窗口（win.list 可见）,panel.set 反向驱动验证通过；
  指令模板占位值显式带引号，含空格/中文输入安全。motor 演示面板与 motor.* 指令已移除。
- [x] **A2-4 危险操作双路径拦截**（留人工：UI 按钮与手输各触发一次 proj.delete / commitall）
  通过标准：两条路径弹出**同一个**总线确认，措辞一致；受保护分支两条路径都直接拒绝。
  备注：两条路径代码上收敛于同一 ConfirmPrompt（机制同模板验收 9），待交互复验。
- [x] **A2-5 布局整合**（留人工：按习惯摆布局 → 重启 → layout.save）
  通过标准：布局恢复；此布局另存为命名方案（layout.save）。

> 附注（2026-07-14）：**控制台长日志显示不全问题已修复**（用户上报）——多行日志按行拆分入列表
> + 滚动单位 Item→Pixel，`ScrollToBottom` 精确到底。改动位于 AppShell.Shell/Console（谨慎区），
> 已复跑验收 8（1000 条/秒 × 30s 承压通过）。
> ✅（2026-07-15）用户批准后已回灌 z-APPShell 升基线 0.4.2，三目录哈希比对 0 差异，**未分叉**。
>
> ✅（2026-07-15）M0~M4 全部「留人工」交互项（A1-8、A2-2~A2-5、A4-1 表窗口筛选、A4-2 悬停、
> A4-3 浏览/回收站删除）已由用户亲手验收通过。

## 节点 V2-M3 —— 模块托管（出口：DLL 进目录即成指令）

> 实现说明（2026-07-15）：ModuleHost/ALC/XmlDocs 进程内移植自 MyAPI-Lite（D3），指令注册/注销经
> WPF Dispatcher 序列化；为支持模块下线，CommandRegistry 新增 `Unregister`（已随基线 0.4.2
> 回灌模板）。样例模块位于 `samples\DemoModule`（契约类编译进模块自身）。
> 自动化验收借助新增 `debug.sleep` 指令在 --exec 序列中途投放/覆盖/删除 DLL。

- [x] **A3-1 首次装载**（2026-07-15）
  记录：运行中投放 DLL+XML → 约 0.8s 后「模块装载完成: 1 个模块,4 条指令」；
  `DemoModule.Add a=1 b=2` → 3。
- [x] **A3-2 XML 注释即帮助**（2026-07-15）
  记录：`help DemoModule.Add` 显示「两个整数相加,返回和」及 a/b 参数中文说明。
- [x] **A3-3 热更新**（2026-07-15）
  记录：同一会话内覆盖新编译 DLL（Add 改 a+b+100），前后两次同指令 3 → 103，未重启。
- [x] **A3-4 下线**（2026-07-15）
  记录：删除 DLL 后调用报「✗ 未知指令: demomodule.add」，help 不再收录该域。
- [x] **A3-5 兼容性**（2026-07-15）
  记录：MyAPI-Lite 原版未重编译产物（DemoModule.dll + 独立 BaseVariable.dll + xml）零修改装载，
  Add→42、Reverse 正常（含跨 DLL 依赖在 ALC 内解析）。
- [x] **A3-6 隔离性**（2026-07-15）
  记录：Garbage.dll（乱码）被跳过告警「Bad IL format」；BadModule.Boom 异常被总线红字兜底，
  宿主继续执行后续指令；同模块 BadModule.Fine 正常。
- [x] **A3-7 重名保护**（2026-07-15）
  记录：模块域名 proj 的 list 方法被拒绝注册并告警（§5.3 禁止覆盖），内置 proj.list 行为不变；
  同模块 proj.hello 正常注册可用。
- [x] **A3-8 module.* 管理指令**（2026-07-15）
  记录：module.list 显示名称/版本/暴露模式/指令数/描述；module.reload 后 Add 仍正确；
  module.dir 显示当前目录（另有 module.open 供 UI-12 面板打开目录）。

## 节点 V2-M4 —— 数据留痕与资源（出口：操作可追溯）

- [x] **A4-1 历史入库**（2026-07-15，表窗口筛选留人工）
  步骤：执行一次 proj.commit 与 proj.delete（沙盒），打开表窗口查 push_history。
  通过标准：两条记录字段完整（时间/分支/动作/消息/结果/警告数）；表窗口筛选可用。
  记录：沙盒 9999-903 走 create→commit→delete 全流程，db.export 断言三行字段完整
  （含中文分支名与消息）；留痕失败设计为只告警不阻断主操作。表窗口筛选为模板既有功能，留人工点验。
- [x] **A4-2 分支描述**（2026-07-15，继承树窗口悬停留人工）
  记录：`proj.note name=2026-018-MyAPI text=基础设施整合:…` 写入 branch_notes 后，
  proj.tree（走缓存、未重扫）立即显示新描述——描述覆盖在读取侧进行，改描述无需重扫。
- [x] **A4-3 资源窗口接线**（2026-07-15，浏览/reveal/回收站删除留人工）
  记录：res.root 确认工作区根 = C:\OneHistory\OneHistory-Projects（DT-04 默认已接线）；
  `res.mkdir path=..\越界测试` 被拒绝「路径越出工作区根目录」。
- [x] **A4-4 P1 项清点**（2026-07-15）
  记录：PJ-09 proj.open ✔（M1）｜PJ-10 proj.scan ✔（M1）｜PJ-11 proj.repair ✔（M1 沙盒断链修复验证）
  ｜UI-12 模块管理面板 ✔（projmod.json：module.list / reload / open）｜DT-02 branch_notes ✔（本节点）
  ｜N-03 中文路径 ✔（全程中文分支名/消息/路径均正常）｜MD-08 模块配套面板 ◑——panels 目录方式可用
  （面板 JSON 放 panels\ 即生效），「模块旁自动发现」顺延 V2.1（**用户已于 2026-07-15 确认**）。

## 节点 V2-M5 —— 交付验收（出口：V1.0 退役）

- [x] **A5-1 模板关键回归**（2026-07-15）
  记录：验收 1——console 停靠左 35% → 重启恢复一致 ✓；验收 10——layout 文件写入乱码后启动,
  「布局文件损坏,已回退默认布局」告警 + 程序正常 ✓;验收 5 / 9 已由用户在 M1/M2 人工验收覆盖。
- [x] **A5-2 承压回归**（2026-07-15）
  记录：部署版 exe 对生产 34 worktree 真实 commitall(msg=V2.0交付验收批量提交),耗时 127s,
  **成功 8 | 跳过 26 | 拒绝 0 | 失败 0**;含 [34/34] 2026-018-MyAPI 本体(commit 8d380329)。
  push_history 35 条(34 逐项目 + 1 汇总)与汇总完全吻合(commitall 已升级为逐项目留痕)。
  大文件警告 2 项目(2026-005/2026-014)如实回显并计入 warnings 字段。
- [x] **A5-3 全新机器视角冒烟**（2026-07-15）
  记录：数据目录整体备份(2MB 全量核对)后删除 → 部署版首启:目录树重建、三面板种子、
  默认配置(生产路径/阈值/保护分支)正确、空模块目录 0 装载、冷启动约 1s → 备份已原样恢复。
- [x] **A5-4 发布打包**（2026-07-15）
  记录：`dotnet publish -c Release` → 项目根 `Publish\`(17 文件) + 根目录快捷方式;
  另按用户要求部署至 **C:\OneHistory\OneHistory-Push\OneHistoryStudio\** + 快捷方式
  (自提交回避:提交自身源码时运行的是部署区 exe,不是 worktree 内产物)。两处均独立冒烟通过。
- [x] **A5-5 文档收口**（2026-07-15）
  记录：Q1 关闭(M3 方法级拒绝实测)、Q3 关闭(M2 保留 V1 展开习惯)、Q2 与 MD-08 移入 V2.1;
  交付版本 **2.0.0**,跟随模板基线 **0.4.2 未分叉**(回灌记录见 TEMPLATE_VERSION.md)。
- [ ] **A5-6 V1.0 退役**
  步骤：确认日常操作全部由 V2.0 承担一周（或用户认可的观察期）。
  通过标准：用户签字确认，V1.0 目录标记「已退役 - 仅归档」，不删除。

---

## 节点 V21-M1 —— MCP 元数据层（出口：不开网络即可看到全部工具形态）

> 实现（2026-07-15）：`src\App\Mcp\CommandSchemaExporter.cs`（MC-01~04/06）+
> `McpCommands.cs`（mcp.schema = MC-05;另加 mcp.parse 调试指令,带 argsfile= 与 exec=
> 两个自动化验收辅助参数）。零框架改动;反向解析的值编码直接复用框架 `CommandParser.QuoteArg`。

- [x] **A211-1 全量导出**（2026-07-15）
  记录：`mcp.schema` 输出 63 工具 = 注册表 69 指令 − 硬排除 6（app.exit + debug.*×3 + mcp.*×2）;
  proj_list / DemoModule_Add 等合法化名称正确;危险指令带 ⚠ 标记（ConfirmPrompt 检出）。
- [x] **A211-2 Schema 正确性**（2026-07-15）
  记录：proj.create 的 required=["name"]；db.query 的 limit 为 integer 且 default=500、page default=1;
  枚举参数导出 enum。备注：proj.create 的 base 无静态 default 属正确——其缺省值是运行时读
  proj.basebranch 配置的动态行为,静态 Schema 如实不标。M1.5 已同步修订工程实施文档 A211-2，
  消除“base 有 default”的旧验收措辞，不改变运行行为。
- [x] **A211-3 热重载联动**（2026-07-15）
  记录：运行中删除 DemoModule.dll → `mcp.schema` 即刻 59 工具(65 指令);投回 → 63(69)。
  每次现算无缓存残留（MC-04）。
- [x] **A211-4 反向解析**（2026-07-15）
  记录：经 `mcp.parse exec=true` 全链路验证——含空格/中文/嵌套引号字符串
  `{"text":"含 空格和\"引号\"与中文"}` 组装为官方转义语法并执行,Reverse 输出精确反转;
  数值 `{"a":40,"b":2}` → 42;布尔 `{"refresh":false,"cached":true}` → 走缓存分支。
  执行以 `MCP:parse` 来源留痕回显（铁律 2 预演）。

## 节点 V21-M2 —— MCP 网关上线（出口：curl 级客户端可用）

> 实现（2026-07-15）：`src\App\Mcp\McpGateway.cs`(HttpListener 127.0.0.1 + JSON-RPC 2.0 无状态子集,
> 零 NuGet 新增)+ mcp.start/stop/status(MG-05)+ mcp_history 留痕(MO-01,并入 HistoryRecorder)。
> 遵守 M1.5 接续约束:网关只对话总线,未触碰 GitRunner/路径守卫。验收全程 Invoke-RestMethod 脚本化。

- [x] **A212-1 生命周期**（2026-07-15）
  记录：mcp.start → status 显示运行/端口/策略/暴露数;mcp.stop 后连接被拒(端口释放);
  mcp.autostart=true 后宿主启动即自动开服(无需 mcp.start)。
- [x] **A212-2 协议握手**（2026-07-15）
  记录：initialize → serverInfo{OneHistoryStudio, 2.1.0} + tools capability;客户端名进日志。
- [x] **A212-3 tools/list**（2026-07-15）
  记录：readonly 档 15 个(白名单);standard 档 57 个 = 65 − 8 条危险类;
  危险指令两档均不出现在清单(§6.2);未知方法 -32601、未知工具 -32602。
- [x] **A212-4 tools/call 读**（2026-07-15）
  记录：proj_list → 「共 34 个工作树」;控制台 `[cmd:MCP:ps-test] proj.list` 回显(铁律 2);
  mcp_history 记录 client/tool/result/elapsed_ms。
- [x] **A212-5 tools/call 写(standard)**（2026-07-15）
  记录：proj_note 经 MCP 写入中文描述成功,随后 proj_tree 调用可见新描述(读取侧覆盖生效)。
- [x] **A212-6 危险拒绝**（2026-07-15）
  记录：proj_delete → isError + 「须在宿主 UI/控制台人工执行(MS-04)」;宿主无删除动作;
  留痕 result=拒绝;readonly 档下 proj_create 同样被策略拒绝并留痕。
- [x] **A212-7 token**（2026-07-15）
  记录：设 mcp.token 后无 Bearer → 401(留痕 (auth)/拒绝);带对 token → 200;
  令牌热生效——运行中清空后免认证恢复,无需重启服务。
- [x] **A212-8 承压/隔离**（2026-07-15）
  记录：BadModule_Boom → isError「模块方法异常」,宿主不崩,紧接 DemoModule_Add → 3 正常。

## 节点 V21-M3 —— Codex 真实客户端联调（出口：AI 原生使用）

- [x] **A213-1 Codex 接入**（2026-07-16）
  记录：`codex mcp add onehistory --url http://127.0.0.1:8737/mcp` 注册成功；重启后 Codex
  原生发现 OneHistoryStudio 工具；`proj_list` 返回 34 个工作树，`proj_tree cached=true` 返回
  34 分支继承树，控制台与 mcp_history 均有 MCP 来源留痕。
- [x] **A213-2 模块能力直达**（2026-07-16）
  记录：DemoModule.dll 热加载为 4 条指令；Codex 新任务原生发现
  `mcp__onehistory__DemoModule_Add`，Schema 映射 a/b 两个必填整数；调用 a=1、b=2 返回 3。
- [x] **A213-3 在线热重载**（2026-07-16）
  记录：宿主与 MCP 服务不停机，DemoModule 下线时模块 2→1、工具 56→52、Demo 工具 4→0；
  HelloWorld 始终保留。恢复 DLL/XML 后模块 1→2、工具 52→56，4 个 Demo 工具恢复并再次 Add=3。
  Codex 客户端在新任务启动时刷新工具表；服务端 tools/list 则随注册表即时现算。
- [ ] **A213-4 aimcp 复核**
  记录：可选兼容性复核；按 V2.1 方案不阻塞 M3 与本版交付。

## 节点 V21-M3 —— 真实客户端联调（出口：AI 真的在用）

- [x] **A213-1 / A213-2 / A213-3**（2026-07-15，客户端换用 Codex,验收语义不变）
  记录：详见 [07-V2.1.0-M3-Codex联调测试.md](./07-V2.1.0-M3-Codex联调测试.md)——Codex 以
  streamable_http 原生注册并发现工具;proj_list/proj_tree(cached) 原生调用通过;
  DemoModule_Add 经「DLL→指令→MCP」两跳返回 3;服务不停机完成模块下线/恢复,
  服务端工具表 56↔52 同步(客户端任务快照特性如实分开记录)。
- [x] **A213-4 aimcp 复核**：可选项,不阻塞 V2.1.0(留待用户按需复测)。

## 节点 V21-M4 —— MD-08 面板自动发现 + 收口（出口：V2.1.0 交付）

> 实现（2026-07-16）：`src\App\Modules\ModulePanelSync.cs` + ModuleHost.ReloadCompleted 事件,
> 零框架改动。语义按 MP-01 原文「等效于放进 panels 目录」:模块旁 `<模块名>.panel.json`
> 自动并入/回收 panels 副本(module- 前缀,不碰用户手写面板)+ 自动 panel.reload——
> **既有面板热刷新即时生效;全新面板窗口按框架 P-08 约定重启后出现**(控制台如实提示)。
> 运行时动态增删停靠窗口属 DockingHost 冻结区改造,列 V2.2 候选(见 04 文档开放问题)。

- [x] **A214-1 面板自动发现**（2026-07-16）
  记录：运行中投放 DemoModule.panel.json → 自动并入 panels/module-DemoModule.json +
  panel.reload +「新增面板需重启后生效: demopanel」;重启 → demopanel 成窗(win.list 可见,
  panel.set 可驱动);运行中整组移除模块 → 模块 2→1、面板副本自动回收;再重启 → 窗口消失。
  MP-03 样例 DemoModule.panel.json 随 samples\DemoModule 提供(按钮驱动 Add/Reverse)。
- [x] **A214-2 全量回归**（2026-07-16）
  记录：①验收 8 承压 1000 条/秒×30s 通过;②布局恢复 console 左 40% 重启一致(已复位);
  ③沙盒 commitall 成功 1/跳过 1/零失败;④模块 A3-3 热更新 3→103(样例已还原)。
- [x] **A214-3 文档收口**（2026-07-16）
  记录：04 文档状态改「已交付」;交付版本 **2.1.0**;TEMPLATE_VERSION.md 登记;
  框架三目录与模板 0.4.2 哈希比对 **0 差异,未分叉**;
  发布已同步部署区 C:\OneHistory\OneHistory-Push\OneHistoryStudio\(冒烟通过:
  MCP 自启动 + 2 模块 + demopanel 成窗)。

## 版本 V2.1.1 —— MCP 工具管理页 + 模块管理页（小升级）

> 实现（2026-07-16）：①「MCP 工具」页(win.show name=mcp,顶部标签组)——全部工具清单
> (搜索/危险标记/自定义标记)+ 提示词查看/编辑/保存/恢复默认;②「模块管理」页
> (win.show name=modules)——已装载模块清单(名称/版本/暴露模式/指令数/来源/描述)+
> 选中模块的注册指令明细(双击行发 help)+ 重载/打开目录按钮;③新指令 `mcp.desc`
> (提示词覆盖存 main 库 mcp_descriptions 表,导出读取侧合成)。
> 页面全部经总线消费数据与执行动作(架构不变量 1);零框架改动。

- [x] **提示词管理全链路**（2026-07-16，独立端口 8747 验证,未打扰用户运行中的实例）
  记录：`mcp.desc name=proj.list text=…` 保存 → 独立实例 tools/list 的 description 即为覆盖文本
  (跨进程持久化 ✓);`reset=true` → 下次 tools/list 立即回默认(导出现算零缓存 ✓)。
- [x] **构建与共存**（2026-07-16）
  记录：0 错误;测试实例(8747)与用户两个运行中实例(8737)并存无扰,main.db 多进程共享正常。
- [ ] **页面视觉与交互**（留人工:重启后顶部标签组见「MCP 工具」「模块管理」两页,
  编辑提示词点保存、模块页双击指令行看 help)
- [x] **部署区同步**（2026-07-17）：该项由 V2.1.2 最终部署直接覆盖完成；部署区已不再停留在 V2.1.1。

## 版本 V2.1.2 —— 应用身份与 MCP 提示词治理

> 实现（2026-07-17）：应用名/版本单一真值；提示词修订、提案、勘误、事故版本化；
> AI只可提议，本地 `mcp.*` 审核；管理页升级为三页治理工作台。

- [x] **应用身份统一**：Shell标题、MCP握手、日志、程序集和文件元数据同源；部署实测
  `OneHistoryStudio / 2.1.2`，FileVersion `2.1.2.0`。
- [x] **V2.1.1 数据迁移**：隔离库验证旧覆盖无损迁移，缺列可幂等补齐；生产数据目录真实启动迁移通过。
- [x] **治理闭环**：远程提案不自生效，批准与应用分离；冲突拒绝、拒绝理由、回滚、勘误/事故关联均通过。
- [x] **MCP策略**：readonly仅开放治理读取；standard开放提案/勘误/事故写入；`mcp.*` 审核始终硬排除。
- [x] **构建与部署**：Debug/Release/Publish均0错误0警告；Publish与部署区17文件哈希完全一致；
  `proj_list`返回34工作树，`module_list`正常。
- [ ] **页面视觉与交互**：人工检查“当前修订/提案审核/勘误与事故”三页，实际点击批准→应用、拒绝和回滚。

## 节点 V216 —— 代码质量整备（方案见 15 号文档）

### Q16-M1 框架分叉转正

- [x] **AQ16-1 差异审阅**（2026-07-16）
  记录：五文件差异逐条审阅——Registry(source 溯源/GetSource/Changed,默认值兼容旧调用)、
  Parser(指令名放宽多段)、Bus(可选 CancellationToken+取消文案)、DockingHost(默认比例种子
  保护+比例施加覆盖停靠组,属比例语义修正)、BuiltinCommands(help 分组计数/列宽/详情类型与
  安全·线程提示)。**全部为通用能力,无派生应用专有逻辑混入**,无需迁回。
- [x] **AQ16-2 回灌 0.4.3**（2026-07-16）
  记录：五文件整体回灌 z-APPShell;模板独立构建 0 警告 0 错误;双侧三目录哈希比对 **0 差异**;
  两份 TEMPLATE_VERSION 已登记 0.4.3(派生侧附纪律备注:先改后报下不为例)。
  另按 §8-1 完成 v215 基线快照采集:docs\snapshots\v215\{console-baseline.log(help/
  mcp.schema/win.list/proj.config), command-manual.md(40KB)}——Q16-M2 起的行为对账基准。

### Q16-M2 死代码与迁移收敛

- [x] **AQ16-3 死代码清除**（2026-07-16）
  记录：删除 Stub 占位工厂、SeedDemoData+DemoNames/DemoCities、debug.seedbench 及
  System.Windows.Controls/Media 无用 using;类头注释更新为当前职责。App.xaml.cs
  600 行/25KB → 362 行/18KB;构建 0 警告 0 错误;help 94→93 条(差值=seedbench,
  MCP 工具 79 个不变,与基线快照吻合)。
- [x] **AQ16-4 迁移器**（2026-07-16）
  记录：新建 StartupMigrations.cs——app.migrated 版号闸(v1 收编 motor/projmod/projops
  三代面板清理+users/bench 演示表回收),V2.1.5 指令详情布局初始化逻辑原样迁入
  (沿用旧键作状态位,含义为"布局已初始化",与历史清理语义不同故不并入版号,15 号文档
  §4 的"回收旧键"一条按此修正)。首启:自动备份 data/main.db.bak-v216 → DROP users/bench
  → 版号落盘;实测 db.tables 剩 8 张全为业务表;**二次启动迁移相关日志 0 行**。

### Q16-M3 复用整合与装配点瘦身

- [x] **AQ16-5 复用整合**（2026-07-16）
  记录：R1——`Data\SqlText.cs` 成为 SQL 转义/截断唯一实现,HistoryRecorder 与
  PromptGovernanceStore 取用(各表时间戳格式原样保持);R2——按 DQ16-4 否决视图基类
  (WPF 要求 XAML 根元素与代码后置基类一致,改基类须动 9 个 XAML 根),改为组合式
  `Views\ViewKit.RunOnceOnLoaded`,三个纯守卫视图(Modules/BranchTree/ProjectOperations)
  接入并删除样板;OverviewView/McpToolsView/MetaView 的标志兼作状态位,保留自持字段并留注;
  R3——评估后**放弃**:ParamType 三向映射各司其职(CLR→ParamType/字符串→CLR/ParamType→
  JSON Schema),共享表反而增重,不为抽而抽;R4——proj.commit 与 commitall 的提交留痕
  (结果映射+警告数)收敛为 ProjectCommands.RecordCommit 单实现;R5——source 标签经 help
  输出通检([framework/app/module]),一致无误。本里程碑净减约 44 行(新增共享件 136,
  删除样板 180),构建 0 警告 0 错误。
- [x] **AQ16-6 装配点瘦身**（2026-07-16）
  记录：debug.logflood/sleep 迁入 `DebugCommands.cs`(source="app" 原样,help 标签不变);
  10 个 ToolWindowDescriptor 改表驱动;异常挂接与 --exec 解析抽私有助手。
  App.xaml.cs 600(整备前)→247 行,**OnStartup 145 行(≤150 达标)**;
  win.list 与 proj.config 输出与 v215 基线快照**逐行 0 差异**。

### Q16-M4 行为不变回归与交付

- [x] **AQ16-7 快照对比**（2026-07-16）
  记录：v216 全量快照采集(docs\snapshots\v216\)与 v215 diff——console 13 行、手册 26 行,
  **逐行核对全部属允许差异**:版本号 2.1.5→2.1.6、seedbench 整节及其计数影响
  (指令 94→93、debug 组 3→2、硬排除 15→14)、快照采集手法差异(v215 的 command.manual
  曾分跑);MCP 工具全量 79 个不变,手册 SHA-256 由指令落盘可复算。
- [x] **AQ16-8 关键回归**（2026-07-16）
  记录：①承压 1000 条/秒×30s 通过;②布局恢复 console 左 45% 重启一致(已复位);
  ③沙盒 9999-916 create→commit(138c9e0)→delete 全链路;④模块热更新 3→103(样例已还原);
  ⑤MCP:serverInfo 报 2.1.6、standard 68 工具、proj_list 提示词经治理层读出、调用成功、
  proj_delete 拒绝。备注:回归发现 mcp.autostart=true 时 `mcp.start port=` 会因服务已自启
  而拒绝(行为正确),独立端口测法需先 mcp.stop——记入运维备注。
- [x] **AQ16-9 交付**（2026-07-16）
  记录：版本 2.1.6(csproj 程序集元数据,AppIdentity 单一真值链路验证);
  Release publish → Publish\ → 部署区同步;部署版冒烟:启动完成 2.1.6 + MCP 8737 自启动。
  15 号文档状态改「已交付」。

## 节点 V220 —— 自扩展飞轮（方案见 17 号文档）

### V22-M1 清单与发现

> 实现（2026-07-18）：`Git\ToolManifest.cs`(清单模型+装载校验)、`Git\ToolSyncService.cs`
> (扫描+SHA-256 部署状态+tool_registry)、`Git\ToolCommands.cs`(tool.scan,source="tool");
> McpExposurePolicy 白名单加 tool.scan。发现挂点复用 ListMetaFoldersAsync(V2.0.1 Meta),
> 路径校验按 M1.5 守卫等级。修正一处 17 号文档 §9-7:tool_registry 建表走构造期幂等
> (HistoryRecorder 先例),不占 StartupMigrations 版号(迁移器只管一次性清理)。
> v217 快照基线已采(docs\snapshots\v217\)。

- [x] **A220-1 manifest 解析**（2026-07-18,沙盒）
  记录：一次扫描五种形态全对——ToolDemo 1.0.0 字段齐全;缺 artifact 报错;
  `..\..\evil.dll` 越界拒绝;DupTool 重名第二份报「已被声明」;产物缺失状态标注;
  逐项报错不中断(TL-03)。
- [x] **A220-2 部署状态判定**（2026-07-18,沙盒）
  记录：同一产物三连:未同步(无溯源行)→ 最新(注入含真实 SHA-256 的溯源行)→
  已过期(产物追加字节后重扫)。判定逻辑与 tool_registry 对账通路验证完毕,
  真实 sync 写入路径 M2 落地后随 A220-4 复验。
- [x] **A220-3 Codex 盘点**（2026-07-18,HTTP 代验;Codex 原生签字留用户,同 V21-M3 惯例）
  记录：readonly 档 tools/list 25 工具含 tool_scan;调用成功,文本段+结构化 JSON 段
  均含清单数据。待用户以 Codex 新任务原生复核后本条完全关闭。

### V22-M2 同步、溯源与模块槽

> 实现（2026-07-18）：ToolSyncService 补 SyncAsync/Remove/ListRegistered(TS-01~05,
> 槽路径守卫+复制后 SHA-256 复核);ToolCommands 补 tool.sync(standard)/tool.remove
> (ConfirmPrompt,dangerous)/tool.list(readonly);ModuleHost 模块槽改造(MH-01~05):
> 每 Modules\<槽>\ 一个独立可回收 ALC、根平铺兼容保留、watcher 含子目录、
> module.list 增列(根)/(槽)来源;ModulePanelSync 扫一层槽目录(MH-04)。零框架改动。

- [x] **A220-4 tool.sync 单个/全量**（2026-07-18,沙盒）
  记录：ToolAlpha/ToolBeta 经 `tool.sync all=true` 入槽,热重载后指令即用;
  tool_registry 溯源(分支/版本/SHA-256/时间)完整,tool.list 与 module.list 视角互补。
- [x] **A220-5 模块槽隔离**（2026-07-18,沙盒)
  记录：两槽各携同名 SharedDep.dll(内容不同版):ToolAlpha.Ver → 「alpha-v1」、
  ToolBeta.Ver → 「beta-v2」,槽内独立解析互不串扰;根平铺 DemoModule/HelloWorld
  同场共存,DemoModule.Add 仍 → 3(兼容不受影响)。
- [x] **A220-6 tool.remove**（2026-07-18,沙盒）
  记录：残行清理(ToolDemo 槽缺失仅溯源行 → 清理成功);全量移除(ToolBeta:槽删+
  溯源销+热重载后调用报「未知指令」);随后 `tool.sync name=ToolBeta` 再入库复活。
  受总线确认闸口保护(--yes 自动化通过)。
- [x] **A220-7 幂等**（2026-07-18,沙盒）
  记录：全量同步后再次 `tool.sync all=true` → 「没有需要同步的工具」,零变化零副作用。
- [ ] **A220-8 页面按钮**（TS-06,P1——按 17 号文档允许**顺延至 V22-M4 收口**,
  与模块管理页合并展示 scan 结果一起做,本条留待补验)

### V22-M3 Codex 飞轮闭环

> 实现（2026-07-19）：CX-01/Q211-2 落地——McpExposurePolicy 增模块感知钩子
> (ModuleOfCommand 经 Registry.GetSource 识别 "module:<名>",ModuleExposure 查
> tool_registry.mcp_exposure 列,带失效缓存);ToolSyncService 同步时落暴露档;
> 全部消费方(网关可见性/调用闸/exporter 硬排除/command.*/手册)经策略单一真值自动生效。
> 版本 2.2.0-M3 已发布并同步部署区(冒烟通过);Codex 第一圈操作手册见 18 号文档。

- [x] **A220-10 mcpExposure 三档**（2026-07-19,沙盒,Q211-2 关闭）
  记录：ToolAlpha 声明 readonly → readonly 档 tools/list 可见且调用返回 alpha-v1;
  ToolBeta 声明 hidden → readonly/standard 两档均不可见,调用 -32602 未知工具(拒);
  根平铺模块(DemoModule 等)无清单 → null=standard 现状不受影响。
- [x] **A220-9 五跳全链路**（2026-07-21,Codex 第一圈按 18 号手册跑通）
  记录：Codex 建 2026-019 工具项目 → 写 ToolKit → 构建 → tool_sync 入库 →
  新任务原生调用 ToolKit_* 得正确结果;ToolKit.Sha256/NewGuid/Now 三指令已装载为证
  (help 全量 102 条含之)。备注:tool.list 溯源仅见 ToolAlpha/Beta 两行、ToolKit 溯源行缺,
  疑与 Codex 沙盒终端的虚拟文件层有关(见 [[sandbox-fs-divergence]] 记录)——ToolKit DLL
  在真实 Modules 目录且已装载,但真实 main.db 的 tool_registry 未收 ToolKit 行;
  用户可在真实实例重跑一次 `tool.sync name=ToolKit` 补齐溯源(不影响 M4 交付)。
- [ ] **A220-11 过期迭代**（待 Codex 第二圈:改源码重建 → 已过期 → 再 sync → 新任务验新行为）

### V22-M4 宿主确认中继 + 收口

> 实现（2026-07-21）：Q211-1 关闭——`mcp.confirm`(deny 默认 / host)+`mcp.confirmtimeout`
> (默认 60,夹 10~600)。host 档:危险工具进 tools/list(供 Codex 发现,带⚠标注)、tools/call
> 时经 `RemoteConfirmDialog`(带倒计时、独立于总线 Confirmation 故 --yes 不生效)由人裁决。
> 批准执行经 `McpConfirmationScope` 预批准域(AsyncLocal)+ `GatewayAwareConfirmation` 包装,
> 使总线不二次弹框且 --yes 对 MCP 危险调用不放行(CX-03);全程留痕 mcp_history。零框架改动。

- [x] **A220-12 deny 档回归**（2026-07-21）
  记录：mcp.confirm 缺省=deny;proj_delete 不进 tools/list、tools/call 被拒并提示切 host。
- [x] **A220-13 host 档中继**（2026-07-21,超时态自动化;点是/点否两态待 Codex/用户交互签字）
  记录：host 档下 proj_delete 进 tools/list(带⚠"需宿主人工确认"标注);tools/call 弹倒计时
  确认框;10s 无人操作 → 精确 10s 后 mcp_history 记「确认超时」并回 isError。
  点「允许执行」→ 执行(预批准域使总线不二次弹框)、点「拒绝」→「远程拒绝」两态机制就绪,
  留 Codex 发起 + 用户点击复核(同 A213/A220-9 交互签字惯例)。
- [x] **A220-14 --yes 不放行**（2026-07-21）
  记录：--yes 启动 + host 档,proj_delete 经 MCP 未被自动确认——弹框等待并超时拒绝(若
  --yes 漏放会立即执行);证 --yes 只对 UI/手动/脚本生效,MCP 危险调用恒走中继(CX-03)。
- [x] **A220-15 回归与交付**（2026-07-21）
  记录：v217↔v220 快照 diff 全属允许增量(tool.* 域 4 条 + 已同步工具模块 5 条,93→102;
  proj.config/win.list 零差异;手册 SHA-256 可复算);版本 2.2.0 发布 + 部署区同步 +
  冒烟(2.2.0 启动、host 档、tool.list 就位);17 号文档状态更新。
  A220-8 页面按钮(TS-06,P1)按 D220-2「Codex/控制台为飞轮主接口」判定**顺延 V2.2.1**。

## 节点 V221 —— 文件格式全覆盖（方案见 20 号文档）

### V221-M1 扫描与台账

> 实现（2026-07-22）：`Git\FormatInventoryService.cs`（按格式聚合 + 三档深度 + 缓存增量）、
> `git.rule.scan` / `git.rule.gaps` 两条只读指令、`GitFileRuleService.ReadDeclaredRulesAsync`
> （规则解析保持唯一实现，不重复造轮）。归宿判定以 git ls-files 三态为权威，不自行实现忽略匹配。

- [x] **A221-1 全库扫描**（2026-07-22）
  记录：279 种格式 / 40,314 文件 / 35 项目；**quick 档全库 1,090ms**（预算 ≤8s）、
  normal 1,671ms、deep 2,939ms（deep 的 35×725ms LFS 枚举经并行压到 2.9s）。
  覆盖率首测 **17.9%**（未决 33,111）——量化坐实了「规则几乎没覆盖」的问题。
- [x] **A221-2 缓存与增量**（2026-07-22）
  记录：二次扫描 **380ms / 缓存命中 35+35**（预算 ≤1s）；失效键含 depth，
  换档位自动重扫；`refresh=true` 全量重扫生效。
- [x] **A221-3 缺口清单**（2026-07-22）
  记录：`git.rule.gaps` 输出 268 种未决格式 + 84 个目录候选；
  **Unity `b-Unity-Teach1/Library` 以 4,330 个无扩展名文件居首**，与设计预测一致。
- [x] **A221-4 结构化**（2026-07-22）
  记录：readonly 档 MCP 可见并可调用 `git_rule_scan` / `git_rule_gaps`；
  结构化 Data 含覆盖率/未决数/267 格式行/84 目录行，Codex 可直接分析全库格式分布。

> **实测发现（M2 建议引擎的直接依据）**：`.eod` 3,108 个合计 **2.06GB**、
> `.par` 2,719 个合计 **1.52GB**（最大单件 94.7MB，LFS 首选）、`.eox` 367MB、`.dll` 390MB、
> `.lck` 2,712 个（每个 4B 的锁文件）——全部处于未决状态。
> 实施中修正一处与文档意图不符的缺陷：体积统计原按「每格式代表文件」估算，
> 已改为**逐文件 stat 求准确最大/合计**（全库仅多耗 580ms），否则 LFS 建议会失真。

### V221-M2 建议引擎与目录规则

> 实现（2026-07-23）：`Git\RuleSuggestionEngine.cs`（分类清单 + **内容嗅探**）、
> `git.rule.suggest` 指令、`GitFileRuleService` 的 pattern 扩容支持目录形态。
> 按 D221-9 删除过度设计：扫描三档 → 布尔 `deep`（体积统计恒开）、
> 不新增 `git.rule.dir`（`git.rule.set` 依形态自判）、不做可配置扩展名清单。
> 按 Q221-3 落地散尾归并：目录候选阈值 50 个文件，未达标的并入 `other` 一行。

- [x] **A221-5 建议引擎**（2026-07-23）
  记录：全库 **169 条建议覆盖 27,453 个未决文件**，98 种未知格式留白不猜。
  `.eox/.eod/.lck/.cache/.dll` → 忽略；`.cs/.md/.json/.xml` → Git+LF；
  `.par`（最大 94.7MB）→ **Git+LFS**。
  **实施中查获并修正两处会损坏文件的缺陷**：
  ①`.asm`(1333 个)/`.cfg`(538 个) 被按扩展名归为"文本源码"，但实测二者在本库是
  Solid Edge 的 **OLE2 复合文档**（`D0 CF 11 E0`）——若应用 `text eol=lf`
  会损坏这 1,871 个 CAD 文件。已加**内容嗅探**（读样本前 512 字节，每格式一次
  即 O(格式) 而非 O(文件)），嗅探结果优先于硬编码清单；顺带使建议数 84→169、
  未知格式 183→98。②LFS 阈值原取 `proj.rejectmb`(100MB)，导致 94.7MB 的 CAD 件
  差 5MB 不触发；改用 `proj.warnmb`(50MB)——超警告线的二进制就该走 LFS。
- [x] **A221-6 目录规则**（2026-07-23，沙盒）
  记录：`git.rule.set pattern=Library/` 预览→应用成功，`.gitignore` 写入托管块，
  git 实测忽略该目录 5 个文件；三类非法一律拒绝——`../越界/`(含 ..)、
  `C:/x/`(绝对路径)、`Library/ track=true`(目录规则只能用于忽略)。
- [x] **A221-7 预览-应用**（2026-07-23，沙盒）
  记录：托管块外手写内容（中文注释 + 自定义规则）在再次应用规则后**逐字保留**；
  新规则并入块内。
- [x] **A221-8 覆盖率提升**（2026-07-23，沙盒）
  记录：沙盒项目应用规则后复扫，覆盖率 29.4% → 35.3%、未决 12 → 11、忽略 5 → 6。
  **生产库全程只读**（扫描/建议），复核后 35 个项目仍无根 `.gitignore`，未发生任何写入。

### V221-M3 基线统一、页面与交付

> 实现（2026-07-23）：`GitFileRuleService` 增 baseline 块（`# OneHistoryStudio baseline
> begin/end`）与 `SyncBaselineAsync`、`git.rule.sync` 指令（危险类，走确认闸口）；
> 项目操作页增覆盖率与四个入口按钮；模块管理页补做 A220-8 工具按钮。零框架改动。

- [x] **A221-9 基线双块**（2026-07-23，沙盒）
  记录：模板建基线（`*.eod` 忽略 + `*.cs` LF）→ `git.rule.sync` 预览 → 应用。
  目标项目 `.gitignore` 呈现**三层完整分离**：baseline 块(`*.eod`) +
  managed 块(`*.tmp`/`Library/` 项目自有) + 块外手写内容(`# 我手写的注释,不许动`)；
  `.gitattributes` 同理（baseline 块 + 块外既有 LFS 行保留）。基线重刷不伤后两者。
- [x] **A221-10 模板治理**（2026-07-23，**生产模板已改，留未提交供审阅**）
  记录：①模板新建根 `.gitignore`（13 条派生件忽略，保守取向：只收 EPLAN/SolidEdge 派生
  与通用临时件，**未含 `*.dll`/`bin/`/`obj/`**——这些在本库可能是交付物，留给用户定）；
  ②`.gitattributes` 清除 5 行垃圾模式（`\#`、`常见视频格式`、`Pro`、`工程文件`、`Premiere`
  ——中文注释被 `git lfs track` 拆碎的产物，已随继承污染 33 个项目），保留 15 条有效 LFS
  规则并纳入 baseline 块。模板规则 28 条、垃圾残留 0；模板项目覆盖率升至 87.5%。
  **改动仍未提交**（当前含 Claude 已暂存内容与 Codex 工作区补正）——待用户审阅后统一提交。
  Codex 交付复核补正：原文件实际仅 11 条忽略，且 managed 块残留
  `*.eod/*.eox text eol=lf`；现已移除两条危险二进制 LF 规则并将其加入忽略块，实数与文档的
  13 条一致。`git check-attr` 对两格式返回 text/eol/filter 均 unspecified，`git check-ignore`
  命中 `.gitignore`。
- [x] **A221-11 页面**（2026-07-23）
  记录：项目操作页规则区新增覆盖率条与四入口——「扫描覆盖」（回填覆盖率/未决数）、
  「查看未决」、「规则建议」、「同步模板基线（预览）」；pattern 输入框提示扩展为
  「格式 或 目录（以 / 结尾）」。基线同步在页面只发预览，写入须控制台显式 apply（人在环上）。
  Codex 交付复核勘误：该记录只证明按钮与覆盖率条，未证明扫描格式进入规则表；完整入表在
  V2.2.2 A222-1~10 实现并由合并模型回归关闭。
- [x] **A221-12 A220-8 补做**（2026-07-23）
  记录：模块管理页增「扫描项目库工具」「同步全部工具」「移除所选工具」三按钮
  （tool.scan / tool.sync all=true / tool.remove，后者随选中态启用并走确认闸口）。
  V2.2.0 顺延项结清。
- [x] **A221-13 回归与交付**（2026-07-23）
  记录：v220↔v221 快照 diff 18 行**全属允许增量**——指令 102→106（新增 git.rule.
  scan/gaps/suggest/sync）、MCP 工具 87→91、`git_rule_sync` 正确标记⚠危险；
  `proj.config`/`win.list` 零差异。版本 2.2.1 发布 + 部署区同步 + 冒烟通过。
  **生产库 34 个项目未被写入**（基线同步只跑了预览：34 个项目待更新，等用户 apply）。
  Codex 复验补充：修复 V213Smoke 装配断裂与 PromptGovernanceSmoke 版本硬编码后，
  Debug/Release 两套均 PASS；正式 MCP 对模板和 `2026-018-MyAPI` 的 scan/gaps/suggest 通过。

## 节点 V222 —— Git 文件规则全格式入表（方案见 21 号文档）

- [x] **A222-1~4 全格式入表模型**（2026-07-23）
  记录：页面读取 `git.rule.scan + git.rule.list`，扫描格式、零文件规则、目录规则去重取并集；
  无扩展名作为只读汇总行。合并回归验证 2 个扫描行 + 3 个声明输入输出 4 行，`*.cs` 仅一行，
  `*.xlsx` 零文件规则与 `Library/` 目录规则均保留。
- [x] **A222-5~10 状态与编辑闭环**（2026-07-23）
  记录：未声明行保持 nullable；选择 Git 后显式落 LFS=false/LF=false；LF 行断言精确为
  `Git + LF（文本，非 LFS 指针）`。保存/删除后强制重扫，删除规则后扫描行仍保留。
- [x] **A222-11~14 回归**（2026-07-23）
  记录：加载代次防迟到覆盖；缓存自动加载只扫所选项目；V213Smoke/PromptGovernanceSmoke
  Debug+Release 全过；现有 `git.rule.*` 指令和 MCP 映射未增删。
- [x] **A222-15 自动交付**（2026-07-23）
  记录：Debug/Release/Publish 0 error；版本单点推进 2.2.2；Publish/部署区 60 文件哈希差异 0；
  正式 EXE SHA-256 `E24E7FB56B308CB3D0F197B243576B9B06A4AB57A41F7E0E76C90C59CD0A4C17`；
  回滚包 `OneHistoryStudio-rollback-2.2.1-20260723-213000`。
- [ ] **A222-16 页面视觉复验**
  记录：自动截图受本机 WPF `0x80004002` 接口错误阻断，未盲点操作；用户在部署版选择至少一个
  非模板项目，确认全格式行及「LFS 指针 / LF 文本 / 存储结果」列无截断后签字。

## 节点 V230 —— 分支历史与安全回滚（方案见 22 号文档）

- [x] **A230-1~5 历史计算与分页**（2026-07-24，临时裸仓库）
  记录：三层分支验证父分支与 merge-base；分叉点固定首行；第一父链自有提交按时间正序；
  limit/skip 分页始终保留基线；origin 已推送/仅本地/未知状态由跟踪引用计算。
- [x] **A230-6~9 详情、差异与安全恢复**（2026-07-24，V230Smoke）
  记录：提交详情、文件变更和 fork->HEAD 差异正确；恢复操作追加一个提交并保留旧链；
  脏工作树、空说明、越界对象以及确认后 HEAD 变化均在写入前拒绝。
- [x] **A230-10~13 硬重置与 lease 保护**（2026-07-24，V230Smoke）
  记录：硬重置只移动本地 HEAD，远端保持不变；保护分支拒绝 reset/forcepush；正常
  `--force-with-lease` 成功，模拟竞争者更新远端后 stale lease 明确失败。
- [x] **A230-14 全回归与自动交付**（2026-07-24）
  记录：主解决方案与 V213Smoke/PromptGovernanceSmoke/V230Smoke 的 Debug/Release 构建均
  0 warning / 0 error，六套 Smoke 全部 PASS。2.3.0 运行时生成命令手册 106 条，三条历史读取
  为 readonly，rollback/reset/forcepush 为 dangerous。Publish/部署均 60 文件、逐文件哈希差异 0；
  正式 EXE SHA-256 `08579D91CB7AFB3BEA4143F15DBEFBE662B743914CDCCC63D79412577D80D2B5`；
  回滚包 `OneHistoryStudio-rollback-2.2.2-20260724-095134`。
- [ ] **A230-15 页面视觉复验（自动部分通过，交互签字待用户）**
  记录：Windows 无障碍树确认部署同源 Release 标题 2.3.0、旧布局自动补入左侧历史窗、模板
  分叉点/HEAD/28 节点/origin 未知状态和表头完整；正式部署只读命令冒烟通过。截图通道仍被本机
  WPF `0x80004002` 阻断，UIA 输入框 ValuePattern 也不可用，故未盲点执行右键和三个对话框；
  用户只需在部署版复核这四项交互即可签字，不影响后端安全与发布验收。

## 节点 V231 —— 子模块提交与推送（方案见 23 号文档）

- [x] **A231-1~5 发现、兼容与边界**（2026-07-24，V231Smoke）
  记录：标准 `.gitmodules` 和无声明历史 `160000 gitlink` 均正确识别；无子仓库保持旧行为；
  越界、未初始化和发生变化的第二层 gitlink 均在写入前拒绝。
- [x] **A231-6~10 子先父后提交**（2026-07-24，V231Smoke）
  记录：修改、删除、未跟踪文件由子仓库 `git add -A` 纳入；子描述覆写/回退、干净跳过、
  父 gitlink 更新均正确；第二个子仓库钩子失败时父提交未发生，首个子提交保留并报告部分完成。
- [x] **A231-11~15 推送、批量与兼容**（2026-07-24，V231Smoke）
  记录：子推送全部成功后才推父；脏工作树、detached、无 origin、父子指针不一致均拒绝；
  commitall/pushall 复用同一路径；四条旧命令的 `submodules` 默认值均为 false。
- [x] **A231-16~20 回归与自动交付**（2026-07-24）
  记录：中文/空格参数通过；主解决方案 Debug/Release 0 warning / 0 error，四套 Smoke 两种配置
  共八次全部 PASS；2.3.1 运行时生成命令手册 106 条；Publish/部署均 60 文件、逐文件哈希差异 0；
  正式 EXE SHA-256 `8C79808FE81AE5F902F38060A0AC0F0D29360429313BCA7E9A152754F8197CED`；
  回滚包 `OneHistoryStudio-rollback-2.3.0-20260724-113946`。
- [ ] **A231-21 页面视觉复验（标题通过，交互签字待用户）**
  记录：Publish 窗口标题确认 `OneHistoryStudio v2.3.1`；本机 WPF 捕获仍报 `0x80004002`，
  未使用盲点坐标点击。用户只需确认项目操作和项目总览中的“包含子模块 / 沿用父提交描述 /
  子模块描述”布局及启用状态，不影响结构化命令和 Git 安全验收。

## 验收签署

| 节点 | 完成日期 | 验收人 | 备注 |
|---|---|---|---|
| V2-M0 骨架派生 | 2026-07-14 | | 五项全过 |
| V2-M1 proj.* 指令域 | 2026-07-15 | | 自动化 + 用户交互复验（A1-8）全过 |
| V2-M2 界面外壳 | 2026-07-15 | | 含控制台修复与继承树算法 v2 / 缓存；用户交互复验全过 |
| V2-M3 模块托管 | 2026-07-15 | | A3-1~8 全过；Unregister 随 0.4.2 回灌模板 |
| V2-M4 数据留痕 | 2026-07-15 | | A4-1~4 过；MD-08 自动发现顺延 V2.1（用户确认） |
| V2-M5 交付验收 | 2026-07-15 | | A5-1~A5-5 全过；仅剩 A5-6 V1.0 退役（观察期后用户签字） |
| V21-M3 Codex 联调 | 2026-07-16 | Codex / 用户 | A213-1~3 全过；aimcp 可选复核不阻塞 |
| V2.1.6 代码质量整备 | 2026-07-16 | | Q16-M1~M4 全过；基线回灌 0.4.3 未分叉 |
| V22-M1 清单发现 | 2026-07-18 | | A220-1/2 沙盒过；A220-3 HTTP 代验，Codex 签字留用户 |
| V22-M2 同步与模块槽 | 2026-07-18 | | A220-4~7 沙盒过（槽隔离实证）；A220-8 顺延 V2.2.1 |
| V22-M3 mcpExposure | 2026-07-19 | | A220-10 三档过；A220-9/11 Codex 闭环 |
| V22-M4 确认中继 | 2026-07-21 | Codex / 用户 | Q211-1 关闭；A220-12/14/15 自动化过，中继点击两态待交互签字 |
