# OneHistory-V2.0 · 项目验收单

> 用法：每完成一个里程碑，按本单逐条执行验收步骤并勾选；全部通过才进入下一节点。
> 「验收步骤」是可照做的操作序列；「通过标准」是唯一判据。
> 涉及生产裸仓库的破坏性验收（删除/修复）一律先在沙盒仓库演练（见开发提示 §4-8）。
> 验收记录：勾选 `[x]` 并在行尾追加日期，如 `……（2026-07-20）`。

---

## 节点 V2-M0 —— 骨架派生（出口：能跑的空壳）

- [x] **A0-1 派生完成**（2026-07-14）
  步骤：拷贝 z-APPShell 解决方案 → 更名 OneHistoryStudio → 删除 WpfApp1 与 bin/obj → 构建。
  通过标准：`dotnet build` 零错误零警告（环境变量 `DOTNET_EnableWriteXorExecute=0` 已设置）。
  记录：解决方案更名 OneHistoryStudio.sln；App 项目 AssemblyName/RootNamespace = OneHistoryStudio；构建 0 警告 0 错误。
- [x] **A0-2 启动冒烟**（2026-07-14）
  步骤：运行程序。
  通过标准：窗口标题为「OneHistory 项目管理工具 V2.0」；主窗口占位页显示应用名与版本；
  控制台输入 `help` 输出完整内置指令表；冷启动 ≤ 2 秒。
  记录：标题实际为「OneHistory 项目管理工具 v2.0.0-M0」（Shell 按 AppName+AppVersion 自动拼接）；
  `--exec help` 输出 45 条指令；启动→执行→退出全程 902ms。
- [x] **A0-3 数据目录就位**（2026-07-14）
  步骤：检查 `%AppData%\OneHistoryStudio\`。
  通过标准：layout / data / workspace / logs / panels 等目录结构自动生成。
  记录：五目录 + main.db / history.txt / window.json / motor.json 全部生成。
- [x] **A0-4 布局持久化回归**（2026-07-14，指令级）
  步骤：把控制台拖到左侧停靠、调整比例 → 退出 → 重启。
  通过标准：布局原样恢复（模板验收 1 的回归）。
  记录：`win.dock name=console pos=left ratio=0.3` → 重启 → `win.list` 显示「console 停靠·左 30%」，
  持久化链路通过；鼠标拖拽手势留人工复验（同模板验收惯例）。验收后已 layout.reset 回默认。
- [x] **A0-5 基线登记**（2026-07-14）
  步骤：检查 TEMPLATE_VERSION.md。
  通过标准：已登记「跟随基线 0.4.1-M4，未分叉」。
  记录：docs\框架文档\TEMPLATE_VERSION.md 顶部新增本应用登记段。

## 节点 V2-M1 —— proj.* 指令域（出口：纯控制台可管理项目库）

> 本节点验收全程**不使用任何工具窗口**，只在控制台手输指令——这是控制台完备性的落点。
> A1-1 ~ A1-6 在沙盒裸仓库执行；A1-8 在生产仓库以只读指令复核。

- [x] **A1-1 列表**（2026-07-14，自动化）
  步骤：`proj.list`，再 `proj.list filter=2026`。
  通过标准：输出与 `git worktree list` 一致；过滤生效；含编号/分支/路径/状态。
  记录：沙盒与生产（34 个 worktree）均验证；filter 包含匹配生效；目录缺失时标注「疑似断链」。
- [x] **A1-2 创建**（2026-07-14，自动化）
  步骤：`proj.create name=9999-901-验收测试`。
  通过标准：分支与同名 worktree 目录生成；基于分支默认 0000-000-Template；
  重复创建、含 `\/:*?"<>|` 的名称、目标目录已存在三种情况分别被明确拒绝且不崩溃。
  记录：沙盒验证创建成功（含 base= 指定基础分支）；重复名/非法字符/目录冲突三类拒绝路径全过。
- [x] **A1-3 提交与大小检查**（2026-07-14，自动化）
  步骤：在测试 worktree 放入普通文件 + 一个 60MB 文件 → `proj.commit name=9999-901-验收测试 msg=验收`；
  再放入一个 110MB 文件重复提交。
  通过标准：60MB → 警告回显但提交成功；110MB → 触发 LFS 流程（无 LFS 环境则明确失败提示；
  确认对话拒绝时该项目跳过，结果为 Rejected 且不产生半截提交）。
  记录：60MB 警告后提交成功；110MB 经 `--yes` 自动确认走通 LFS 全链路（裸仓库内为标准指针
  version/oid/size，.gitattributes track 规则生成）。「拒绝→跳过」分支留人工点否复验。
- [x] **A1-4 推送**（2026-07-14，失败路径自动化；成功路径留真实推送时复验）
  步骤：`proj.push name=<已配置 remote 的测试分支>`。
  通过标准：成功/失败均有明确回显与 ExitCode 判定，失败不吞错。
  记录：无 remote 沙盒推送 → 「退出码 128 + fatal 原文」完整回显。成功路径待首次生产推送确认。
- [x] **A1-5 批量**（2026-07-14，自动化）
  步骤：`proj.commitall msg=批量验收`。
  通过标准：逐项进度回显（n/总数）；结束输出成功/跳过/拒绝/失败四类汇总；全程 UI 不冻结。
  记录：沙盒 2 项目逐项 [n/2] 进度 + 四类汇总输出正确；UI 不冻结由长任务异步机制保证，交互复验留 A5-2。
- [x] **A1-6 删除与保护**（2026-07-14，自动化 + 人工确认框待点验）
  步骤：`proj.delete name=9999-901-验收测试`；再 `proj.delete name=0000-000-Template`。
  通过标准：普通分支弹总线二次确认，确认后 worktree 与分支均消失；
  受保护分支（Template/main/master）在确认之前即被拒绝。
  记录：受保护分支未弹框直接拒绝 ✓；`--yes` 自动确认后 worktree+分支删除 ✓；
  确认对话框的人工「是/否」点击留交互复验。
- [x] **A1-7 继承树与配置**（2026-07-14，自动化）
  步骤：`proj.tree`；`proj.config` / `app.get` 读配置；`app.set` 改仓库路径后 `proj.list`。
  通过标准：树形输出反映真实分支继承关系；配置改动即时生效且重启后保留。
  记录：生产 34 分支继承树 50s 构建成功；沙盒↔生产配置切换同进程即时生效、跨重启保留。
  **修复一处 V1 遗留缺陷**：tip 相同的分支（刚创建未提交的新项目）merge-base 距离互为 0 会互认
  父子成环、从根不可达——已加不可达分量修复（tip 最早者改挂根），沙盒复现并验证修复。
- [x] **A1-8 控制台完备性（本节点总验收）**（2026-07-15，用户人工交互验收通过）
  步骤：生产仓库上，关闭全部工具窗口只留控制台，依次完成：list → tree → create（沙盒名）→
  commit → delete（刚建的沙盒名）。
  通过标准：全流程仅靠手输指令完成，每条均有回显与日志落盘。
  备注：全部指令已在沙盒+生产（只读）自动化验证；用户已亲手交互复验（含确认框点击）。

> 附注（2026-07-14）：为自动化回归新增 `--yes` 启动参数（确认通道自动通过，App 层实现，
> 框架 IConfirmationService 注释预留的场景）。日常交互使用严禁携带。
> 新增 `proj.config` 指令（PJ-12 的查看入口）。

## 节点 V2-M2 —— 界面外壳（出口：UI 与指令双向一致）

- [x] **A2-1 项目总览**（2026-07-14，机制自动化验证；视觉与双击留人工）
  步骤：打开主窗口总览视图，搜索框输入关键字，双击某行。
  通过标准：列表与 proj.list 同源；过滤即时；双击后资源管理器打开对应文件夹，
  控制台回显等价 `proj.open` 指令。
  记录：视图启动即自动执行 `[UI] proj.list` 并消费其 Data（日志可证，双向同源）；
  搜索为纯视图过滤不发指令；双击 = `proj.open name="…"` 经总线。列表新增「最后提交」列
  （单次 for-each-ref 补全，不拖慢列表）。
- [x] **A2-2 继承树视图**（代码就绪，留人工：启动自动加载缓存秒开，悬停看描述，核对树形）
  步骤：打开「继承树」窗口（自动读缓存），悬停节点，全部展开/折叠；点「重新扫描」重建。
  通过标准：与生产裸仓库真实分支一致；悬停显示描述；操作流畅。
  备注（2026-07-14 更新）：①总览/继承树已改为**独立可停靠工具窗口**（win.show name=overview / tree），
  中央区回到占位页（D7）；②继承树算法重写 v2（D8）——修正 V1"同点分叉的兄弟分支被随机挂成父子"
  的缺陷，生产 34 分支重扫从 50s 降至 2s，产出树形＝一条 AGV 继承链 + 其余平铺模板下（待你核对）；
  ③树结构缓存至 data/branch-tree.json，启动秒开，「重新扫描」= proj.tree refresh=true。
- [x] **A2-3 面板 → 指令**（面板已就位，留人工点击）
  步骤：「项目操作」面板输入项目名后点创建；「提交推送」面板填分支与描述后点提交。
  通过标准：控制台回显的指令中包含输入框**当前值**（模板验收 6 的机制）；执行结果正确。
  记录：projops / projpush 两面板已注册为可停靠窗口（win.list 可见）,panel.set 反向驱动验证通过；
  指令模板占位值显式带引号，含空格/中文输入安全。motor 演示面板与 motor.* 指令已移除。
- [x] **A2-4 危险操作双路径拦截**（留人工：UI 按钮与手输各触发一次 proj.delete / commitall）
  通过标准：两条路径弹出**同一个**总线确认，措辞一致；受保护分支两条路径都直接拒绝。
  备注：两条路径代码上收敛于同一 ConfirmPrompt（机制同模板验收 9），待交互复验。
- [x] **A2-5 布局整合**（留人工：按习惯摆布局 → 重启 → layout.save）
  通过标准：布局恢复；此布局另存为命名方案（layout.save）。

> 附注（2026-07-14）：**控制台长日志显示不全问题已修复**（用户上报）——多行日志按行拆分入列表
> + 滚动单位 Item→Pixel，`ScrollToBottom` 精确到底。改动位于 AppShell.Shell/Console（谨慎区），
> 已复跑验收 8（1000 条/秒 × 30s 承压通过）。
> ✅（2026-07-15）用户批准后已回灌 z-APPShell 升基线 0.4.2，三目录哈希比对 0 差异，**未分叉**。
>
> ✅（2026-07-15）M0~M4 全部「留人工」交互项（A1-8、A2-2~A2-5、A4-1 表窗口筛选、A4-2 悬停、
> A4-3 浏览/回收站删除）已由用户亲手验收通过。

## 节点 V2-M3 —— 模块托管（出口：DLL 进目录即成指令）

> 实现说明（2026-07-15）：ModuleHost/ALC/XmlDocs 进程内移植自 MyAPI-Lite（D3），指令注册/注销经
> WPF Dispatcher 序列化；为支持模块下线，CommandRegistry 新增 `Unregister`（已随基线 0.4.2
> 回灌模板）。样例模块位于 `samples\DemoModule`（契约类编译进模块自身）。
> 自动化验收借助新增 `debug.sleep` 指令在 --exec 序列中途投放/覆盖/删除 DLL。

- [x] **A3-1 首次装载**（2026-07-15）
  记录：运行中投放 DLL+XML → 约 0.8s 后「模块装载完成: 1 个模块,4 条指令」；
  `DemoModule.Add a=1 b=2` → 3。
- [x] **A3-2 XML 注释即帮助**（2026-07-15）
  记录：`help DemoModule.Add` 显示「两个整数相加,返回和」及 a/b 参数中文说明。
- [x] **A3-3 热更新**（2026-07-15）
  记录：同一会话内覆盖新编译 DLL（Add 改 a+b+100），前后两次同指令 3 → 103，未重启。
- [x] **A3-4 下线**（2026-07-15）
  记录：删除 DLL 后调用报「✗ 未知指令: demomodule.add」，help 不再收录该域。
- [x] **A3-5 兼容性**（2026-07-15）
  记录：MyAPI-Lite 原版未重编译产物（DemoModule.dll + 独立 BaseVariable.dll + xml）零修改装载，
  Add→42、Reverse 正常（含跨 DLL 依赖在 ALC 内解析）。
- [x] **A3-6 隔离性**（2026-07-15）
  记录：Garbage.dll（乱码）被跳过告警「Bad IL format」；BadModule.Boom 异常被总线红字兜底，
  宿主继续执行后续指令；同模块 BadModule.Fine 正常。
- [x] **A3-7 重名保护**（2026-07-15）
  记录：模块域名 proj 的 list 方法被拒绝注册并告警（§5.3 禁止覆盖），内置 proj.list 行为不变；
  同模块 proj.hello 正常注册可用。
- [x] **A3-8 module.* 管理指令**（2026-07-15）
  记录：module.list 显示名称/版本/暴露模式/指令数/描述；module.reload 后 Add 仍正确；
  module.dir 显示当前目录（另有 module.open 供 UI-12 面板打开目录）。

## 节点 V2-M4 —— 数据留痕与资源（出口：操作可追溯）

- [x] **A4-1 历史入库**（2026-07-15，表窗口筛选留人工）
  步骤：执行一次 proj.commit 与 proj.delete（沙盒），打开表窗口查 push_history。
  通过标准：两条记录字段完整（时间/分支/动作/消息/结果/警告数）；表窗口筛选可用。
  记录：沙盒 9999-903 走 create→commit→delete 全流程，db.export 断言三行字段完整
  （含中文分支名与消息）；留痕失败设计为只告警不阻断主操作。表窗口筛选为模板既有功能，留人工点验。
- [x] **A4-2 分支描述**（2026-07-15，继承树窗口悬停留人工）
  记录：`proj.note name=2026-018-MyAPI text=基础设施整合:…` 写入 branch_notes 后，
  proj.tree（走缓存、未重扫）立即显示新描述——描述覆盖在读取侧进行，改描述无需重扫。
- [x] **A4-3 资源窗口接线**（2026-07-15，浏览/reveal/回收站删除留人工）
  记录：res.root 确认工作区根 = C:\OneHistory\OneHistory-Projects（DT-04 默认已接线）；
  `res.mkdir path=..\越界测试` 被拒绝「路径越出工作区根目录」。
- [x] **A4-4 P1 项清点**（2026-07-15）
  记录：PJ-09 proj.open ✔（M1）｜PJ-10 proj.scan ✔（M1）｜PJ-11 proj.repair ✔（M1 沙盒断链修复验证）
  ｜UI-12 模块管理面板 ✔（projmod.json：module.list / reload / open）｜DT-02 branch_notes ✔（本节点）
  ｜N-03 中文路径 ✔（全程中文分支名/消息/路径均正常）｜MD-08 模块配套面板 ◑——panels 目录方式可用
  （面板 JSON 放 panels\ 即生效），「模块旁自动发现」顺延 V2.1（**用户已于 2026-07-15 确认**）。

## 节点 V2-M5 —— 交付验收（出口：V1.0 退役）

- [x] **A5-1 模板关键回归**（2026-07-15）
  记录：验收 1——console 停靠左 35% → 重启恢复一致 ✓；验收 10——layout 文件写入乱码后启动,
  「布局文件损坏,已回退默认布局」告警 + 程序正常 ✓;验收 5 / 9 已由用户在 M1/M2 人工验收覆盖。
- [x] **A5-2 承压回归**（2026-07-15）
  记录：部署版 exe 对生产 34 worktree 真实 commitall(msg=V2.0交付验收批量提交),耗时 127s,
  **成功 8 | 跳过 26 | 拒绝 0 | 失败 0**;含 [34/34] 2026-018-MyAPI 本体(commit 8d380329)。
  push_history 35 条(34 逐项目 + 1 汇总)与汇总完全吻合(commitall 已升级为逐项目留痕)。
  大文件警告 2 项目(2026-005/2026-014)如实回显并计入 warnings 字段。
- [x] **A5-3 全新机器视角冒烟**（2026-07-15）
  记录：数据目录整体备份(2MB 全量核对)后删除 → 部署版首启:目录树重建、三面板种子、
  默认配置(生产路径/阈值/保护分支)正确、空模块目录 0 装载、冷启动约 1s → 备份已原样恢复。
- [x] **A5-4 发布打包**（2026-07-15）
  记录：`dotnet publish -c Release` → 项目根 `Publish\`(17 文件) + 根目录快捷方式;
  另按用户要求部署至 **C:\OneHistory\OneHistory-Push\OneHistoryStudio\** + 快捷方式
  (自提交回避:提交自身源码时运行的是部署区 exe,不是 worktree 内产物)。两处均独立冒烟通过。
- [x] **A5-5 文档收口**（2026-07-15）
  记录：Q1 关闭(M3 方法级拒绝实测)、Q3 关闭(M2 保留 V1 展开习惯)、Q2 与 MD-08 移入 V2.1;
  交付版本 **2.0.0**,跟随模板基线 **0.4.2 未分叉**(回灌记录见 TEMPLATE_VERSION.md)。
- [ ] **A5-6 V1.0 退役**
  步骤：确认日常操作全部由 V2.0 承担一周（或用户认可的观察期）。
  通过标准：用户签字确认，V1.0 目录标记「已退役 - 仅归档」，不删除。

---

## 节点 V21-M1 —— MCP 元数据层（出口：不开网络即可看到全部工具形态）

> 实现（2026-07-15）：`src\App\Mcp\CommandSchemaExporter.cs`（MC-01~04/06）+
> `McpCommands.cs`（mcp.schema = MC-05;另加 mcp.parse 调试指令,带 argsfile= 与 exec=
> 两个自动化验收辅助参数）。零框架改动;反向解析的值编码直接复用框架 `CommandParser.QuoteArg`。

- [x] **A211-1 全量导出**（2026-07-15）
  记录：`mcp.schema` 输出 63 工具 = 注册表 69 指令 − 硬排除 6（app.exit + debug.*×3 + mcp.*×2）;
  proj_list / DemoModule_Add 等合法化名称正确;危险指令带 ⚠ 标记（ConfirmPrompt 检出）。
- [x] **A211-2 Schema 正确性**（2026-07-15）
  记录：proj.create 的 required=["name"]；db.query 的 limit 为 integer 且 default=500、page default=1;
  枚举参数导出 enum。备注：proj.create 的 base 无静态 default 属正确——其缺省值是运行时读
  proj.basebranch 配置的动态行为,静态 Schema 如实不标。M1.5 已同步修订工程实施文档 A211-2，
  消除“base 有 default”的旧验收措辞，不改变运行行为。
- [x] **A211-3 热重载联动**（2026-07-15）
  记录：运行中删除 DemoModule.dll → `mcp.schema` 即刻 59 工具(65 指令);投回 → 63(69)。
  每次现算无缓存残留（MC-04）。
- [x] **A211-4 反向解析**（2026-07-15）
  记录：经 `mcp.parse exec=true` 全链路验证——含空格/中文/嵌套引号字符串
  `{"text":"含 空格和\"引号\"与中文"}` 组装为官方转义语法并执行,Reverse 输出精确反转;
  数值 `{"a":40,"b":2}` → 42;布尔 `{"refresh":false,"cached":true}` → 走缓存分支。
  执行以 `MCP:parse` 来源留痕回显（铁律 2 预演）。

## 节点 V21-M2 —— MCP 网关上线（出口：curl 级客户端可用）

> 实现（2026-07-15）：`src\App\Mcp\McpGateway.cs`(HttpListener 127.0.0.1 + JSON-RPC 2.0 无状态子集,
> 零 NuGet 新增)+ mcp.start/stop/status(MG-05)+ mcp_history 留痕(MO-01,并入 HistoryRecorder)。
> 遵守 M1.5 接续约束:网关只对话总线,未触碰 GitRunner/路径守卫。验收全程 Invoke-RestMethod 脚本化。

- [x] **A212-1 生命周期**（2026-07-15）
  记录：mcp.start → status 显示运行/端口/策略/暴露数;mcp.stop 后连接被拒(端口释放);
  mcp.autostart=true 后宿主启动即自动开服(无需 mcp.start)。
- [x] **A212-2 协议握手**（2026-07-15）
  记录：initialize → serverInfo{OneHistoryStudio, 2.1.0} + tools capability;客户端名进日志。
- [x] **A212-3 tools/list**（2026-07-15）
  记录：readonly 档 15 个(白名单);standard 档 57 个 = 65 − 8 条危险类;
  危险指令两档均不出现在清单(§6.2);未知方法 -32601、未知工具 -32602。
- [x] **A212-4 tools/call 读**（2026-07-15）
  记录：proj_list → 「共 34 个工作树」;控制台 `[cmd:MCP:ps-test] proj.list` 回显(铁律 2);
  mcp_history 记录 client/tool/result/elapsed_ms。
- [x] **A212-5 tools/call 写(standard)**（2026-07-15）
  记录：proj_note 经 MCP 写入中文描述成功,随后 proj_tree 调用可见新描述(读取侧覆盖生效)。
- [x] **A212-6 危险拒绝**（2026-07-15）
  记录：proj_delete → isError + 「须在宿主 UI/控制台人工执行(MS-04)」;宿主无删除动作;
  留痕 result=拒绝;readonly 档下 proj_create 同样被策略拒绝并留痕。
- [x] **A212-7 token**（2026-07-15）
  记录：设 mcp.token 后无 Bearer → 401(留痕 (auth)/拒绝);带对 token → 200;
  令牌热生效——运行中清空后免认证恢复,无需重启服务。
- [x] **A212-8 承压/隔离**（2026-07-15）
  记录：BadModule_Boom → isError「模块方法异常」,宿主不崩,紧接 DemoModule_Add → 3 正常。

## 节点 V21-M3 —— Codex 真实客户端联调（出口：AI 原生使用）

- [x] **A213-1 Codex 接入**（2026-07-16）
  记录：`codex mcp add onehistory --url http://127.0.0.1:8737/mcp` 注册成功；重启后 Codex
  原生发现 OneHistoryStudio 工具；`proj_list` 返回 34 个工作树，`proj_tree cached=true` 返回
  34 分支继承树，控制台与 mcp_history 均有 MCP 来源留痕。
- [x] **A213-2 模块能力直达**（2026-07-16）
  记录：DemoModule.dll 热加载为 4 条指令；Codex 新任务原生发现
  `mcp__onehistory__DemoModule_Add`，Schema 映射 a/b 两个必填整数；调用 a=1、b=2 返回 3。
- [x] **A213-3 在线热重载**（2026-07-16）
  记录：宿主与 MCP 服务不停机，DemoModule 下线时模块 2→1、工具 56→52、Demo 工具 4→0；
  HelloWorld 始终保留。恢复 DLL/XML 后模块 1→2、工具 52→56，4 个 Demo 工具恢复并再次 Add=3。
  Codex 客户端在新任务启动时刷新工具表；服务端 tools/list 则随注册表即时现算。
- [ ] **A213-4 aimcp 复核**
  记录：可选兼容性复核；按 V2.1 方案不阻塞 M3 与本版交付。

## 节点 V21-M3 —— 真实客户端联调（出口：AI 真的在用）

- [x] **A213-1 / A213-2 / A213-3**（2026-07-15，客户端换用 Codex,验收语义不变）
  记录：详见 [07-V2.1.0-M3-Codex联调测试.md](./07-V2.1.0-M3-Codex联调测试.md)——Codex 以
  streamable_http 原生注册并发现工具;proj_list/proj_tree(cached) 原生调用通过;
  DemoModule_Add 经「DLL→指令→MCP」两跳返回 3;服务不停机完成模块下线/恢复,
  服务端工具表 56↔52 同步(客户端任务快照特性如实分开记录)。
- [x] **A213-4 aimcp 复核**：可选项,不阻塞 V2.1.0(留待用户按需复测)。

## 节点 V21-M4 —— MD-08 面板自动发现 + 收口（出口：V2.1.0 交付）

> 实现（2026-07-16）：`src\App\Modules\ModulePanelSync.cs` + ModuleHost.ReloadCompleted 事件,
> 零框架改动。语义按 MP-01 原文「等效于放进 panels 目录」:模块旁 `<模块名>.panel.json`
> 自动并入/回收 panels 副本(module- 前缀,不碰用户手写面板)+ 自动 panel.reload——
> **既有面板热刷新即时生效;全新面板窗口按框架 P-08 约定重启后出现**(控制台如实提示)。
> 运行时动态增删停靠窗口属 DockingHost 冻结区改造,列 V2.2 候选(见 04 文档开放问题)。

- [x] **A214-1 面板自动发现**（2026-07-16）
  记录：运行中投放 DemoModule.panel.json → 自动并入 panels/module-DemoModule.json +
  panel.reload +「新增面板需重启后生效: demopanel」;重启 → demopanel 成窗(win.list 可见,
  panel.set 可驱动);运行中整组移除模块 → 模块 2→1、面板副本自动回收;再重启 → 窗口消失。
  MP-03 样例 DemoModule.panel.json 随 samples\DemoModule 提供(按钮驱动 Add/Reverse)。
- [x] **A214-2 全量回归**（2026-07-16）
  记录：①验收 8 承压 1000 条/秒×30s 通过;②布局恢复 console 左 40% 重启一致(已复位);
  ③沙盒 commitall 成功 1/跳过 1/零失败;④模块 A3-3 热更新 3→103(样例已还原)。
- [x] **A214-3 文档收口**（2026-07-16）
  记录：04 文档状态改「已交付」;交付版本 **2.1.0**;TEMPLATE_VERSION.md 登记;
  框架三目录与模板 0.4.2 哈希比对 **0 差异,未分叉**;
  发布已同步部署区 C:\OneHistory\OneHistory-Push\OneHistoryStudio\(冒烟通过:
  MCP 自启动 + 2 模块 + demopanel 成窗)。

## 版本 V2.1.1 —— MCP 工具管理页 + 模块管理页（小升级）

> 实现（2026-07-16）：①「MCP 工具」页(win.show name=mcp,顶部标签组)——全部工具清单
> (搜索/危险标记/自定义标记)+ 提示词查看/编辑/保存/恢复默认;②「模块管理」页
> (win.show name=modules)——已装载模块清单(名称/版本/暴露模式/指令数/来源/描述)+
> 选中模块的注册指令明细(双击行发 help)+ 重载/打开目录按钮;③新指令 `mcp.desc`
> (提示词覆盖存 main 库 mcp_descriptions 表,导出读取侧合成)。
> 页面全部经总线消费数据与执行动作(架构不变量 1);零框架改动。

- [x] **提示词管理全链路**（2026-07-16，独立端口 8747 验证,未打扰用户运行中的实例）
  记录：`mcp.desc name=proj.list text=…` 保存 → 独立实例 tools/list 的 description 即为覆盖文本
  (跨进程持久化 ✓);`reset=true` → 下次 tools/list 立即回默认(导出现算零缓存 ✓)。
- [x] **构建与共存**（2026-07-16）
  记录：0 错误;测试实例(8747)与用户两个运行中实例(8737)并存无扰,main.db 多进程共享正常。
- [ ] **页面视觉与交互**（留人工:重启后顶部标签组见「MCP 工具」「模块管理」两页,
  编辑提示词点保存、模块页双击指令行看 help)
- [ ] **部署区同步**：Publish\ 已是 2.1.1;部署区 exe 被运行中实例占用,**关闭实例后需同步**
  (Copy Publish\* → C:\OneHistory\OneHistory-Push\OneHistoryStudio\)。

## 验收签署

| 节点 | 完成日期 | 验收人 | 备注 |
|---|---|---|---|
| V2-M0 骨架派生 | 2026-07-14 | | 五项全过 |
| V2-M1 proj.* 指令域 | 2026-07-15 | | 自动化 + 用户交互复验（A1-8）全过 |
| V2-M2 界面外壳 | 2026-07-15 | | 含控制台修复与继承树算法 v2 / 缓存；用户交互复验全过 |
| V2-M3 模块托管 | 2026-07-15 | | A3-1~8 全过；Unregister 随 0.4.2 回灌模板 |
| V2-M4 数据留痕 | 2026-07-15 | | A4-1~4 过；MD-08 自动发现顺延 V2.1（用户确认） |
| V2-M5 交付验收 | 2026-07-15 | | A5-1~A5-5 全过；仅剩 A5-6 V1.0 退役（观察期后用户签字） |
| V21-M3 Codex 联调 | 2026-07-16 | Codex / 用户 | A213-1~3 全过；aimcp 可选复核不阻塞 |
