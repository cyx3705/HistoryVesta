# OneHistory-V2.0 · 个人项目管理工具 —— 需求文档

> 文档性质：需求定义（不含实现代码）
> 定位：z-APPShell 模板的**首个正式派生应用**
> 版本：v1.0 ｜ 日期：2026-07-14 ｜ 状态：核心决策已给出默认判定，开放问题见 §9
> 配套文档：[00-开发提示.md](./00-开发提示.md)（开发者/AI 必读）｜ [02-项目验收单.md](./02-项目验收单.md)（验收节点与步骤）

---

## 0. 一句话定义

OneHistory-V2.0 是基于 z-APPShell 框架派生的**个人项目管理工具**：以指令化方式提供
OneHistory 全项目库的 Git 分支/工作树管理能力（承接 b-Code-OneHistory-V1.0 的全部功能），
并融合 MyAPI-Lite 的 **DLL 模块热重载**机制作为能力扩展层——今后所有分散的小工具都以
模块 DLL 的形式装进这一个程序。

## 1. 背景与目标

### 1.1 背景

- 历史上积累了一批电脑控制与项目管理小工具，分散、页面杂乱、各自维护，效率低。
- b-Code-OneHistory-V1.0（WPF 单窗体）承载着 OneHistory 项目库的日常管理（裸仓库 +
  worktree 模式），功能已验证但架构无法扩展。
- z-APPShell 模板（基线 0.4.1-M4）已完成 M1~M4 并通过全部十条验收，具备五类标准窗口 +
  指令总线 + SQLite + 日志体系，正是为这类整合而生。
- MyAPI-Lite 提供了零依赖的 DLL 热重载托管模式（已冻结，2026-07-07），其
  ModuleHost / Invoker 机制可作为能力扩展的技术蓝本。

### 1.2 设计目标

| 编号 | 目标 |
|---|---|
| G1 | **功能对齐**：V1.0 的全部 Git 管理功能在 V2.0 中有等价实现，V1.0 可退役 |
| G2 | **一切皆指令**：全部功能先有指令、后有 UI，继承模板三条架构不变量 |
| G3 | **能力可扩展**：新小工具以模块 DLL 放入 Modules 目录即成为新指令域，主程序永不为单个工具改代码 |
| G4 | **操作可追溯**：每次提交/推送/删除留痕入库，表窗口可查历史 |
| G5 | **半月内交付**：依托模板派生流程（模板需求文档 §9），骨架半天、全功能两周内 |

### 1.3 非目标（首版明确不做）

- 不做 HTTP / MCP 对外服务（aimcp 接入现存问题，列为 P2 预留，见 §9-Q2）。
- 不做远程仓库托管配置（GitHub remote 沿用现有仓库既有配置）。
- 不修改 z-APPShell 的 AppShell.Core / Services / Shell 三个只读项目（除非按演进手册上报流程）。
- 不修改 MyAPI-Lite 本体（其已代码冻结；只移植其机制，不引用其进程）。
- 不做多用户 / 权限 / 网络协同。

## 2. 术语与既有资产

| 术语 | 含义 |
|---|---|
| 裸仓库 | `C:\OneHistory\OneHistory-Projects\OneHistory-Projects.git`，全部项目分支的唯一存储 |
| 工作树根目录 | `C:\OneHistory\OneHistory-Projects`，每个项目 = 一个分支 = 一个同名 worktree 文件夹 |
| 模板分支 | `0000-000-Template`，所有新项目分支的默认基础分支（受保护） |
| 受保护分支 | `0000-000-Template` / `main` / `master`，删除操作一律拒绝 |
| 模块（Module） | 放入 Modules 目录的 DLL，含 `ModuleInfoBase` 子类者暴露为指令域 |
| V1.0 | b-Code-OneHistory-V1.0\OneHistoryGitTool（被替代的旧工具） |
| 模板 / Shell | z-APPShell，基线 0.4.1-M4，见其 docs/TEMPLATE_VERSION.md |

## 3. 总体架构

```
┌────────────────────────────────────────────────────────┐
│ 模块层（Modules\*.dll，热重载）                            │
│   各历史小工具逐步迁移为模块 → 自动注册为指令域              │
├────────────────────────────────────────────────────────┤
│ App（派生应用层，本项目全部开发工作所在）                    │
│   · Git 项目管理指令域 proj.*                             │
│   · 模块托管指令域 module.*（移植 MyAPI-Lite 机制）         │
│   · 主窗口内容（项目总览视图）                              │
│   · 控制面板 JSON ＋ 菜单项 ＋ 数据表结构                   │
├────────────────────────────────────────────────────────┤
│ z-APPShell 模板（只读区，基线 0.4.1-M4）                   │
│   AppShell.Shell / AppShell.Services / AppShell.Core     │
│   五类标准窗口 · 指令总线 · SQLite · 日志 · 布局持久化       │
└────────────────────────────────────────────────────────┘
```

依赖铁律沿用模板演进手册 §2：依赖方向永远向下；AvalonDock 类型只出现在 Shell 内部；
模块 DLL 只依赖模块契约（BaseVariable 模式），不反向依赖 App。

## 4. 功能需求 A —— Git 项目管理（指令域 `proj`）

> 来源：V1.0 全部功能的指令化重写。V1.0 中可复用的纯逻辑类
> （WorktreeFileScanner / WorktreeLfsHelper / BranchNode 解析）允许移植源码到 App 层。
> 优先级：P0 必须（首版）｜ P1 应该 ｜ P2 可选

| 编号 | 需求描述 | 对应 V1.0 功能 | 优先级 |
|---|---|---|---|
| PJ-01 | `proj.list [filter=]`：列出全部 worktree（编号、分支名、路径、状态），支持关键字过滤 | 刷新工作树列表 + 搜索 | P0 |
| PJ-02 | `proj.create name= [base=0000-000-Template]`：创建分支 + worktree。校验：名称非空、无非法文件名字符、分支不存在、目标目录不存在；预览等价 git 命令后执行 | 创建分支+工作树 | P0 |
| PJ-03 | `proj.delete name=`：先移除 worktree 再强制删分支。受保护分支直接拒绝；其余走总线 ConfirmPrompt 二次确认（UI 与手输同一闸口） | 删除分支+工作树（强警告） | P0 |
| PJ-04 | `proj.tree`：输出分支继承树（基于裸仓库真实提交图），含每分支描述与最后提交时间 | Git 操作继承树 | P0 |
| PJ-05 | `proj.commit name= msg=`：对单个 worktree 执行大小检查 → LFS 处理 → git add . → commit。≥50MB 警告继续并留痕；≥100MB 检查 LFS 指针，未启用则确认，拒绝则跳过该项目 | 提交到本地仓库 | P0 |
| PJ-06 | `proj.push name=`：git push origin 分支名，结果回显 | 推送到 GitHub | P0 |
| PJ-07 | `proj.commitall msg=`：遍历全部 worktree 逐个执行 PJ-05 流程，汇总成功/跳过/拒绝/失败四类结果 | 一键推送全部到裸仓库 | P0 |
| PJ-08 | `proj.pushall`：推送全部分支到 GitHub（git push --all origin） | 一键推送 GitHub | P0 |
| PJ-09 | `proj.open name=`：在系统资源管理器中打开该 worktree 文件夹 | 打开工作树文件夹 | P1 |
| PJ-10 | `proj.scan name=`：仅执行大文件扫描并输出报告（≥50MB / ≥100MB 分级），不提交 | 推送前检查（独立化） | P1 |
| PJ-11 | `proj.repair`：worktree 断链批量修复（prune → 按分支清单重建），流程按主项目 Note.txt 既有方案实现，执行前二次确认 | Note.txt 手工修复方案 | P1 |
| PJ-12 | 裸仓库路径 / 工作树根目录 / 基础分支 / 大小阈值均为配置项（app.set/app.get 可读写），默认值取 §2 现值 | 仓库配置区 | P0 |
| PJ-13 | 长任务（commitall / pushall / repair）异步执行并上报进度，不阻塞 UI | —（V1.0 为 async） | P0 |

## 5. 功能需求 B —— 模块托管（指令域 `module`）

> 来源：MyAPI-Lite 的 ModuleHost / Invoker 机制**进程内移植**（判定见 §9-D3）。
> MyAPI-Lite 本体保持冻结不动；HTTP / MCP 部分首版不移植。

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| MD-01 | 模块目录默认 `<应用数据目录>/Modules`（可配置）。放入/覆盖/删除 DLL 自动热重载（约 1s，800ms 防抖），无需重启 | P0 |
| MD-02 | 模块契约沿用 MyAPI-Lite 的 `ModuleInfoBase` 鸭子类型识别（按基类全名匹配），**既有为 MyAPI-Lite 编写的模块 DLL 不改一行即可装入** | P0 |
| MD-03 | 模块业务类的公共方法自动注册为指令：`<模块名>.<方法名> [参数=值 ...]`，与内置指令同表、help 自动收录；XML 注释成为帮助文本 | P0 |
| MD-04 | 参数绑定支持基本类型 + 默认值可省略；返回对象序列化为 JSON 回显控制台 | P0 |
| MD-05 | `module.list`：列出已加载模块（名称/版本/描述/方法数）；`module.reload`：手动整体重载；`module.dir [path=]`：查看/切换模块目录 | P0 |
| MD-06 | 模块隔离：模块方法抛出的异常由指令总线兜底（N-05），模块加载失败只下线该模块并告警，不影响宿主与其他模块 | P0 |
| MD-07 | 热重载后指令注册表同步增删；与内置指令重名时**拒绝加载该方法**并告警（不静默覆盖，沿用模板 §5.3 规则） | P0 |
| MD-08 | 模块可声明配套控制面板 JSON（放入模块旁或 panels 目录），面板按钮触发模块指令 | P1 |
| MD-09 | DemoModule（Calculator）作为验收样例模块随项目提供 | P0 |
| MD-10 | 对外 HTTP / MCP 暴露：预留，不在首版（见 §9-Q2） | P2 |

## 6. 功能需求 C —— 界面与数据

### 6.1 主窗口（中央区内容）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| UI-01 | 主窗口 = **项目总览视图**：worktree 列表（编号/分支/路径/最后提交时间），顶部搜索框过滤，双击行 = proj.open | P0 |
| UI-02 | 继承树视图：分支继承树（TreeView），悬停显示完整描述，展开/折叠；与 proj.tree 同源 | P0 |
| UI-03 | 主窗口一切操作按钮均组装指令文本经总线执行（架构不变量 1），控制台可见等价回显 | P0 |
| UI-04 | 总览与继承树的组织形式（Tab 切换或上下分区）由实现自定，保持简洁 | P0 |

### 6.2 控制面板（JSON 声明）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| UI-10 | 「项目操作」面板：项目名输入框 + 基础分支下拉 + 创建按钮（proj.create）+ 打开按钮（proj.open） | P0 |
| UI-11 | 「提交推送」面板：分支名输入 + 提交描述输入 + 提交按钮（proj.commit）+ 推送按钮（proj.push）+ 一键提交全部（proj.commitall，danger 样式） | P0 |
| UI-12 | 「模块管理」面板：重载按钮（module.reload）+ 打开模块目录按钮 | P1 |

### 6.3 数据与资源

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| DT-01 | SQLite main 库新增 `push_history` 表：时间、分支、动作（commit/push/delete/create）、消息、结果、大文件警告数——proj.* 每次变更类操作自动写入 | P0 |
| DT-02 | `branch_notes` 表：分支名 → 项目描述，继承树悬停与 proj.tree 从此表取描述；提供 proj.note name= text= 写入 | P1 |
| DT-03 | 表窗口默认连接 main，push_history 可查、可筛选（复用模板表窗口，无需开发） | P0 |
| DT-04 | 资源窗口工作区根目录默认指向 `C:\OneHistory\OneHistory-Projects`（只读浏览为主，删除等写操作沿用模板回收站+确认语义） | P0 |
| DT-05 | 应用名定为 **OneHistoryStudio**（数据目录 `%AppData%\OneHistoryStudio\`），窗口标题「OneHistory 项目管理工具 V2.0」 | P0 |

## 7. 非功能需求

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| N-01 | 沿用模板全部非功能基线：冷启动 ≤2s、控制台承压、异常不崩溃、布局损坏回退 | P0 |
| N-02 | git 进程调用统一封装（工作目录、UTF-8 输出、超时、ExitCode 判定），禁止散落 Process.Start | P0 |
| N-03 | 中文路径兼容：git 调用设置 `core.quotepath=false` 语义读取输出；worktree 断链场景由 PJ-11 兜底 | P1 |
| N-04 | 破坏性操作（delete / repair / 无确认批量）必须过总线 ConfirmPrompt 单闸口，禁止旁路 MessageBox 自成体系 | P0 |
| N-05 | 构建环境：net8.0-windows；dotnet CLI 构建需 `DOTNET_EnableWriteXorExecute=0`；NuGet 仅 nuget.org（见模板 TEMPLATE_VERSION.md） | P0 |

## 8. 里程碑总览

> 每个里程碑的**出口标准与验收步骤**详见 [02-项目验收单.md](./02-项目验收单.md)，此处只给范围。

| 阶段 | 内容 | 预估 |
|---|---|---|
| V2-M0 | 模板派生：拷贝解决方案、更名 OneHistoryStudio、启动冒烟、TEMPLATE_VERSION 登记 | 0.5 天 |
| V2-M1 | proj.* 指令域全量（PJ-01~13），纯控制台可驱动全流程 | 3 天 |
| V2-M2 | 主窗口内容（UI-01~04）+ 控制面板 JSON（UI-10~12） | 2 天 |
| V2-M3 | 模块托管（MD-01~09），DemoModule 热重载验收 | 3 天 |
| V2-M4 | 数据留痕（DT-01~03）+ 资源窗口接线（DT-04）+ P1 项收尾 | 2 天 |
| V2-M5 | 整体验收 + 回归模板关键验收 + 发布打包 + V1.0 退役 | 1 天 |

## 9. 决策记录与开放问题

### 已判定（默认方案，如需推翻请在开发前提出）

| # | 判定 |
|---|---|
| D1 | **集成方式 = 模板派生**：拷贝 z-APPShell 解决方案，只改 App 项目；AppShell.* 三目录视为只读（演进手册 §3） |
| D2 | **V1.0 功能全部指令化重写**，不搬 WPF 窗体代码；纯逻辑类（文件扫描/LFS/分支树解析）可移植源码 |
| D3 | **模块托管 = 进程内移植** MyAPI-Lite 的 ModuleHost/Invoker/ALC 机制（约两个源文件的体量），模块方法注册为指令而非 HTTP 端点；MyAPI-Lite 本体不动、不作为外部进程依赖 |
| D4 | **模块契约兼容**：识别 `BaseVariable.ModuleInfoBase` 鸭子类型，既有模块 DLL 直接可用 |
| D5 | **应用名 OneHistoryStudio**；配置/数据/模块目录全部落 `%AppData%\OneHistoryStudio\` |
| D6 | **仓库路径等运行参数走配置服务**（app.set），不再像 V1.0 一样写死在 UI 文本框 |
| D7 | **（2026-07-14 M2 中调整）项目总览与继承树为独立可停靠工具窗口**（Id=overview/tree，默认顶部标签组），中央区保留模板占位页——取代 UI-01/UI-04 原定的"主窗口内容"方案 |
| D8 | **（2026-07-14 新增）继承树算法 v2 + 文件缓存**：分叉点须为候选父分支的自有提交（模板血统不构成父子证据）+ 编号顺序不变量（父编号必小于子编号）；结果缓存于 data/branch-tree.json（带裸仓库路径校验），proj.tree 默认读缓存、refresh=true 重扫 |

### 开放问题（2026-07-15 全部关闭或移入 V2.1）

| # | 问题 | 状态 |
|---|---|---|
| Q1 | 模块指令域与内置域重名时的处理粒度 | **已关闭（V2-M3）**：按 MD-07 仅方法级拒绝，A3-7 实测（模块伪装 proj.list 被拒、proj.hello 正常注册） |
| Q2 | 对外 MCP/HTTP 服务的实现方式 | **移入 V2.1 规划**，与 MD-08「模块旁面板自动发现」（2026-07-15 用户确认顺延）一并 |
| Q3 | 继承树默认展开等展示细节 | **已关闭（V2-M2）**：保留 V1 习惯（根+一级全展开+每支前 5 个二级） |

## 10. V2.3.0 分支历史与安全回滚增量

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| PH-01 | `proj.history` 以裸仓库 Git 提交为真值，显示分叉点基线和当前分支第一父链自有提交 | P0 |
| PH-02 | 项目总览与继承树选择通过 `ProjectSelectionState` 同步左侧分支历史窗口 | P0 |
| PH-03 | 历史节点提供结构化详情、与当前 HEAD 的差异和真正分页；命令日志只输出摘要 | P0 |
| PH-04 | origin 状态分为已推送、仅本地和远端未知；只有显式 remote=true 才刷新跟踪引用 | P1 |
| PH-05 | `proj.rollback` 生成恢复提交并保留历史；工作树不干净或确认后 HEAD 变化时拒绝 | P0 |
| PH-06 | `proj.reset` 只允许非保护分支且只改本地；`proj.forcepush` 固定使用 `--force-with-lease` | P0 |
| PH-07 | rollback/reset/forcepush 继续写入 push_history 操作审计，不新增数据库表 | P1 |

V2.3.0 沿用 D8 的继承树父子关系，不开发 merge/rebase/cherry-pick 图形工作流；完整设计与
安全边界见 [22-V2.3.0-分支历史与安全回滚.md](./22-V2.3.0-分支历史与安全回滚.md)。

## 11. V2.3.1 子模块提交与推送增量

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| SM-01 | 以父索引 `160000 gitlink` 识别直属子仓库，同时兼容标准 `.gitmodules` 与历史嵌入式仓库 | P0 |
| SM-02 | `proj.commit/commitall` 支持 `submodules/submsg`，全量预检后按子仓库先、父仓库后提交 | P0 |
| SM-03 | `proj.push/pushall` 支持子仓库优先推送，任一子仓库失败时禁止父仓库推送 | P0 |
| SM-04 | CLI/MCP 默认 `submodules=false` 保持兼容，两个正式页面默认显式发送 true | P0 |
| SM-05 | detached、未初始化、越界、缺少身份、脏推送、指针不一致和第二层 gitlink 变化均在写入前拒绝 | P0 |
| SM-06 | 多仓库部分成功不自动回退；结构化结果与 push_history 准确记录前后 SHA 和失败点 | P1 |
| SM-07 | 父项目大文件扫描必须剪枝 gitlink 目录，每个脏子仓库独立执行现有 LFS 检查 | P1 |

本版不自动创建/修复 `.gitmodules`，不递归处理第二层子模块，不创建远端、不设置 upstream、
不 reset、不强推。完整合同见 [23-V2.3.1-子模块提交与推送.md](./23-V2.3.1-子模块提交与推送.md)。

—— 文档结束 ——
