# SE2SW V3.0 发布与部署收口

> 日期：2026-08-01  
> 目标版本：3.0.0  
> 状态：正式槽已部署；窗口布局人工动作待复核

> 后续：3.0.0 的两个独立窗口入口已在 3.0.1 合并为一个普通工具窗口内的“零件转换 / 装配转换”双页；
> 详见 [14-V3.0.1-零件与装配双页入口修复.md](14-V3.0.1-零件与装配双页入口修复.md)。本文件保留 3.0.0
> 首次装配发布事实，不反写历史结果。

## 发布边界

- 只发布 SE2SW UI、Contracts、Worker 与 XML 文档；
- `tools/AssemblyProbe`、`tools/AssemblyProductionGate`、CAD fixture、转换附件和本目录实施文档不进入模块槽；
- 不向 OHS `b-Office/evidence` 上抛 SE2SW 实现材料；
- AppShell 只消费冻结公共包 `OneHistory.AppShell.Core 3.0.3`，不修改 AppShell/OHS 产品代码。

## 版本投影

| 位置 | 值 |
|---|---|
| `src/SE2SW/SE2SW.csproj` | 3.0.0 |
| `src/SE2SW.Contracts/SE2SW.Contracts.csproj` | 3.0.0 |
| `src/SE2SW.Worker/SE2SW.Worker.csproj` | 3.0.0 |
| `ModuleInfo.Version` | 3.0.0 |
| `z-SE2SW/module.manifest.json` | 3.0.0 |
| AppShell Core | 3.0.3 |

## 正式门禁

- [x] Release 构建 0 warning / 0 error；
- [x] `SE2SW.Smoke` PASS；
- [x] `SE2SW.UiSmoke` 构建通过；
- [x] 五个项目的 `dotnet format --verify-no-changes --no-restore` 全部通过；
- [x] L0–L4 真实装配门禁；
- [x] V2.3.1 双烧杯真实 CAD 回归；
- [x] 正式 2.3.1 槽已复制并逐文件核验到 `%APPDATA%\OneHistoryStudio\ModuleBackups`；
- [x] `tool.sync name=SE2SW` 重建正式槽；
- [x] `tool.list` 显示 SE2SW 3.0.0、槽状态“在位”；
- [x] `module.list` 显示 `se2sw 3.0.0`；
- [x] 正式槽与 Release/清单来源 8/8 文件 SHA-256 一致；
- [x] OHS 实际可访问性树同时出现“SE2SW 格式转换”和“SE2SW 装配转换”，装配窗口具有普通工具窗口的
  “窗口位置 / 自动隐藏 / 隐藏”控件；
- [ ] 在无人操作 OHS 时补做两个窗口的拖动、浮动、停靠、最大化、复位和释放人工动作。

## 2026-08-01 正式部署记录

### 构建与测试

最终门禁在 `b-Code-SE2SW` 执行：

- `src/SE2SW/SE2SW.csproj` Release：0 warning / 0 error；
- `tests/SE2SW.UiSmoke/SE2SW.UiSmoke.csproj` Release：0 warning / 0 error；
- `tests/SE2SW.Smoke/SE2SW.Smoke.csproj`：`SE2SW.Smoke: PASS`；
- UI、Smoke、UI Smoke、AssemblyProbe、AssemblyProductionGate 五个项目格式门禁全部为退出码 0。

### 回退备份

部署前正式槽为 `se2sw 2.3.1`，共 8 个文件。先复制备份并完成 8/8 SHA-256 比对：

`C:\Users\Administrator\AppData\Roaming\OneHistoryStudio\ModuleBackups\SE2SW-v231-pre-v300-20260801-20260801-173147`

OHS 2.8.2 的历史无溯源槽存在同名升级边界：已加载的 `se2sw.*` 指令会让 `tool.scan` 把同名清单误判为
“模块名与内置指令域冲突”，而该旧槽又不在 `tool.list` 中，无法走 `tool.remove`。为继续使用标准同步链，旧槽被
整体移动到第二份可恢复备份，未删除任何文件：

`C:\Users\Administrator\AppData\Roaming\OneHistoryStudio\ModuleBackups\SE2SW-v231-detached-for-v300-20260801-20260801-173344`

随后执行 `module.reload`，确认 2.3.1 已卸载且其他三个模块仍在；再次 `tool.scan` 后 SE2SW 3.0.0 清单合法，
再执行 `tool.sync name=SE2SW`，结果为 `同步完成: 1/1 个工具`。

### 正式槽和溯源

- 正式槽：`C:\Users\Administrator\AppData\Roaming\OneHistoryStudio\Modules\SE2SW`；
- 同步时间：2026-08-01 17:34:12；
- `tool.list`：SE2SW 3.0.0，来源 `2026-019-Studio工具箱`，槽状态“在位”；
- 主程序集溯源 SHA-256：`E0E545533FDD8E0A194785EA2236CECA35449850C08D726EA2623FFECAD036EB`；
- `module.list`：`se2sw 3.0.0`，描述为“Solid Edge 零件与展平装配体转换为 SolidWorks”。

| 正式槽文件 | SHA-256 |
|---|---|
| `module.manifest.json` | `4EC876A769BB536631851C2D410AE50930D50D898D15E7203F87AC8CF5039759` |
| `SE2SW.Contracts.dll` | `93068BDF7AE181E06702F93A7B069C8482B2783EEA7A2ABC93BC8601A6EAA352` |
| `SE2SW.dll` | `E0E545533FDD8E0A194785EA2236CECA35449850C08D726EA2623FFECAD036EB` |
| `SE2SW.Worker.deps.json` | `2717F003F662C5A002D12C44FEB6FC761F6258F819A5763E1AE3268070846F68` |
| `SE2SW.Worker.dll` | `1FF869D291FE113643DB52FC862B8FAB5E3D0410686E880C77A54BE244EC1721` |
| `SE2SW.Worker.exe` | `24877A30AB7F80854DF6BEA9F5A9B4C3FE5087A716D24852FDE71E01A9DA93C3` |
| `SE2SW.Worker.runtimeconfig.json` | `CA1A6B5B185174698E0F5F9AE732AD3D2D3ED5B122ED666F35CC214283E83B7F` |
| `SE2SW.xml` | `1F570AF01F8DB6015F78C37BD9108DAF4983FBD5AD8CA865CCFBAE322CD48D43` |

### 窗口现场

OHS 2.8.2 热重载后的实际可访问性树同时出现：

- `SE2SW 格式转换`；
- `SE2SW 装配转换`；
- 装配窗口完整的 `.asm` 选择、探查、装配树、唯一零件表、失败策略与转换控件；
- 普通工具窗口的“窗口位置 / 自动隐藏 / 隐藏”控件。

`win.list` 是 `frontend:OneHistoryStudio` 的 UI 线程指令，当前 MCP 策略显示 `PolicyVisible=false`，不能从本任务
直接调用。尝试使用桌面控制器切换 OHS 控制台时连续检测到用户输入，控制器按安全规则拒绝点击，因此没有冒充执行
结果。双窗口显示已经现场确认；拖动、浮动、停靠、最大化和复位留作无人操作 OHS 时的最终人工动作。

### 进程与文档边界

- 收尾时 `SE2SW.Worker` 与 `Edge` 均无进程；
- 工作站保留一个 16:10:20 启动的 `SLDWORKS` DCOM 会话。生产装配门禁和双烧杯回归目录分别创建于
  17:19:40、17:20:52，该会话早于门禁，属于门禁前基线，不作为 SE2SW 自有进程关闭；
- OHS `b-Office/evidence` 当前不存在，未上抛 SE2SW 实现文档、CAD fixture 或转换附件。

## 回退规则

若后续窗口人工动作发现生产缺陷，先把当前 3.0.0 槽整体移出正式目录，再从上述任一 2.3.1 备份恢复
`Modules\SE2SW`，执行 `module.reload` 并核验 `module.list` 回到 2.3.1。不得在正式槽内混合覆盖不同版本文件。
