# SE2SW 2.2.0 生命周期部署与外界模式实测

> 文档归属：019 Studio 工具箱 `b-Code-SE2SW/docs`。本文包含 SE2SW 的实现验证与转换验收，禁止复制或
> 上抛到 OneHistoryStudio 的 `b-Office`；OHS 只保留版本、哈希、注册和装载等宿主集成事实。

验证时间：2026-07-27 20:51-20:54（Asia/Shanghai）。

## 自动化

- `SE2SW.Smoke` Debug：PASS。
- `SE2SW.Smoke` Release：PASS。
- `SE2SW` Release：0 警告、0 错误。
- `SE2SW.UiSmoke` Release：0 警告、0 错误；测试壳为人工交互窗口，不记作自动退出测试。
- 活动转换用例使用可控假 Worker，验证 Dispose 发送取消、等待 Worker 延迟退出、等待 `StartAsync` 完成、
  销毁后禁止再次启动，并撤销尚未执行的进度回调。

## 部署

- 正式槽：`%AppData%\OneHistoryStudio\Modules\SE2SW`。
- 本轮只替换发生变化的 `SE2SW.dll`；Worker、Contracts、XML 与清单未变。
- Release/槽内 SHA-256：`C42DFF2E3395B8DC3AC35BBDC0FE697E8B85D6D7C688378FE3C9B021537385BB`。
- 旧 DLL 回退：`%AppData%\OneHistoryStudio\ModuleBackups\SE2SW-lifecycle-prepatch-20260727-205128`。
- 最终重启后前端 PID 19812，服务 PID 20740；20:58:49 日志装载 `se2sw 2.2.0`。

## 正式 MCP

- `initialize`：OneHistoryStudio 2.7.0，协议 `2025-06-18`。
- `page.list`：仅一个活动页 `se2sw`，owner=`SE2SW`。
- `page.open id=se2sw`、`page.activate id=se2sw`：成功。
- `page.close id=se2sw`：按 `CanClose=false` 明确拒绝；再次 `page.list` 页面仍唯一且活动。
- `win.max name=se2sw`、`win.restore`：成功。
- 顶层窗口只有 `OneHistoryStudio v2.7.0`，无独立 SE2SW 窗口。
- 本次启动除主动验证关闭拒绝形成的预期 Error 外，无其他 Warn/Error。

## 正式空闲热重载

- 21:00:20 在运行态以同一最终 DLL 原位覆盖正式槽，前端 PID 19812、服务 PID 20740 均未退出。
- 21:00:21 日志完成 5 个模块/13 条指令装载，SE2SW 仍为 2.2.0。
- 重载后 `page.list` 仍只有一个活动 `se2sw`，无页面重影，无 Warn/Error。

## 外界模式实机转换摘要

- 20:22 首次样件在 Solid Edge 导出 XT 阶段返回 `0x80004004 (E_ABORT)`，未生成后续零件；该失败用于确认
  Worker 能把 COM HRESULT 和失败阶段完整回传。
- 20:25 更换样件重试成功：XT 为 7,303 字节，SolidWorks 零件为 56,428 字节。
- FeatureWorks 已按需加载；`RecognizeFeatureAutomatic` 返回 0，未识别到可编辑特征，按设计降级为导入实体。
- 特征识别耗时 2,265 ms；原生错误码和警告码均为 0，最终转换事件 `isError=false`。
- 原始请求曾包含本机桌面绝对路径。2026-07-28 文档治理时已将必要事实汇总于本节，并删除请求 JSON、
  事件 JSONL 和两个空错误日志，避免把设备路径及可再生成输出继续留在仓库。
