A lightweight, message-driven plugin control system built on .NET. This project serves as a host for independent business modules, enabling HTTP-triggered actions such as displaying module information in the browser/CMD and monitoring module-level performance metrics. The architecture prioritizes minimalism, with the MyAPI host acting only as a router and each business module encapsulating its own functionality and display logic.
