# AppShell 3.1.9

This is the current AppShell host snapshot.

- host/AppShell.exe: win-x64, framework-dependent AppShell host.
- docs/: current API, module, runtime, change, and UI style contracts.
- AppShell.reuse.md: minimal entry point for projects and AI consumers.
- manifest.json and SHA256SUMS: snapshot identity and integrity.

Run host/AppShell.exe. Historical releases are stored under -Publish/history/.