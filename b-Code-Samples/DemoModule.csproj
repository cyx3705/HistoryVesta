<Project Sdk="Microsoft.NET.Sdk">

  <!-- 验收样例模块(MD-09):独立编译,产物 DemoModule.dll + DemoModule.xml
       放入消费应用的 <数据目录>\Modules 即自动装载为 DemoModule.* 指令域。
       构建:dotnet build b-Code-Samples(注意 DOTNET_EnableWriteXorExecute=0) -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

</Project>
